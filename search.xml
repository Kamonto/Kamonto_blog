<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>OS2025 上机考试全攻略</title>
      <link href="/Kamonto_blog/2025/07/03/OS2025%20%E4%B8%8A%E6%9C%BA%E8%80%83%E8%AF%95%E5%85%A8%E6%94%BB%E7%95%A5/"/>
      <url>/Kamonto_blog/2025/07/03/OS2025%20%E4%B8%8A%E6%9C%BA%E8%80%83%E8%AF%95%E5%85%A8%E6%94%BB%E7%95%A5/</url>
      
        <content type="html"><![CDATA[<h1 id="OS2025-上机考试全攻略"><a href="#OS2025-上机考试全攻略" class="headerlink" title="OS2025 上机考试全攻略"></a>OS2025 上机考试全攻略</h1><h2 id="Lab-0-Exam"><a href="#Lab-0-Exam" class="headerlink" title="Lab 0 Exam"></a>Lab 0 Exam</h2><details id="lab0-exam">  <summary>点击查看详细题目</summary><h3 id="题目描述"><a href="#题目描述" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab0-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab0-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析"><a href="#题目解析" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab0-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-0-Extra"><a href="#Lab-0-Extra" class="headerlink" title="Lab 0 Extra"></a>Lab 0 Extra</h2><details id="lab0-extra">  <summary>点击查看详细题目</summary><h3 id="题目描述-1"><a href="#题目描述-1" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab0-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab0-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-1"><a href="#题目解析-1" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab0-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-1-Exam"><a href="#Lab-1-Exam" class="headerlink" title="Lab 1 Exam"></a>Lab 1 Exam</h2><details id="lab1-exam">  <summary>点击查看详细题目</summary><h3 id="题目描述-2"><a href="#题目描述-2" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab1-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab1-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-2"><a href="#题目解析-2" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab1-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-1-Extra"><a href="#Lab-1-Extra" class="headerlink" title="Lab 1 Extra"></a>Lab 1 Extra</h2><details id="lab1-extra">  <summary>点击查看详细题目</summary><h3 id="题目描述-3"><a href="#题目描述-3" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab1-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab1-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-3"><a href="#题目解析-3" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab1-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-2-Exam"><a href="#Lab-2-Exam" class="headerlink" title="Lab 2 Exam"></a>Lab 2 Exam</h2><details id="lab2-exam">  <summary>点击查看详细题目</summary><h3 id="题目描述-4"><a href="#题目描述-4" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab2-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab2-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-4"><a href="#题目解析-4" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab2-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-2-Extra"><a href="#Lab-2-Extra" class="headerlink" title="Lab 2 Extra"></a>Lab 2 Extra</h2><details id="lab2-extra">  <summary>点击查看详细题目</summary><h3 id="题目背景"><a href="#题目背景" class="headerlink" title="题目背景"></a>题目背景</h3><p>在 Lab2 课下实验中，我们实现了粒度为 4KB 的页式内存管理，在这里，我们将要实现简化的 <code>malloc()</code> 和 <code>free()</code> 函数。</p><p><code>malloc()</code> 函数将会在堆区域申请一个指定大小的内存空间，并且搭配 <code>free()</code> 函数可以实现运行中的内存回收。<code>malloc()</code> 在分配内存空间时，将该空间的几个字节作为管理内存块的元数据，元数据后紧挨着分配给用户的内存空间。在我们的程序中，元数据为一个特别设计的结构体 <code>MBlock</code>，用于记录被切割的内存空间。</p><p><code>MBlock</code> 结构体介绍：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">MBlock</span> &#123;</span> </span><br><span class="line">    MBlock_LIST_entry_t mb_link; <span class="comment">// 链表控制块（该属性占用 8 字节）</span></span><br><span class="line"></span><br><span class="line">    u_int size; <span class="comment">// 该元数据所管理的内存空间大小（该属性占用 4 字节）</span></span><br><span class="line">    <span class="type">void</span> *ptr; <span class="comment">// 该元数据管理内存空间的首地址（即 data 位置，仅作为魔数使用）（该属性占用 4 字节）</span></span><br><span class="line">    u_int <span class="built_in">free</span>; <span class="comment">// 该元数据所管理内存空间是否空闲，1 为空闲，0 为占用（该属性占用 4 字节）</span></span><br><span class="line">    u_int padding; <span class="comment">// 填充，用于将结构体内存大小对齐到 8 字节（该属性占用 4 字节） </span></span><br><span class="line">    <span class="type">char</span> data[]; <span class="comment">// 该元数据管理的内存空间，仅用于表示内存空间的首地址，不带有实际含义 </span></span><br><span class="line">&#125;; </span><br></pre></td></tr></table></figure><p>其中 <code>data[]</code> 在结构体中并不占用内存空间，如果你用 <code>sizeof(struct MBlock)</code> ，会发现，结构体的大小只有 24 字节，正好是<strong>前五项参数</strong>的大小，这里的 <code>data[]</code> 是 C 语言的柔性数组，表示在结构体后的任意长度空间，可以看作表示所管理的内存空间的数组。</p><p>那么 <code>*ptr</code> 又是什么呢，为什么已经存在 <code>data[]</code> 表示内存空间地址的情况下，还需要 <code>*ptr</code> 这一项呢？实际上，<code>*ptr</code> 是作为魔数存在的，通过使 <code>ptr = data</code> 将内存空间地址保存在元数据内部，而 <code>free</code> 时可以再次检查 <code>ptr == data</code> ，确认当前地址是否是由  <code>malloc</code> 分配得到。</p><p>当进行一次分配时，程序会在 <code>MBlock</code> 组成的链表中找到合适的空闲 <code>MBlock</code> ，并切割出足够的空间，用于生成记录新空间的元数据 <code>MBlock</code> 和对应分配出的内存空间。换句话说，<code>MBlock</code> 作为分割符，在一段连续的内存空间中，切割出了一系列内存空间。</p><p>通过这种方式，可以实现任意粒度的内存分配，以此来减少页中产生的内碎片，注意，元数据和分配给用户的内存空间应当交替紧邻。</p><h3 id="题目描述-5"><a href="#题目描述-5" class="headerlink" title="题目描述"></a>题目描述</h3><p>在本题中，你需要对 MOS 的内核空间 4MB 虚拟内存提供简化的 <code>malloc()</code> 和 <code>free()</code> 函数。</p><p><code>malloc()</code> 函数在收到分配内存的请求时，会将请求值向上对齐到 8 字节的倍数，并且从链表中找到合适的元数据进行分配，在这里，我们选择 First-Fit 方法，即选择遇到的第一个符合要求的元数据。</p><p>当遇到一个元数据时，检查其空间大小是否足以分配需要的内存空间:</p><ul><li><p>如果是，则维护元数据，将需要的空间返回给用户，并通过新建元数据的方式维护未被使用的剩余空间</p></li><li><p>如果不足，继续遍历链表，寻找下一个元数据<br><code>free()</code> 函数通过 <code>MBlock</code> 中的 <code>ptr</code> 属性检查地址是否是当初被 <code>malloc()</code> 分配出去的地址，之后标记为可用，<strong>合并相邻空闲空间</strong>。</p></li></ul><p>以进行两次分配和一次释放后的内存为示例，其调用顺序和参数为:</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *p1 = <span class="built_in">malloc</span>(<span class="number">32</span>);</span><br><span class="line"><span class="type">void</span> *p2 = <span class="built_in">malloc</span>(<span class="number">40</span>);</span><br><span class="line">printk(<span class="string">&quot;p1:%x p2:%x\n&quot;</span>, (<span class="type">int</span>)p1,(<span class="type">int</span>)p2);</span><br><span class="line"><span class="built_in">free</span>(p1);</span><br></pre></td></tr></table></figure><h3 id="题目要求"><a href="#题目要求" class="headerlink" title="题目要求"></a>题目要求</h3><p>在本题中，你需要使用对内核空间的 <code>0x80400000 - 0x80800000</code> 实现动态内存分配和回收。</p><p>元数据结构将会在 <code>include/malloc.h</code> 给出，其中结构体有效大小为 24 字节，最后 <code>data</code> 指针不需要实际分配内存，仅表示分给用户的空间位置。</p><p>初始将在 <code>0x80400000</code> 处声明一个基础元数据，其可用空间大小为 4MB 减去 24B ，之后从这里作为起点。</p><p>同时，我们将提供部分代码（请参看实验提供代码部分），你需要将其粘贴至 <code>kern/pmap.c</code> 之后，并补全或者实现如下函数：</p><p><strong>内存的分配 (</strong> <code>void* malloc(size_t size)</code> <strong>)</strong></p><p>本函数的功能为：</p><ul><li>使用 First-Fit 分配大小不低于 <code>size</code> 字节的内存空间：<br>○ 分配成功时，将分配的内存区间的<strong>首地址</strong>返回。<br>○ 分配失败时，返回值为 <code>NULL</code> 。</li><li>分配的内存区间需要满足以下条件：<br>○ 内存区间空闲，也即未被分配。<br>○ 区间应当紧凑，区间和元数据之间不存在无意义的空白区域。<br>○ 内存区间大小为不小于 <code>size</code> 的最小的 8 字节倍数。<br>○ 分配地址应当<strong>以 8 对齐</strong>。</li></ul><p>以下是 <code>malloc()</code> 函数的参考实现方案（你也可以自行实现本函数，但必须保证满足函数定义与功能约束）：</p><ol><li>计算需要分配的 8 字节对齐的字节数。</li><li>从 <code>0x80400080</code> 处开始，遍历链表，直到找到剩余空间大于等于 <code>size</code> 的元数据。</li><li>当需要分配的 <code>size</code> 小于等于剩余空间大小时：<br>i. 若剩余空间大小，除去分配给用户的内存空间 <code>size</code> ，剩余大小不足分配一个新的的元数据与至少 8 字节的空闲空间，则剩余空间一齐分配给调用者。<br>ii. 若剩余空间大小，除去分配给用户的内存空间 <code>size</code> ，仍剩余一个元数据的空间与对应至少 8 字节的空闲空间可以使用，则在分配的数据空间 <code>size</code> 后，再建立新的元数据，并维护元数据。</li><li>若未找到满足条件的元数据，则返回 <code>NULL</code> 。</li><li>将为用户分配空间的首地址返回用户（<code>data</code> 指针位置），而不是 <code>MBlock</code> 的地址。</li></ol><p><strong>注意：</strong></p><ul><li>本函数返回的内存区间必须从 <code>0x80400008 - 0x80800800</code> 中取得，并且相邻空间不互相覆盖。</li><li>保证调用函数时参数 <code>size</code> 不为 0 ，也不超过 1MB 。</li><li>不需要考虑清空对应物理页面中的数据。</li><li>不应存在不被元数据管理的内存空间，也不应存在管理空间大小为 0 的元数据。</li><li>结合 <code>free()</code> 函数，可能会先后申请总和超过 4MB 的内容。</li></ul><p><strong>内存的释放 (</strong> <code>void free(void*p)</code> <strong>)</strong></p><p>本函数的功能为：</p><ul><li>释放 <code>p</code> 对应的内存区间，<code>p</code> 应当是 <code>malloc()</code> 分配的区间首地址，如果不是首地址，则<strong>无需释放</strong>。</li><li>如果释放后，相邻有其他空闲空间，则<strong>合并相邻内存空间</strong>。</li></ul><p>以下是 <code>free()</code> 函数的参考实现方案（你也可以自行实现本函数，但必须保证满足函数定义与功能约束）：</p><ol><li>判断当前地址 <code>p</code> 是否在合理的范围内 <code>[HEAP BEGIN + MBLOCK_SIZE, HEAP_BEGIN + HEAP_SIZE]</code></li><li>通过当前地址 <code>p</code> 减去 <code>MBLOCK_SIZE</code> 得到 <code>MBlock</code> 应在位置。</li><li>判断当前 <code>MBlock</code> 是否合法，可以通过元数据中指向首地址的指针 <code>ptr == data</code> 判断（保证测试中可以使用）。</li><li>当需要释放空闲区间，且相邻存在空闲区间时：<br>i. 若后一个区间空闲，将后元数据清除，空间大小加到当前区间元数据中，修改指针位置，设为空闲。<br>ii. 若前一个区间空闲，将当前元数据清除，空间大小加到前区间元数据中，修改指针位置。</li><li>当前后区间均占用：<br>i. 将当前空间元数据设为空闲。</li></ol><p><strong>注意：</strong></p><ul><li>当需要释放的内存区间及其相邻空间均空闲时，必须立即将其合并为更大的空闲区间。</li><li>参数 <code>p</code> 可能存在并非通过 <code>malloc()</code> 分配得到的地址，此时 <code>p</code> 不应被释放。</li><li><code>malloc.h</code> 中定义的 <code>MBLOCK_PREV</code> 是为本道题简化的前向链表方法，通过 <code>MBLOCK_PREV(elm，field)</code> 可以得到 <code>MBlock</code> 链表前一个元素的指针。</li><li>可以用 <code>LIST_FIRST</code> 或者 <code>MBLOCK_PREV</code> 是否指向链表头判断是否是头元素。</li></ul><h3 id="任务总结"><a href="#任务总结" class="headerlink" title="任务总结"></a>任务总结</h3><p>在提交前，你需要完成以下任务：</p><ul><li>完成 <code>malloc()</code> 函数。</li><li>完成 <code>free()</code> 函数。</li><li>修改 <code>kernel.lds</code> 将 <code>end</code> 设置为 <code>0x80800800</code></li></ul><h3 id="实验提供代码"><a href="#实验提供代码" class="headerlink" title="实验提供代码"></a>实验提供代码</h3><p>请将本部分提供代码附加在你的 <code>kern/pmap.c</code> 的尾部，然后开始完成题目。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;malloc.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">MBlock_list</span> <span class="title">mblock_list</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">malloc_init</span><span class="params">()</span> &#123;</span><br><span class="line"></span><br><span class="line">    printk(<span class="string">&quot;malloc init begin\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    LIST_INIT(&amp;mblock_list);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">MBlock</span> *<span class="title">heap_begin</span> =</span> (<span class="keyword">struct</span> MBlock*)HEAP_BEGIN;</span><br><span class="line"></span><br><span class="line">    printk(<span class="string">&quot;heap begin: 0x%X\n&quot;</span>, heap_begin);</span><br><span class="line"></span><br><span class="line">    heap_begin-&gt;size = HEAP_SIZE - MBLOCK_SIZE;</span><br><span class="line">    heap_begin-&gt;ptr = (<span class="type">void</span>*) heap_begin-&gt;data;</span><br><span class="line">    heap_begin-&gt;<span class="built_in">free</span> = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    LIST_INSERT_HEAD(&amp;mblock_list, heap_begin, mb_link);</span><br><span class="line"></span><br><span class="line">    printk(<span class="string">&quot;malloc init end\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *<span class="title function_">malloc</span><span class="params">(<span class="type">size_t</span> size)</span> &#123;</span><br><span class="line">    <span class="comment">/*Your Code Here(1/2)*/</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="type">void</span> <span class="title function_">free</span><span class="params">(<span class="type">void</span> *p)</span> &#123;</span><br><span class="line">    <span class="comment">/*Your Code Here(2/2)*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><div class="custom-summary" onclick="closeDetails('lab2-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab2-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-5"><a href="#题目解析-5" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab2-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-3-Exam"><a href="#Lab-3-Exam" class="headerlink" title="Lab 3 Exam"></a>Lab 3 Exam</h2><details id="lab3-exam">  <summary>点击查看详细题目</summary><h3 id="题目描述-6"><a href="#题目描述-6" class="headerlink" title="题目描述"></a>题目描述</h3><p>课下我们在 MOS 系统中实现了时间片轮转算法（Round-Robin，RR）用于进程调度。在本题中，我们将实现最早截止时间优先算法（Earliest Deadline First，EDF），用于调度周期性进程。</p><h3 id="题目要求-1"><a href="#题目要求-1" class="headerlink" title="题目要求"></a>题目要求</h3><p>在本题中，你需要实现函数 <code>env_create_edf()</code> 用于创建周期性进程，并返回指向被创建进程的进程控制块的指针。该函数声明如下：</p><p><code>struct Env *env_create_edf(const void *binary, size_t size, int runtime, int period);</code></p><p>其中，参数 <code>binary</code> <code>size</code> 与 <code>env_create()</code> 函数中的定义相同，<code>runtime</code> <code>period</code> 为 EDF 调度参数，以时间片为单位：<code>runtime</code> 表示进程在每个周期内需要运行的时间，<code>period</code> 表示进程的运行周期。</p><p>在本题中，我们将 MOS 系统两次时钟中断之间的间隔定义为一个时间片，将 MOS 首次调用 <code>schedule()</code> 函数直至下次时钟中断前的间隔作为第 0 个时间片，再次调用 <code>schedule()</code> 函数直至下次时钟中断前的间隔作为第 1 个时间片，以此类推。</p><p>对于一个运行周期为 <code>period</code> 的进程来说，其第 <code>t</code> 个周期（从 0 开始计数）的范围为 MOS 的第 <code>[period * t, period * (t + 1))</code> 个时间片，相应地，进程第 <code>t</code> 个周期的截止时间为 <code>period * (t + 1)</code> 。例如，对于某运行周期为 5 的进程 A，其前三个周期与时间片、时钟中断的对应关系如下图所示：</p><p>（这里没有图，请自行脑补）</p><p>对于每个使用 <code>env_create_edf()</code> 创建的进程，你需要使用 EDF 算法尽可能保证进程在每个周期内运行 <code>runtime</code> 个时间片，但不一定连续，也不一定运行完（详见调度规则和示例）。</p><h3 id="调度规则"><a href="#调度规则" class="headerlink" title="调度规则"></a>调度规则</h3><p>新增的 EDF 算法与 MOS 原有的 RR 算法拥有各自独立的调度队列。EDF 调度队列中包含所有使用 <code>env_create_edf()</code> 创建的进程，而 RR 调度队列（MOS 原有的调度队列）中包含所有使用 <code>env_create()</code> 创建的进程。</p><p>当时钟中断产生时，若 EDF 调度队列中存在尚未运行完当前周期的时间片的进程，则选取其中截止时间最早的进程调度。如果多个进程的截止时间相同，则选择 <code>env_id</code> 最小的进程调度。</p><p>从 EDF 调度队列选取进程后，你仅需使其运行一个时间片，并在下个时钟中断产生时使用第二条规则，重新选择进程调度。本题中要实现的 EDF 调度算法不受 <code>yield</code> 参数和进程优先级的影响，只与进程的截止时间有关。</p><p>如果 EDF 调度队列为空，或其中的所有进程均已运行完当前周期的时间片，则使用课下实现的 RR 算法调度 RR 调度队列中的进程。需要注意的是，EDF 调度的进程可以在任何时刻抢占 RR 调度的进程，且 RR 调度的进程运行的时间片在被 EDF 抢占后不发生变化。例如，如果 RR 算法选择调度一个优先级为 5 的进程 A，并已经使其运行了 3 个时间片，此时 EDF 队列中产生了可以被调度的进程 B，则进程 B 会抢占进程 A 的运行，直至进程 B 运行完当前周期的时间片后，进程 A 继续运行剩余的 2 个时间片。</p><p>此外，如果在某个周期内，使用 EDF 算法未能保证某进程运行完 <code>runtime</code> 个时间片，则剩余的时间片不会被保留至下一周期，而是直接作废。</p><h3 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h3><p>进程按照下表的顺序依次加入 RR 调度队列和 EDF 调度队列：</p><table><thead><tr><th align="center">进程名</th><th align="center">优先级</th><th align="center">每个周期内需要运行的时间片</th><th align="center">运行周期</th></tr></thead><tbody><tr><td align="center">A</td><td align="center">1</td><td align="center">-</td><td align="center">-</td></tr><tr><td align="center">B</td><td align="center">3</td><td align="center">-</td><td align="center">-</td></tr><tr><td align="center">C</td><td align="center">-</td><td align="center">1</td><td align="center">5</td></tr><tr><td align="center">D</td><td align="center">-</td><td align="center">2</td><td align="center">7</td></tr></tbody></table><p>注：由于课下代码中进程总是插入调度链表的头部，因此 RR 调度队列中的实际顺序为 B、A。</p><p>第 0 个时间片，进程 C、D 均位于第 0 个运行周期，未运行完当前周期的时间片，进程 C 截止时间为 5，进程 D 截止时间为 7，选择截止时间最早的进程 C 运行；<br>第 1 个时间片，进程 C 已运行完当前周期的时间片，而进程 D 未运行完，选择进程 D 运行；<br>第 2 个时间片，进程 C 已运行完当前周期的时间片，而进程 D 未运行完，选择进程 D 运行；<br>第 3 个时间片，进程 C、D 已运行完当前周期的时间片，因此选择 RR 调度队列中的进程 B 运行三个时间片；<br>第 4 个时间片，进程 C、D 已运行完当前周期的时间片，因此继续选择进程 B 运行剩余的两个时间片；<br>第 5 个时间片，进程 D 已运行完当前周期的时间片，而进程 C 进入第 1 个运行周期，未运行完当前周期的时间片，截止时间为 10，选择进程 C 运行；<br>第 6 个时间片，进程 C、D 已运行完当前周期的时间片，因此继续选择进程 B 运行剩余的一个时间片；<br>第 7 个时间片，进程 C 已运行完当前周期的时间片，而进程 D 进入第 1 个运行周期，未运行完当前周期的时间片，截止时间为 14，选择进程 D 运行；<br>第 8 个时间片，进程 C 已运行完当前周期的时间片，而进程 D 未运行完，选择进程 D 运行；<br>第 9 个时间片，进程 C、D 已运行完当前周期的时间片，RR 调度队列中的进程 B 已运行完分配的时间片，被移动至调度队列尾部，因此选择进程 A 运行一个时间片；<br>…</p><p>调度流程如下图所示：</p><p>（这里没有图，请自行脑补）</p><h3 id="参考实现思路"><a href="#参考实现思路" class="headerlink" title="参考实现思路"></a>参考实现思路</h3><p>本题参考实现思路如下，你也可以采取其它思路，满足题目要求即可。</p><p>在 <code>include/env.h</code> 中添加以下声明：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">LIST_HEAD(Env_edf_sched_list, Env);</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="class"><span class="keyword">struct</span> <span class="title">Env_edf_sched_list</span> <span class="title">env_edf_sched_list</span>;</span> <span class="comment">// EDF 调度队列</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> Env *<span class="title function_">env_create_edf</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *binary, <span class="type">size_t</span> size, <span class="type">int</span> runtime, <span class="type">int</span> period)</span>;</span><br></pre></td></tr></table></figure><p>注：由于 EDF 算法不需要在队尾插入或取出的操作，因此我们推荐使用 LIST 结构实现 EDF 调度队列。</p><p>在 <code>kern/env.c</code> 中添加 <code>env_edf_sched_list</code> 的定义，并在 <code>env_init</code> 函数中初始化 <code>env_edf_sched_list</code> 。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env_edf_sched_list</span> <span class="title">env_edf_sched_list</span>;</span> <span class="comment">// EDF 调度队列</span></span><br></pre></td></tr></table></figure><p>在 <code>include/env.h</code> 的 <code>Env</code> 结构体中添加以下字段：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">LIST_ENTRY(Env) env_edf_sched_link; <span class="comment">// 构造 env_edf_sched_list 的链表项</span></span><br><span class="line">u_int env_edf_runtime; <span class="comment">// EDF 调度参数：进程在每个周期内需要运行的时间片</span></span><br><span class="line">u_int env_edf_period; <span class="comment">// EDF 调度参数：进程的运行周期</span></span><br><span class="line">u_int env_period_deadline; <span class="comment">// 进程当前周期的截止时间</span></span><br><span class="line">u_int env_runtime_left; <span class="comment">// 进程当前周期剩余的时间片</span></span><br></pre></td></tr></table></figure><p>在 <code>kern/env.c</code> 中仿照 <code>env_create()</code> 实现 <code>env_create_edf()</code> 函数，其中需要初始化进程控制块的相关字段，参考如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> Env *<span class="title function_">env_create_edf</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *binary, <span class="type">size_t</span> size, <span class="type">int</span> runtime, <span class="type">int</span> period)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    e-&gt;env_edf_runtime = runtime;</span><br><span class="line">    e-&gt;env_edf_period = period;</span><br><span class="line">    e-&gt;env_period_deadline = <span class="number">0</span>; <span class="comment">// 初始化为 0，使进程在首次调用 schedule 函数时触发条件判断，进入首个运行周期</span></span><br><span class="line">    e-&gt;env_status = ENV_RUNNABLE;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>修改 <code>kern/sched.c</code> 中的 <code>schedule()</code> 函数，实现 EDF 调度算法。参考实现方式如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">schedule</span><span class="params">(<span class="type">int</span> yield)</span> &#123;</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> clock = <span class="number">-1</span>; <span class="comment">// 当前时间片，从 0 开始计数</span></span><br><span class="line">    clock++;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* (1) 遍历 env_edf_sched_list，如果进程进入了新的运行周期（可通过 clock == env_period_deadline 判断），则更新 env_period_deadline，并将 env_runtime_left 设置为 env_edf_runtime。 */</span></span><br><span class="line">    <span class="comment">/* 在此实现你的代码 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* (2) 遍历 env_edf_sched_list，选取 env_runtime_left 大于 0 且 env_period_deadline 最小的进程调度（若相同，则选择 env_id 最小的进程）。如果不存在这样的进程，则不进行调度。 */</span></span><br><span class="line">    <span class="comment">/* 在此实现你的代码 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* (3) 使用课下实现的 RR 算法调度 env_sched_list 中的进程。 */</span></span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> count = <span class="number">0</span>; <span class="comment">// remaining time slices of current env</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span> =</span> curenv; <span class="comment">// 请根据提示修改这行代码</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 请将 Exercise 3.12 中的代码粘贴至此 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="提示"><a href="#提示" class="headerlink" title="提示"></a>提示</h3><p>你可以使用 <code>LIST_FOREACH</code> 宏遍历队列，示例如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">env</span>;</span> <span class="comment">// 循环变量</span></span><br><span class="line"></span><br><span class="line">LIST_FOREACH (env, &amp;env_edf_sched_list, env_edf_sched_link) &#123;</span><br><span class="line">    <span class="comment">// 在这里对 env 进行操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在课下代码中，我们定义 <code>struct Env *e = curenv</code> ，随后对 <code>e</code> 进行了一系列的判断和操作。在本题中，为了使 RR 算法的调度行为不受 EDF 抢占的影响，你可能需要适当修改这一设计，使 <code>e</code> 的初值为上次使用 RR 算法调度的进程，并在必要时新增全局变量或静态变量。你无需考虑进程退出的情况，详见“评测说明”。如果你在测试样例时遇到 address too low 等访问空指针引发的异常，请检查是否正确实现了这一修改。</p><h3 id="评测说明"><a href="#评测说明" class="headerlink" title="评测说明"></a>评测说明</h3><p>测试程序的行为仅限于在初始化阶段调用 <code>env_create()</code> 和 <code>env_create_edf()</code> 函数创建一批进程，评测时只评测进程的调度顺序和进程 <code>main()</code> 函数的返回值。</p><p>在评测中，我们保证进程运行完 <code>main()</code> 函数后自动进入死循环，而不会主动退出（详见测试目录下的 <code>entry.S</code> 文件）。因此，<code>env_destroy()</code> 、<code>env_free()</code> 函数不会被调用，你无需对它们进行修改。你可以据此结合提示，简化你的实现。</p><p>评测中保证不会出现 EDF 调度队列中无可调度进程且 RR 调度队列为空的情况。</p><div class="custom-summary" onclick="closeDetails('lab3-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab3-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-6"><a href="#题目解析-6" class="headerlink" title="题目解析"></a>题目解析</h3><p>题目中已经帮我们实现好了全部的定义，将题目中给出的代码复制好后，我们就只需要实现两个内容：一是仿照 <code>env_create()</code> 函数补全 <code>env_create_edf()</code> 函数；二是根据代码中的注释修改 <code>schedule()</code> 函数。</p><p>我们首先进行第一步，将 <code>env_create()</code> 函数中为新进程的属性赋值部分替换为题目中给出的内容，再将插入 <code>env_sched_list</code> 队列改为插入 <code>env_edf_sched_list</code> 队列即可（这里需要注意两个队列的类型是不同的，插入时要改用 <code>LIST_INSERT_HEAD</code> ）</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/env.c</span></span><br><span class="line"><span class="keyword">struct</span> Env *<span class="title function_">env_create_edf</span><span class="params">(<span class="type">const</span> <span class="type">void</span> *binary, <span class="type">size_t</span> size, <span class="type">int</span> runtime, <span class="type">int</span> period)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span>;</span></span><br><span class="line"></span><br><span class="line">    env_alloc(&amp;e, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    e-&gt;env_edf_runtime = runtime;</span><br><span class="line">    e-&gt;env_edf_period = period;</span><br><span class="line">    e-&gt;env_period_deadline = <span class="number">0</span>; <span class="comment">// 初始化为 0，使进程在首次调用 schedule 函数时触发条件判断，进入首个运行周期</span></span><br><span class="line">    e-&gt;env_status = ENV_RUNNABLE;</span><br><span class="line"></span><br><span class="line">    load_icode(e, binary, size);</span><br><span class="line">    LIST_INSERT_HEAD(&amp;env_edf_sched_list, e, env_edf_sched_link);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> e; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来就是本次 exam 的重难点，即修改 <code>schedule()</code> 函数。在动手操作之前，请一定确保自己完全了解了题目中的调度规则。如果有疑惑的话，可以参考一下题目描述中的“示例”一节，我个人觉得这一节的讲解是最清楚的。</p><p>接下来我们跟着注释一步一步完成代码：</p><p><code>(1) 遍历 env_edf_sched_list，如果进程进入了新的运行周期（可通过 clock == env_period_deadline 判断），则更新 env_period_deadline，并将 env_runtime_left 设置为 env_edf_runtime。</code></p><p>关于如何更新 <code>env_period_deadline</code> ，从题目描述的示例可以总结出，<code>env_period_deadline</code> 是 <code>env_edf_period</code> 的倍数，每当进程到达截止时间时，将截止时间再加上一倍即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/sched.c</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">env</span>;</span> <span class="comment">// 循环变量</span></span><br><span class="line"></span><br><span class="line">LIST_FOREACH (env, &amp;env_edf_sched_list, env_edf_sched_link) &#123;</span><br><span class="line">    <span class="comment">// 这里可以根据提示，使用 LIST_FOREACH 来循环</span></span><br><span class="line">    <span class="keyword">if</span> (clock == env-&gt;env_period_deadline) &#123;</span><br><span class="line">        env-&gt;env_period_deadline += env-&gt;env_edf_period;</span><br><span class="line">        env-&gt;env_runtime_left = env-&gt;env_edf_runtime;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>(2) 遍历 env_edf_sched_list，选取 env_runtime_left 大于 0 且 env_period_deadline 最小的进程调度（若相同，则选择 env_id 最小的进程）。如果不存在这样的进程，则不进行调度。</code></p><p>首先，我们需要通过遍历找出需要调度的 edf 进程。下一步，我们还需要判断进程是否存在。若存在，我们需要调用 <code>env_run()</code> 来运行该进程，<strong>同时注意维护</strong> <code>env_runtime_left</code> <strong>变量的值，减去一个时间片</strong>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/sched.c</span></span><br><span class="line">u_int min = <span class="number">-1</span>; <span class="comment">// 截止时间最小的进程的截止时间</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">wheremin</span> =</span> <span class="literal">NULL</span>; <span class="comment">// 截止时间最小的进程</span></span><br><span class="line"></span><br><span class="line">LIST_FOREACH (env, &amp;env_edf_sched_list, env_edf_sched_link) &#123;</span><br><span class="line">    <span class="keyword">if</span> (env-&gt;env_runtime_left &gt; <span class="number">0</span>) &#123; <span class="comment">// 这里注意判断当前进程是否执行完</span></span><br><span class="line">        <span class="keyword">if</span> (min == <span class="number">-1</span>) &#123;</span><br><span class="line">            min = env-&gt;env_period_deadline;</span><br><span class="line">            wheremin = env;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (env-&gt;env_period_deadline &lt; min || (env-&gt;env_period_deadline == min &amp;&amp; env-&gt;env_id &lt; wheremin-&gt;env_id)) &#123;</span><br><span class="line">                min = env-&gt;env_period_deadline;</span><br><span class="line">                wheremin = env;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (wheremin != <span class="literal">NULL</span> &amp;&amp; wheremin-&gt;env_status == ENV_RUNNABLE) &#123;</span><br><span class="line">    <span class="comment">// 不是很确定是否要判断 status 是否为 ENV_RUNNABLE ，感觉可能不太用</span></span><br><span class="line">    wheremin-&gt;env_runtime_left--;</span><br><span class="line">    env_run(wheremin);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>(3) 使用课下实现的 RR 算法调度 env_sched_list 中的进程。</code></p><p>到这一步有些同学就看不太懂了，不知道有什么可改的地方，其实有两处是要修改的，当然这就需要我们对本题的调度规则有比较清晰的理解了。</p><p>第一处，不知道各位有没有怀疑过注释 (3) 的表述，根据上面的规则和示例，显然只有在 edf 队列中没有找到要调度的 edf 进程时，才会从 RR 队列中选取进程调度，也就是说 (3) 中的调度不是无条件的，这就需要我们将整个 RR 调度的逻辑放在上一步的分支判断中。</p><p>第二处，根据代码中的注释和后文的提示都可以看出，我们需要将 RR 调度中的 <code>e</code> 由当前进程 <code>curenv</code> 改为上次执行的 RR 进程。这里我们需要新开一个 static 变量 <code>struct Env *lastRRenv</code> ，每次进行 RR 调度时将当前进程存储进去即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/sched.c</span></span><br><span class="line"><span class="keyword">if</span> (wheremin != <span class="literal">NULL</span> &amp;&amp; wheremin-&gt;env_status == ENV_RUNNABLE) &#123;</span><br><span class="line">    wheremin-&gt;env_runtime_left--;</span><br><span class="line">    env_run(wheremin);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="type">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">lastRRenv</span> =</span> <span class="literal">NULL</span>; </span><br><span class="line">    <span class="comment">// 这里必须为静态变量 static ，否则每次调用 schedule() 函数时 lastRRenv 都会被重新定义</span></span><br><span class="line">    <span class="comment">// 仿照 curenv 的定义，初值赋为 NULL</span></span><br><span class="line">    <span class="comment">// struct Env *e = curenv;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Env</span> *<span class="title">e</span> =</span> lastRRenv; <span class="comment">// 删去 e 的原定义，改为 lastRRenv</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (yield || count == <span class="number">0</span> || e == <span class="literal">NULL</span> || e-&gt;env_status != ENV_RUNNABLE) &#123;</span><br><span class="line">        <span class="keyword">if</span> (e != <span class="literal">NULL</span> &amp;&amp; e-&gt;env_status == ENV_RUNNABLE) &#123;</span><br><span class="line">            TAILQ_REMOVE(&amp;env_sched_list, e, env_sched_link);</span><br><span class="line">            TAILQ_INSERT_TAIL(&amp;env_sched_list, e, env_sched_link);</span><br><span class="line">        &#125;</span><br><span class="line">        e = TAILQ_FIRST(&amp;env_sched_list);</span><br><span class="line">        lastRRenv = e; <span class="comment">// 此时新的 e 已经确定，更新 lastRRenv 的值</span></span><br><span class="line">        <span class="keyword">if</span> (e == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            panic(<span class="string">&quot;schedule: no runnable envs\n&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        count = e-&gt;env_pri;</span><br><span class="line">    &#125;</span><br><span class="line">    count--;</span><br><span class="line">    env_run(e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>到这里我们就将本次 exam 全部解决了！总的来说，本次 exam 还是非常吃细节的，只有将题目中的调度规则完全搞清楚，并且在做题时不断思考实现细节，时刻记得维护属性值，才能顺利通过测试。</p><div class="custom-summary" onclick="closeDetails('lab3-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-3-Extra"><a href="#Lab-3-Extra" class="headerlink" title="Lab 3 Extra"></a>Lab 3 Extra</h2><details id="lab3-extra">  <summary>点击查看详细题目</summary><h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p>课下实验中，我们主要介绍了异常的分发、异常向量组和 0 号异常（时钟中断）的处理过程。在本次 Extra 中，我们希望大家拓展 4 号异常 <code>AdEL</code> 和 5 号异常 <code>AdES</code> 的异常分发，并在此基础上实现自定义的异常处理。</p><p>在 MIPS 指令集中，对这两种异常的描述如下：</p><table><thead><tr><th align="center">ExcCode(异常码)</th><th align="center">助记符</th><th align="center">描述</th></tr></thead><tbody><tr><td align="center">4</td><td align="center"><code>AdEL</code></td><td align="center">在 <code>load</code> &#x2F; <code>fetch</code> 指令执行过程中发生的地址错误</td></tr><tr><td align="center">5</td><td align="center"><code>AdES</code></td><td align="center">在 <code>store</code> 指令执行过程中发生的地址错误</td></tr></tbody></table><p>以上错误可能是由于地址不对齐，也可能是由于违反了访问权限。在本次题目中，我们只考虑地址不对齐导致的上述错误，且只考虑 <code>lw</code> 和 <code>sw</code> 指令（即必须四字节对齐）的情况。</p><p>且我们要实现自定义的异常处理——通过修改相应的 <code>lw</code> 或 <code>sw</code> 指令，将未 4 字节对齐的地址的低 2 位（二进制格式下）置 0 ，得到符合对齐要求的地址。例如：对于地址<code>0x7f000009</code> ，需要将其修改为 <code>0x7f000008</code>（即修改后地址 <code>new_addr = old_addr &amp; ~0x3</code> ），方可得到符合要求的地址。注意，这里我们要求仅修改指令中的立即数部分，不要修改任何寄存器中的值。</p><p>在异常处理完成，返回用户态之前，需要输出相应信息：</p><p>lw处理完成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">printk(<span class="string">&quot;AdEL handled, new imm is : %04x\n&quot;</span>, new_inst &amp; <span class="number">0xffff</span>); <span class="comment">// 这里的 new_inst 替换为修改后的指令</span></span><br></pre></td></tr></table></figure><p>sw处理完成：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">printk(<span class="string">&quot;AdES handled, new imm is : %04x\n&quot;</span>, new_inst &amp; <span class="number">0xffff</span>); <span class="comment">// 这里的 new_inst 替换为修改后的指令</span></span><br></pre></td></tr></table></figure><p>注意上述中的异常处理函数需要完成的输出内容不包含双引号而且应该单独成行，并注意避免其他非必要的输出，否则将影响评测结果。</p><p><code>lw</code> 和 <code>sw</code> 指令的机器码如下：</p><table><thead><tr><th align="center">指令</th><th align="center">31…26</th><th align="center">25…21</th><th align="center">20…16</th><th align="center">15…0</th><th align="center">行为</th></tr></thead><tbody><tr><td align="center"><code>lw</code></td><td align="center"><code>100011</code></td><td align="center"><code>base</code></td><td align="center"><code>rt</code></td><td align="center"><code>imm</code></td><td align="center"><code>GPR[rt] &lt;- memory[GPR[base] + (sign-extend)imm]</code></td></tr><tr><td align="center"><code>sw</code></td><td align="center"><code>101011</code></td><td align="center"><code>base</code></td><td align="center"><code>rt</code></td><td align="center"><code>imm</code></td><td align="center"><code>memory[GPR[base] + (sign-extend)imm] &lt;- GPR[rt]</code></td></tr></tbody></table><h3 id="题目要求-2"><a href="#题目要求-2" class="headerlink" title="题目要求"></a>题目要求</h3><p>建立 <code>AdEL</code> 和 <code>AdES</code> 异常的处理函数，完成异常分发。</p><p>在处理函数中，你需要通过修改相应指令以确保重新执行指令时的地址是四字节对齐的。</p><h3 id="提示-1"><a href="#提示-1" class="headerlink" title="提示"></a>提示</h3><p>可以在 <code>kern/genex.S</code> 中，用 <code>BUILD_HANDLER</code> 宏来构建异常处理函数：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">BUILD_HANDLER adel do_adel</span><br><span class="line">BUILD_HANDLER ades do_ades</span><br></pre></td></tr></table></figure><p>可以在 <code>kern/traps.c</code> 修改异常向量组，并实现异常处理函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 声明 handle 函数</span></span><br><span class="line"><span class="keyword">extern</span> <span class="type">void</span> <span class="title function_">handle_adel</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"><span class="keyword">extern</span> <span class="type">void</span> <span class="title function_">handle_ades</span><span class="params">(<span class="type">void</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// exception_handlers 请按以下代码实现</span></span><br><span class="line"><span class="type">void</span> (*exception_handlers[<span class="number">32</span>])(<span class="type">void</span>) = &#123;</span><br><span class="line">    [<span class="number">0</span> ... <span class="number">31</span>] = handle_reserved,</span><br><span class="line">    [<span class="number">0</span>] = handle_int,</span><br><span class="line">    [<span class="number">2</span> ... <span class="number">3</span>] = handle_tlb,</span><br><span class="line">    [<span class="number">4</span>] = handle_adel,</span><br><span class="line">    [<span class="number">5</span>] = handle_ades,</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> !defined(LAB) || LAB &gt;= 4</span></span><br><span class="line">    [<span class="number">1</span>] = handle_mod,</span><br><span class="line">    [<span class="number">8</span>] = handle_sys,</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">do_adel</span><span class="params">(<span class="keyword">struct</span> Trapframe *tf)</span> &#123;</span><br><span class="line">     <span class="comment">// 在此实现相应操作以使修改后指令符合要求</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">do_ades</span><span class="params">(<span class="keyword">struct</span> Trapframe *tf)</span> &#123;</span><br><span class="line">     <span class="comment">// 在此实现相应操作以使修改后指令符合要求</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>本题涉及对寄存器值的读取。你需要访问保存现场的 <code>Trapframe</code> 结构体（定义在 <code>include/trap.h</code> 中）中对应通用寄存器。</p><p>本题涉及对内存的访问，由于我们要修改的指令部分在 <code>kuseg</code> 区间，这一部分的虚拟地址需要通过 TLB 来获取物理地址。我们设置了程序中保存操作指令代码的 <code>.text</code> 节权限为只读，这部分空间在页表中仅被映射为 <code>PTE_V</code> ，而不带有 <code>PTE_D</code> 权限，因此经由页表项无法对物理页进行写操作。可以考虑查询 <code>curenv</code> 的页表获取对应指令的物理地址，再转化为 <code>kseg0</code> 的虚拟地址，从而修改相应内容。</p><p>本题要求自定义行为是要修改对应的指令的立即数部分，而不是通过在内核态用四字节对齐的地址直接获得数据，在这种做法下使用 <code>epc + 4</code> 跳过异常将无法通过测试。</p><h3 id="题目约束"><a href="#题目约束" class="headerlink" title="题目约束"></a>题目约束</h3><p>保证地址出错当且仅当 <code>lw</code> 和 <code>sw</code> 访问的地址未四字节对齐！</p><p>保证不会出现取指发生 <code>AdEL</code> 或 <code>AdES</code> 异常！</p><p>不保证 <code>lw</code> 和 <code>sw</code> 指令的立即数为正数，但保证异常指令的初始立即数和按要求修改后的立即数都在 <code>signed short</code> (16位) 的取值范围内！</p><p>地址不对齐的指令中立即数对应的地址可能是四字节对齐的，即地址不对齐可能是寄存器的值未对齐，但本题要求仅修改指令中的立即数部分！</p><p>保证可以仅通过修改立即数部分的方式得到正确结果！</p><div class="custom-summary" onclick="closeDetails('lab3-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab3-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-7"><a href="#题目解析-7" class="headerlink" title="题目解析"></a>题目解析</h3><p>事实上，lab 3 extra 的题目虽然总是异常处理，但和 CO P7 的关系真的不是特别大，毕竟还要照顾一下 CO 没学过异常的同学们。其实只要对 MIPS 指令结构和作用有稍微的了解，就足以拿下这道问题了。</p><p>异常的注册在提示部分就已经全部给出了，我们只需要实现 <code>do_adel()</code> 和 <code>do_ades()</code> 两个函数即可。其实只要通读一遍题目，稍作思考就可以发现，这两个函数是一模一样的，除了最后的输出以外没有任何区别。</p><p>题目中没有给出具体的实现步骤，现在我们就来拆解一下函数的实现流程：首先我们需要取出当前出现异常的指令，然后对指令进行修改，最后将指令写回原来的位置。接下来我们一步一步实现上面的流程：</p><p>第一步其实是最难的一步，很多没看过往年博客的同学直接被硬控到死，到最后家门都没出去。而机智如我发现往年总这么考，直接把这段代码抄在纸上，考试时无脑爆抄，直接助我拿下全考场手刹，所以同学们知道该怎么做了吧（笑）<br>↑以防大家不知道，再次提醒一下，这门课是开卷考试，可以携带纸质资料入场，下回记得带~</p><p>题目的提示部分提过，我们从 <code>tf-&gt;cp0_epc</code> 中获取到的地址并不是存放当前指令的真实地址，而是它的虚拟地址。我们需要通过一顿猛如虎的操作将其转化为物理地址，才能将当前指令取出。如果你不能理解下面的代码，记住抄下来就对了！祖传代码，屡试不爽：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/traps.c</span></span><br><span class="line">u_int badva = tf-&gt;cp0_epc; <span class="comment">// 获取虚拟地址</span></span><br><span class="line">Pde *pgdir = curenv-&gt;env_pgdir; <span class="comment">// 获取页目录的基地址</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">p</span> =</span> page_lookup(pgdir, badva, <span class="literal">NULL</span>); <span class="comment">// 查找虚拟地址所在的页</span></span><br><span class="line">u_int badpa = page2pa(p) + (badva &amp; <span class="number">0xfff</span>); <span class="comment">// 将页转换为物理地址（不含页内偏移），再加上虚拟地址中的页内偏移部分，构成完整物理地址</span></span><br><span class="line">u_int *badk0va = <span class="number">0x80000000</span> | badpa; <span class="comment">// 转换为 kseg0 的虚拟地址（kseg0 起始地址为 0x80000000）</span></span><br><span class="line">u_int code = *badk0va; <span class="comment">// code 的值即为 32 位 MIPS 指令的内容</span></span><br></pre></td></tr></table></figure><p>接下来，我们就需要按照题目中的要求来更改指令了。这一步比较简单，但也有几个需要注意的地方：</p><p>首先我们需要注意，这里的修改绝不是直接将当前指令的 <code>offset</code> 低 2 位置 0 。回忆 <code>lw</code> 和 <code>sw</code> 指令的用法，我们可以发现，需要能被 4 整除的并不是最后的 <code>offset</code> 值，而是 <code>GPR[base] + offset</code> 的结果。所以我们需要先计算出这个值，然后根据它对 4 取模的结果调整 <code>offset</code> 。</p><p>于是就出现了另外一个问题，就是在计算 <code>GPR[base]</code> 的时候，该如何从寄存器中取值。其实只需要用一步 <code>int gpr_base = (int)tf-&gt;regs[base];</code> 即可取到，但这一步或许也难住了许多英雄汉，属实可惜。</p><p>最后就是要时刻注意其中不断变化的数据长度和有无符号问题了。整个代码中涉及到 <code>int</code> <code>u_int</code> <code>signed short</code> 类型的反复转换，虽然写法多种多样（比如我就是乱写的，我也不知道哪些有用哪些没用），但还是要尽量避免类型的隐式转换，让整个计算过程都在我们的可控范围之内。同时也一定要格外注意长度转换带来的有无符号拓展问题，确保 <code>offset</code> 在计算时将 <code>signed short</code> 类型转换为 <code>int</code> 类型：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/traps.c</span></span><br><span class="line">u_int base = ((code &amp; <span class="number">0x03e00000</span>) &gt;&gt; <span class="number">21</span>); <span class="comment">// 取出指令的 21 ~ 25 位作为寄存器的编号</span></span><br><span class="line">u_int u_offset = (code &amp; <span class="number">0x0000ffff</span>); <span class="comment">// 取出指令的 0 ~ 15 位作为偏移值</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> gpr_base = (<span class="type">int</span>)tf-&gt;regs[base]; <span class="comment">// 从寄存器中取出基地址值</span></span><br><span class="line"><span class="type">signed</span> <span class="type">short</span> offset = (<span class="type">signed</span> <span class="type">short</span>)u_offset; <span class="comment">// 我也不知道这步有没有用，但总之这么写是对的</span></span><br><span class="line"><span class="type">int</span> now_res = gpr_base + (<span class="type">int</span>)offset; <span class="comment">// 算出 GPR[base] + offset 的值</span></span><br><span class="line"><span class="type">int</span> fix_res = (now_res &amp; ~<span class="number">0x3</span>); <span class="comment">// 将上述值的低 2 位置 0</span></span><br><span class="line"><span class="type">int</span> sub = now_res - fix_res; <span class="comment">// 算出置 0 前后的差值</span></span><br><span class="line">offset = offset - (<span class="type">signed</span> <span class="type">short</span>)sub; <span class="comment">// 在 offset 上减去上述差值</span></span><br><span class="line">code = ((code &amp; <span class="number">0xffff0000</span>) | ((u_int)offset &amp; <span class="number">0x0000ffff</span>)); <span class="comment">// 将 offset 放回原来的位置</span></span><br></pre></td></tr></table></figure><p>最后一步就是把修复好的指令放回原位置了，因为我们刚才已经算出指令在 kseg0 的虚拟地址了，怎么把它取出来就怎么把它放回去即可。最后别忘了按照题目要求输出：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/tras.c</span></span><br><span class="line">*badk0va = code;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in do_adel():</span></span><br><span class="line">printk(<span class="string">&quot;AdEL handled, new imm is : %04x\n&quot;</span>, code &amp; <span class="number">0xffff</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// in do_ades():</span></span><br><span class="line">printk(<span class="string">&quot;AdES handled, new imm is : %04x\n&quot;</span>, code &amp; <span class="number">0xffff</span>);</span><br></pre></td></tr></table></figure><p>最后还是总结一下，毕竟是 extra ，难度还是有一些的，我觉得涉及到地址转换的题目都还挺难的。不过把上面那段八股抄下来直接可秒，后面难度就不太大了，可能涉及到一些位运算的知识，不过我相信大家都能 hold 住。总而言之，多看看往年题，多看看往届博客，好处还是大大的有。大家认真准备，千万不要裸考口牙！</p><div class="custom-summary" onclick="closeDetails('lab3-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-4-Exam"><a href="#Lab-4-Exam" class="headerlink" title="Lab 4 Exam"></a>Lab 4 Exam</h2><details id="lab4-exam">  <summary>点击查看详细题目</summary><h3 id="题目描述-7"><a href="#题目描述-7" class="headerlink" title="题目描述"></a>题目描述</h3><div class="custom-summary" onclick="closeDetails('lab4-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab4-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-8"><a href="#题目解析-8" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab4-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-4-Extra"><a href="#Lab-4-Extra" class="headerlink" title="Lab 4 Extra"></a>Lab 4 Extra</h2><details id="lab4-extra">  <summary>点击查看详细题目</summary><h3 id="题目背景-1"><a href="#题目背景-1" class="headerlink" title="题目背景"></a>题目背景</h3><p>在 Lab4 课下实验中，我们已经了解了系统调用机制，并且实现了一个简易的进程间通信（IPC）。但是这样的机制基于单页，并且限于双方进程，我们现在想要实现一个简易的共享内存（Shared Memory，简记为 SHM），能够实现<strong>多进程间</strong>的<strong>不定页数</strong>通信。</p><p>为了实现这一点，我们需要在<strong>内核态</strong>维护一个数据结构 <code>struct Shm</code> ，用来维护“一块<strong>共享内存</strong>”，其各个成员如下：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Shm</span> &#123;</span></span><br><span class="line">    u_int npage;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">pages</span>[<span class="title">N_SHM_PAGE</span>];</span></span><br><span class="line">    u_int open;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>其中：</p><ul><li><code>npage</code> 表示共享内存总共的页数，在我们的测试中被限制为 <code>[1, N_SHM_PAGE] = [1, 8]</code></li><li><code>pages</code> 为用于共享的<strong>物理页面</strong>，通过将<strong>不同进程的虚拟地址</strong>映射到相同的物理页面，可以实现内存共享</li><li><code>open</code> 用于维护这一块共享内存的状态，<code>open != 0</code> 表示其已经被分配</li></ul><p>在我们的测试中，限制共享内存的数量为 <code>N_SHM = 8</code> 个，使用<strong>内核数组</strong>维护。<strong>数组索引即为它们各自的编号</strong>。</p><h3 id="系统调用约定"><a href="#系统调用约定" class="headerlink" title="系统调用约定"></a>系统调用约定</h3><p>你需要实现下列系统调用，它们的定义与功能如下：</p><p><code>1. int sys_shm_new(u_int npage)</code></p><p>申请总大小为 <code>npage</code> 个页面的共享内存。在申请时，首先找到一个<strong>编号最小的</strong>、未被分配的（<code>open = 0</code>）的共享内存，并使用 <code>page_alloc</code> 函数申请 <code>npage</code> 个页面，进行必要的操作后，记录在 <code>pages</code> 数组中。</p><p>如果找不到这样可用的共享内存，返回 <code>-E_SHM_INVALID</code> 而无需申请页面。如果在申请页面时，空闲页面不足，返回 <code>-E_NO_MEM</code> 。在这个过程中，请避免造成内存泄漏（也即，当发现分配失败时，你需要释放已经分配的页面）。</p><p>正确执行后，返回共享内存的编号（数组下标）。</p><p><code>2. int sys_shm_bind(int key, u_int va, u_int perm)</code></p><p>将虚拟地址 <code>va</code>（保证按页对齐）作为共享内存的起始地址，映射到编号为 <code>key</code> 的共享内存中。你需要将虚拟地址范围 <code>[va, va + npage * PAGE_SIZE)</code> <strong>依次映射</strong>到共享内存的 <code>npage</code> 个物理页面上。</p><p>如果对应的共享内存未被分配（<code>open = 0</code>），则返回 <code>-E_SHM_NOT_OPEN</code>。否则，返回 0 即可。</p><p><code>3. int sys_shm_unbind(int key, u_int va)</code></p><p>将虚拟地址范围 <code>[va, va + npage * PAGE_SIZE)</code> <strong>解除映射</strong>（<code>va</code> 保证按页对齐）。简单起见，这里的实现不需要关心这些虚拟地址是否真的被映射到共享内存之中，使用 <code>page_remove</code> 函数移除映射即可。</p><p>如果 <code>key</code> 对应的共享内存未被分配（<code>open = 0</code>），返回 <code>-E_SHM_NOT_OPEN</code> 。否则，返回 0 即可。</p><p><code>4. int sys_shm_free(int key)</code></p><p>将 <code>key</code> 对应的共享内存注销（将 <code>open</code> 设为 0），并对页面进行必要的操作，将它们释放。</p><p>如果该共享内存原本未被分配，返回 <code>-E_SHM_NOT_OPEN</code> 。否则，返回 0 即可。</p><p>测试数据保证在调用 <code>sys_shm_free</code> 前，全部的映射都已经被 <code>unbind</code> 了。</p><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol><li>测试数据保证，<strong>如果一个进程存在一个绑定了共享内存的页面时，则不会进行</strong> <code>fork</code> ，因为此时的行为没有在题面中给出定义。你不需要考虑这种情形。</li><li>你的实现要能够支持<strong>同一个进程不同地址间</strong>的共享，同一个进程的不同地址也可以被映射到同一个物理页面上，但测试数据保证，同一个进程的某一虚拟地址不会被多次绑定。你不需要维护虚拟地址与共享内存的关系，仅需要实现系统调用的功能即可。</li><li>对于某个共享内存的页面，可能被绑定多个进程，然后被全部解除绑定，此时，你应该保证这些物理页面<strong>仍可以进行新的绑定操作</strong>。提示：<strong>你可以通过在创建共享内存时，增加页面的</strong> <code>pp_ref</code> <strong>记录，并在销毁共享内存时使用</strong> <code>page_decref</code> <strong>函数释放页面。</strong></li><li>测试数据保证，共享页面中的内存仅作为数组进行访问，而不会存放代码段、栈数据等特殊内容。</li><li>测试程序会<strong>使用 IPC 机制进行进程间的同步</strong>。</li></ol><h3 id="题目要求-3"><a href="#题目要求-3" class="headerlink" title="题目要求"></a>题目要求</h3><p><code>user/lib/shm.c</code> 与 <code>include/shm.h</code> 的内容已在初始化分支时向仓库中添加，你可以查看它们的具体内容。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/shm.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">ifndef</span> _SHM_H_</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> _SHM_H_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mmu.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;types.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> N_SHM_PAGE 8</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> N_SHM 8</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Shm</span> &#123;</span></span><br><span class="line">    u_int npage;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">pages</span>[<span class="title">N_SHM_PAGE</span>];</span></span><br><span class="line">    u_int open;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/lib/shm.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mmu.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_new</span><span class="params">(u_int npage)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> syscall_shm_new(npage);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_bind</span><span class="params">(u_int key, <span class="type">void</span> *va)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> syscall_shm_bind(key, (u_int)va, PTE_D);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_unbind</span><span class="params">(u_int key, <span class="type">void</span> *va)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> syscall_shm_unbind(key, (u_int)va);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_free</span><span class="params">(u_int key)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> syscall_shm_free(key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此外，你可以按照下面的步骤进行：</p><ol><li>在 <code>include/error.h</code> 中，添加下列宏：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> E_SHM_INVALID 14</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> E_SHM_NOT_OPEN 15</span></span><br></pre></td></tr></table></figure><ol start="2"><li>在 <code>user/include/lib.h</code> 中，添加下列系统调用的用户态封装，以及库函数声明：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// syscalls</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_new</span><span class="params">(u_int npage)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_bind</span><span class="params">(<span class="type">int</span> key, u_int va, u_int perm)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_unbind</span><span class="params">(<span class="type">int</span> key, u_int va)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_free</span><span class="params">(<span class="type">int</span> key)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// shm.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_new</span><span class="params">(u_int npage)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_bind</span><span class="params">(u_int key, <span class="type">void</span> *va)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_unbind</span><span class="params">(u_int key, <span class="type">void</span> *va)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">shm_free</span><span class="params">(u_int key)</span>;</span><br></pre></td></tr></table></figure><ol start="3"><li>在 <code>include/syscall.h</code> 中，向 <code>enum</code> 中添加以下系统调用号。请注意新增系统调用号的位置，应当位于 <code>MAX_SYSNO</code> 之前：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">SYS_shm_new</span><br><span class="line">SYS_shm_bind</span><br><span class="line">SYS_shm_unbind</span><br><span class="line">SYS_shm_free</span><br></pre></td></tr></table></figure><ol start="4"><li>在 <code>user/lib/syscall_lib.c</code> 中添加系统调用封装的具体实现，使用 <code>msyscall</code> 函数陷入内核：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_new</span><span class="params">(u_int npage)</span> &#123;</span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (1/8)</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_bind</span><span class="params">(<span class="type">int</span> key, u_int va, u_int perm)</span> &#123;</span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (2/8)</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_unbind</span><span class="params">(<span class="type">int</span> key, u_int va)</span> &#123;</span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (3/8)</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_shm_free</span><span class="params">(<span class="type">int</span> key)</span> &#123;</span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (4/8)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="5"><li>在 <code>kern/syscall_all.c</code> 中<strong>引入相关的头文件</strong>，定义表示共享内存的<strong>内核数组</strong>，并添加系统调用在内核中的实现函数。请保证函数的定义位于系统调用函数表 <code>void *syscall_table[MAX_SYSNO]</code> 之前：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;shm.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ... ...</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Shm</span> <span class="title">shm_pool</span>[<span class="title">N_SHM</span>];</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_shm_new</span><span class="params">(u_int npage)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (npage == <span class="number">0</span> || npage &gt; N_SHM_PAGE) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_SHM_INVALID;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (5/8)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ??;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_shm_bind</span><span class="params">(<span class="type">int</span> key, u_int va, u_int perm)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (key &lt; <span class="number">0</span> || key &gt;= N_SHM) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_SHM_INVALID;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (6/8)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ??;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_shm_unbind</span><span class="params">(<span class="type">int</span> key, u_int va)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (key &lt; <span class="number">0</span> || key &gt;= N_SHM) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_SHM_INVALID;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (7/8)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ??;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_shm_free</span><span class="params">(<span class="type">int</span> key)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (key &lt; <span class="number">0</span> || key &gt;= N_SHM) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_SHM_INVALID;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Lab4-Extra: Your code here. (8/8)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> ??;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="6"><li>在 <code>kern/syscall_all.c</code> 中的 <code>void *syscall_table[MAX_SYSNO]</code> 系统调用函数表中，为步骤 3 中添加的系统调用号添加对应的内核函数指针：</li></ol><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[SYS_shm_new] = sys_shm_new,</span><br><span class="line">[SYS_shm_bind] = sys_shm_bind,</span><br><span class="line">[SYS_shm_unbind] = sys_shm_unbind,</span><br><span class="line">[SYS_shm_free] = sys_shm_free,</span><br></pre></td></tr></table></figure><div class="custom-summary" onclick="closeDetails('lab4-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab4-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-9"><a href="#题目解析-9" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab4-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-5-Exam"><a href="#Lab-5-Exam" class="headerlink" title="Lab 5 Exam"></a>Lab 5 Exam</h2><details id="lab5-exam">  <summary>点击查看详细题目</summary><h3 id="题目背景-题目描述"><a href="#题目背景-题目描述" class="headerlink" title="题目背景 &amp; 题目描述"></a>题目背景 &amp; 题目描述</h3><p>在 Linux 系统中，find 命令是一条很常用的命令，它支持根据多种条件筛选文件，并把文件的路径输出。比如根据路径、名字来匹配相应的文件：</p><p><code>find / -name &quot;motd&quot;</code></p><p>该命令在根目录下查找名为 motd 的文件，并把找到的所有文件的路径输出。</p><p>在本次测试中，我们希望同学们实现一个支持以基础路径和文件名为条件查找文件的简易版函数<br><code>int find(const char *path, const char *name, struct Find_res *res)</code> 。该函数在 <code>path</code>（都以<strong>绝对路径</strong>给出）对应的文件及其子目录中递归查找名字为 <code>name</code> 的文件，并把查到的文件的<strong>绝对路径</strong>和文件总数结果存入 <code>res</code> 指向的预定义结构体，只有文件名和 <code>name</code> 完全一致才视作匹配，无需支持通配符。</p><p><code>struct Find_res</code> 定义在了 <code>user/include/lib.h</code> 中，<code>file_path</code> 用于存查到的路径，<code>count</code> 用于存查到的文件数量。我们保证在本次测试中，该结构体足够装下所满足条件的路径，不会溢出。同时你需要注意我们修改了 <code>MAXPATHLEN</code> ，本题中你需要用宏来表示最大路径长度。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAXPATHNUM ((PAGE_SIZE - sizeof(int)) / MAXPATHLEN)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Find_res</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> file_path[MAXPATHNUM][MAXPATHLEN];</span><br><span class="line">    <span class="type">int</span> count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>函数正常执行则返回 0 ，错误时对应的负错误码，本题保证只会出现两种错误，均已经在 lab5 课下实现，即查找 <code>path</code> 对应文件的时候没找到或者路径太长。如果在 <code>path</code> 对应的文件目录下查找名字为 <code>name</code> 的文件过程中发现路径过长，则跳过该路径继续处理其他路径即可，不视作错误。</p><p>需要注意的是，本函数的匹配的规则和标准 <code>find &lt;path&gt; -name &quot;&lt;name&gt;&quot;</code> 实现一致，你可以在跳板机上尝试运行 <code>find</code> 命令，查看一些特殊情况下标准行为（例如 <code>&lt;path&gt;</code> 定位的文件名字恰为 <code>&lt;name&gt;</code> ，提示：在本题中，这种情况下你需要把这个文件包括在查询结果中），为了简化实现，我们保证测试点中 <code>&lt;path&gt;</code> 对应的文件如果能够找到，一定是目录类型，同时，为了简化实现，我们保证所有测试点中，<code>path</code> 均以路径分隔符 <code>/</code> 结尾。</p><h3 id="实现思路"><a href="#实现思路" class="headerlink" title="实现思路"></a>实现思路</h3><p>在 <code>user/include/lib.h</code> 中我们已经给出了所需的查询结果结构体，你无需再复制：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAXPATHNUM ((PAGE_SIZE - sizeof(int)) / MAXPATHLEN)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Find_res</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> file_path[MAXPATHNUM][MAXPATHLEN];</span><br><span class="line">    <span class="type">int</span> count;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>你可以使用文件系统服务进程来完成整个查找过程。可以在用户函数中添加如下请求接口:</p><p>在 <code>user/include/lib.h</code> 的 <code>file.c</code> 注释下新增声明：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">find</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span></span><br></pre></td></tr></table></figure><p>将该函数 <code>find()</code> 的实现复制到 <code>user/lib/file.c</code> ：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">find</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> fsipc_find(path, name, res);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>(请注意，本次 <code>lab5-exam</code> 只需要通过编译，就可以通过第一个测试点并拿到 50 分，如果后续完成确实有困难，你需要根据上面的思路添加 <code>find()</code> 函数的声明和实现，并把 <code>find()</code> 的函数体内部 <code>return fsipc_find(path, name, res);</code> 改成 <code>return 0;</code> ，这样能用最少的行数通过编译，并通过第一个测试点。如果想完整实现本题目要求的所有功能，请忽略这一段话)</p><p>在 <code>user/include/fsreq.h</code> 中的枚举类型中增加一个对于文件系统的请求类型 <code>FSREQ_FIND</code> ，请注意要把它放在 <code>MAX_FSREQNO</code> 前。</p><p>在同一文件 <code>user/include/fsreq.h</code> 里面加上请求结构体：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Fsreq_find</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> req_path[MAXPATHLEN];</span><br><span class="line">    <span class="type">char</span> req_name[MAXNAMELEN];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在 <code>user/include/lib.h</code> 的 <code>fsipc.c</code> 的注释下新增声明：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">fsipc_find</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span>;</span><br></pre></td></tr></table></figure><p>在 <code>user/lib/fsipc.c</code> 中将该函数的实现复制过去：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">fsipc_find</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strlen</span>(path) == <span class="number">0</span> || <span class="built_in">strlen</span>(path) &gt; MAXPATHLEN) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_BAD_PATH;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Fsreq_find</span> *<span class="title">req</span> =</span> (<span class="keyword">struct</span> Fsreq_find *)fsipcbuf;</span><br><span class="line">    <span class="built_in">strcpy</span>((<span class="type">char</span> *)req-&gt;req_path, path);</span><br><span class="line">    <span class="built_in">strcpy</span>((<span class="type">char</span> *)req-&gt;req_name, name);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> r = fsipc(FSREQ_FIND, req, res, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后在文件系统服务函数中实现服务接收和递归查找：</p><p>仿照其他项，在 <code>fs/serv.c</code> 的服务函数分发表 <code>serve_table</code> 最后新增一项 <code>[FSREQ_FIND] = serve_find,</code> </p><p>在 <code>fs/serve.c</code> 的分发表上方将 <code>serve_find()</code> 函数的实现复制过去：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">serve_find</span><span class="params">(u_int envid, <span class="keyword">struct</span> Fsreq_find *rq)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Find_res</span> <span class="title">res</span> __<span class="title">attribute</span>((<span class="title">aligned</span>(<span class="title">PAGE_SIZE</span>))) =</span> &#123;&#125;;</span><br><span class="line">    <span class="type">int</span> r = find_files(rq-&gt;req_path, rq-&gt;req_name, &amp;res);</span><br><span class="line">    <span class="keyword">if</span> (r) &#123;</span><br><span class="line">        ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        ipc_send(envid, <span class="number">0</span>, (<span class="type">const</span> <span class="type">void</span>*)&amp;res, PTE_D | PTE_LIBRARY);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>fs/serv.h</code> 中添加声明：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">find_files</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span></span><br></pre></td></tr></table></figure><p>然后在 <code>fs/fs.c</code> 中完成 <code>fs/find_files()</code> 函数，实现本服务的核心功能。你可以先使用 <code>walk_path()</code> 函数找到 <code>path</code> 对应的文件夹，然后再文件夹下寻找名字恰为 <code>name</code> 的文件。你可以参照 <code>fs/fs.c</code> 文件里的 <code>dir_lookup()</code> 学会对文件的各个块进行访问，不过需要注意 <code>dir_lookup()</code> 不支持递归查找，你需要用深度优先搜索支持递归查找(下一条提示给出了参考实现)：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">find_files</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">file</span>;</span></span><br><span class="line">    <span class="comment">// 用 walk_path 来找到 path 对应的文件夹</span></span><br><span class="line">    <span class="comment">// Lab5-Exam: Your code here. (1/2)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在 path 对应的文件夹下面遍历，找到所有名字为 name 的文件，你可以调用下面的参考函数 traverse_file</span></span><br><span class="line">    <span class="comment">// Lab5-Exam: Your code here. (2/2)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>你可以用任意正确的方式实现 (2&#x2F;2) 部分，这里用一个提供了一个参考实现函数，你只需要按注释填写四个空：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">traverse_file</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="keyword">struct</span> File *file, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span> &#123;</span><br><span class="line">    u_int nblock;</span><br><span class="line">    nblock = file-&gt;f_size / BLOCK_SIZE;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 检查路径长度是否符合要求，如不符合，直接返回</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="comment">/* path 的长度为零或不小于最大路径长度*/</span>) &#123;</span><br><span class="line">        <span class="comment">/*返回*/</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 比较当前文件名是否等于 name，如果相等则更改 res</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="comment">/*file 的名字等于 name*/</span>) &#123;</span><br><span class="line">        <span class="comment">/*增加 res-&gt;count*/</span></span><br><span class="line">        <span class="comment">/*添加 res 的路径*/</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (file-&gt;f_type == FTYPE_DIR) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; nblock; i++) &#123;</span><br><span class="line">            <span class="type">void</span> *blk;</span><br><span class="line">            try(file_get_block(file, i ,&amp;blk));</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">files</span> =</span> (<span class="keyword">struct</span> File *)blk;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">struct</span> File *f = files; f &lt; files + FILE2BLK; ++f) &#123;</span><br><span class="line">                <span class="type">char</span> curpath[MAXPATHLEN + MAXNAMELEN + <span class="number">1</span>];</span><br><span class="line">                <span class="comment">// 3. 把 path 和 name 拼接起来得到下一层文件路径，注意结尾的 &#x27;\0&#x27;</span></span><br><span class="line">                <span class="comment">// 提示：我们没有实现 strcat 工具函数，你可以用 strcpy 实现拼接</span></span><br><span class="line">                <span class="comment">// 4. 递归调用 traverse_file 函数</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><div class="custom-summary" onclick="closeDetails('lab5-exam')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab5-exam-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-10"><a href="#题目解析-10" class="headerlink" title="题目解析"></a>题目解析</h3><p>文件系统服务的添加流程还是非常复杂的，虽然 exam 中已经给出了全部流程，将题目中给的代码复制一下即可，但 extra 就不会有这种张嘴就来的事了，建议大家还是对照着题目中“实现思路”一节好好捋顺一下整个流程，对以后写挑战性任务也很有帮助。</p><p>将题目中的代码复制完成之后，我们需要完成的就只剩下 <code>fs/fs.c</code> 中的 <code>find_files()</code> 和 <code>traverse_file()</code> 两个函数了。</p><p>首先我们来完成 <code>find_files()</code> 函数，需要调用 <code>walk_path()</code> 函数找到路径名 <code>path</code> 对应的文件，这就需要我们充分了解 <code>walk_path()</code> 函数的构成。</p><p><code>walk_path()</code> 函数定义为 <code>int walk_path(char *path, struct File **pdir, struct File **pfile, char *lastelem)</code> ，其中 <code>char *path</code> 为要寻找的文件的路径，其余三个参数用来存储返回值。若成功找到文件，则 <code>struct File **pdir</code> 为该文件所在的文件夹，<code>struct File **pfile</code> 为该文件本身；若未成功找到文件，但找到了该文件所在的文件夹，则 <code>struct File **pdir</code> 为该文件所在的文件夹， <code>char *lastelem</code> 为该文件的名称。除成功找到文件函数返回 0 以外，其它未找到文件的情况函数均返回负值错误码。</p><p>我们现在可以发现，在 <code>find_files()</code> 函数中调用 <code>walk_path()</code> 函数时，是完全用不到第二和第四个参数的，只需要保证在 <code>path</code> 对应的文件存在的情况下能够将文件本身取出就可以了。当然，为了防止访问空指针的情况，我们还是在这些参数的位置塞一些东西进去为好，虽然我们拿到这些数据以后也会转手把它们扔掉。</p><p>最后按照题目中定义调用 <code>traverse_file()</code> 函数即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/fs.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">find_files</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">const</span> <span class="type">char</span> *name, <span class="keyword">struct</span> Find_res *res)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">file</span> =</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// Lab5-Exam: Your code here. (1/2)</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">pdir</span> =</span> <span class="literal">NULL</span>; <span class="comment">// actually no use</span></span><br><span class="line">    <span class="type">char</span> nname[MAXNAMELEN]; <span class="comment">// actually no use</span></span><br><span class="line">    <span class="type">int</span> r = walk_path(path, &amp;pdir, &amp;file, nname); <span class="comment">// 只需把 file 取出即可</span></span><br><span class="line">    <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> r;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Lab5-Exam: Your code here. (2/2)</span></span><br><span class="line">    traverse_file(path, file, name, res); <span class="comment">// 按定义调用 traverse_file() 函数</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来我们来完成 <code>traverse_file()</code> 函数，根据注释一步一步完成：</p><p><code>1. 检查路径长度是否符合要求，如不符合，直接返回</code></p><p>根据注释提示，若 <code>path</code> 的长度为零或不小于最大路径长度，则返回 <code>-E_BAD_PATH</code> 错误码，使用 <code>strlen()</code> 函数即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/fs.c</span></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">strlen</span>(path) == <span class="number">0</span> || <span class="built_in">strlen</span>(path) &gt;= MAXPATHLEN <span class="comment">/* path 的长度为零或不小于最大路径长度*/</span>) &#123;</span><br><span class="line">    <span class="comment">/*返回*/</span></span><br><span class="line">    <span class="keyword">return</span> -E_BAD_PATH;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>2. 比较当前文件名是否等于 name，如果相等则更改 res</code></p><p>观察 <code>res</code> 的结构，我们知道 int 型变量 <code>res-&gt;count</code> 存储着查找到的文件的总数量，而字符串数组 <code>res-&gt;file_path[]</code> 存储着每个查找到的文件的路径。所以只需要更新这两个变量即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/fs.c</span></span><br><span class="line"><span class="keyword">if</span> (!<span class="built_in">strcmp</span>(file-&gt;f_name, name) <span class="comment">/*file 的名字等于 name*/</span>) &#123;</span><br><span class="line">    <span class="comment">/*增加 res-&gt;count*/</span></span><br><span class="line">    <span class="comment">/*添加 res 的路径*/</span></span><br><span class="line">    <span class="built_in">strcpy</span>(res-&gt;file_path[res-&gt;count], path);</span><br><span class="line">    res-&gt;count++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>3. 把 path 和 name 拼接起来得到下一层文件路径，注意结尾的 &#39;\0&#39;</code><br><code>提示：我们没有实现 strcat 工具函数，你可以用 strcpy 实现拼接</code></p><p>这一步看似十分简单，实则却暗藏玄机。就因为这一步，我在考场上被硬控了 70 分钟，创下 6 次 exam 的 PW（笑哭），甚至有好多同学直接被硬控两个半小时（是的，这次考试延时了半小时，也是这一届唯一一次 OS 上机延时，不过应该是 extra 出太难了的原因），最后连 exam 都没做上。</p><p><strong>本质原因其实是：我们在拼接的时候并不知道</strong> <code>path</code> <strong>的结尾到底有没有</strong> <code>/</code> <strong>！</strong></p><p>你或许会问，题目中不是已经保证了 <code>path</code> 均以 <code>/</code> 结尾吗？我会告诉你，<code>traverse_file()</code> 是一个递归函数，这里 <code>path</code> 的值不仅可能是最初输入的 <code>path</code> ，还可能是其中任意一个文件夹的路径。</p><p>你或许还会问，只要找到哪些文件路径后面有 <code>/</code> ，哪些后面没有，然后特判一下就好了嘛。是啊，你说得对，可是怎么找到呢？经验之谈，任何尝试找到其中规律或者是尝试特判的方法全部都是错的。我也非常奇怪，为什么评测时的测试点会那么强（如果没记错的话，当时有上千个评测点），很多同学在这里钻牛角尖试图找到规律，结果就打出 GG 了。</p><p>最后经过我一番冥思苦想，终于找到了破局之法。说来也简单，甚至可能简单到让大家啼笑皆非。如果我找不到什么时候会缺一个 <code>/</code> ，那我不找了还不行吗？！在拼接的时候，我直接检查当前 <code>path</code> 的结尾是不是 <code>/</code> ，如果不是的话，加一个就好了嘛。是的，就是这么简单，也是这道题的唯一解法。如果你在看到这一段之前就已经想到了这个做法，恭喜你，你肯定比我聪明~</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/fs.c</span></span><br><span class="line"><span class="comment">// 3. 把 path 和 name 拼接起来得到下一层文件路径，注意结尾的 &#x27;\0&#x27;</span></span><br><span class="line"><span class="comment">// 提示：我们没有实现 strcat 工具函数，你可以用 strcpy 实现拼接</span></span><br><span class="line"><span class="built_in">strcpy</span>(curpath, path);</span><br><span class="line"><span class="type">int</span> curlen = <span class="built_in">strlen</span>(curpath);</span><br><span class="line"><span class="comment">// 如果 path 不以 &#x27;/&#x27; 结尾，在后面加一个 &#x27;/&#x27;</span></span><br><span class="line"><span class="keyword">if</span> (curpath[curlen - <span class="number">1</span>] != <span class="string">&#x27;/&#x27;</span>) &#123;</span><br><span class="line">    curpath[curlen] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">    curlen++; <span class="comment">// 别忘了 len++</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="type">int</span> len = <span class="built_in">strlen</span>(f-&gt;f_name);</span><br><span class="line"><span class="comment">// 这里人傻了，明明可以用 strcpy(curpath + curlen, f-&gt;f_name) 直接拼接的</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; len; j++) &#123;</span><br><span class="line">    curpath[curlen + j] = f-&gt;f_name[j];</span><br><span class="line">&#125;</span><br><span class="line">curpath[curlen + len] = <span class="string">&#x27;\0&#x27;</span>; <span class="comment">// 别忘了最后加 &#x27;\0&#x27;</span></span><br></pre></td></tr></table></figure><p><code>4. 递归调用 traverse_file 函数</code></p><p>最后按照定义递归调用 <code>traverse_file()</code> 函数，这道题就全部做完了：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/fs.c</span></span><br><span class="line"><span class="comment">// 4. 递归调用 traverse_file 函数</span></span><br><span class="line">traverse_file(curpath, f, name, res);</span><br></pre></td></tr></table></figure><p>还是总结一下吧，这场 exam 对我来说实在是一场苦战，导致最后也没有太多时间做 extra 了，即使续费了半个小时也还是没写完，实在是太痛苦了。考场上确实很容易钻牛角尖，在高压状况下大脑很容易宕机，还是祝愿各位能够保持一个稳定平和的心态，以冷静的头脑和灵活的思维面对层出不穷的突发状况。</p><div class="custom-summary" onclick="closeDetails('lab5-exam-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><h2 id="Lab-5-Extra"><a href="#Lab-5-Extra" class="headerlink" title="Lab 5 Extra"></a>Lab 5 Extra</h2><details id="lab5-extra">  <summary>点击查看详细题目</summary><p>本题涉及文件系统诸多细节，若题面表述有不清楚之处，请及时询问助教。</p><h3 id="题目背景-2"><a href="#题目背景-2" class="headerlink" title="题目背景"></a>题目背景</h3><p>当前， MOS 的文件系统将数据以明文的形式保存在磁盘上，这埋下了数据泄漏的隐患，所有用户都可以直接读取任意文件，磁盘意外被他人获取后也可以直接读取数据。</p><p>为了消除这些隐患，加密文件系统的概念被提出。通过对文件或文件夹进行加密，可以防止未授权的访问。同时，加密文件系统提供了透明性，只需要正确配置密钥，合法用户可以使用既有方式，且对加密解密过程无感地读写文件。加密解密工作将由文件系统自动完成。</p><p>Windows 中的 NTFS 文件系统支持这样的功能。右键一个文件，在菜单中点击“属性”，在属性窗口中的“常规”选项卡中点击“高级”按钮，在高级属性窗口中可以看到“加密内容以便保护数据”​​。</p><p>我们可以在 MOS 现有的文件系统的基础上实现简单的加密文件系统。我们实现的加密文件系统需要支持对于普通文件的加密，不需要考虑对于目录的加密，同时需要提供一定的透明性。</p><h4 id="加密算法"><a href="#加密算法" class="headerlink" title="加密算法"></a>加密算法</h4><p>我们的加密文件系统的加密算法是异或加密。</p><p>异或运算 (XOR) 满足 <code>d ^ k = e</code> ， <code>e ^ k = (d ^ k) ^ k = d</code> 。所以异或加密的加密和解密方法是相同的。对于原文，与密钥进行异或运算，即可得到密文；对于密文，与密钥进行异或运算，即可得到原文。</p><p>当然，异或加密安全性较低，易被攻破。实际的加密文件系统采用 AES 算法或者更强大的非对称加密算法。</p><p>在 MOS 的文件系统中，文件内容被组织为一系列的磁盘块 (block)，每个磁盘块的大小为 4096 字节（由 <code>user/include/fs.h</code> 中的 <code>BLOCK_SIZE</code> 定义）。基于此，我们的加密文件系统的异或加密也是以磁盘块为单位进行的。具体地，规定密钥长度为磁盘块的大小，即 4096 字节。加密时，对于文件内容的一个磁盘块的各个字节，将其与密钥的各个字节进行异或，即可得到密文。由上述异或运算性质，通过同样的方法可以由密文得到原文。</p><p>上述过程用 C 语言代码可以如下表示：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">char</span> *blk;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">char</span> encrypt_key[BLOCK_SIZE];</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; BLOCK_SIZE; i++) &#123;</span><br><span class="line">    blk[i] ^= encrypt_key[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>密钥以未加密的普通文件在磁盘中存储。合法的密钥文件满足文件内容至少有两个磁盘块并且第一个磁盘块的第一个字为 <code>FS_MAGIC</code>（在 <code>user/include/fs.h</code> 中定义）。文件其余字节可为任意值。规定密钥存储于合法的密钥文件的第二个磁盘块。</p><h3 id="系统行为"><a href="#系统行为" class="headerlink" title="系统行为"></a>系统行为</h3><p>结合 MOS 现有的文件系统，为了使加密文件系统提供一定的透明性，使用加密文件系统操作文件的整体过程为：加载密钥 &gt; 打开文件 &gt; 读写操作 &gt; 关闭文件 &gt; 卸载密钥。</p><p>具体地，我们的加密文件系统的行为分为两部分，一是用户态程序与文件系统进行的交互，二是文件系统服务 <code>fs/serv.c</code> 的加密解密工作。</p><p>（一）用户态程序使用声明于 <code>user/include/lib.h</code> 的函数与文件系统进行交互。</p><p>已经实现的文件系统功能不应受影响。</p><p>通过已有的 <code>open()</code> 函数以加密方式打开一个文件，调用函数时需要额外给 <code>open()</code> 函数的第二个参数 <code>mode</code> 或运算 <code>O_ENCRYPT</code> 。如果当前未加载密钥，应返回 <code>-E_BAD_KEY</code> 。</p><p>由 MOS 的文件系统设计，使用 <code>open()</code> 函数打开文件时会将文件的所有磁盘块读取到内存中，故可以利用文件系统服务 <code>fs/serv.c</code> 的文件解密功能实现该异常处理。</p><p>通过已有的 <code>close()</code> 函数关闭一个以加密方式打开的文件。如果当前未加载密钥，应返回 <code>-E_BAD_KEY</code> 。</p><p>由 MOS 的文件系统设计，所有磁盘块直到使用 <code>close()</code> 函数关闭文件时才会写入磁盘，故可以利用文件系统服务 <code>fs/serv.c</code> 的文件加密功能实现该异常处理。</p><p>通过已有的 <code>read()</code> ，<code>write()</code> ，<code>seek()</code> 等函数进行读写和其他操作。调用读取函数时，读出的内容应为解密后的原文。调用写入函数时，传入的是未加密的原文，但应将加密后的密文写入磁盘。</p><p>通过新增的 <code>int fskey_set(int fd)</code> 函数加载密钥。参数 <code>fd</code> 为使用 <code>open()</code> 函数以非加密且允许读的方式打开密钥文件返回的文件描述符。如果文件描述符不合法，或以加密方式打开，或打开方式为只写，则返回 <code>-E_INVAL</code> 。函数通过 IPC 调用文件系统服务 <code>fs/serv.c</code> 的密钥加载功能并返回 IPC 的返回值。</p><p>通过新增的 <code>int fskey_unset()</code> 函数卸载密钥。函数通过 IPC 调用文件系统服务 <code>fs/serv.c</code> 的密钥卸载功能并返回 IPC 的返回值。</p><p>通过新增的 <code>int fskey_isset()</code> 函数判断当前是否加载密钥。函数通过 IPC 调用文件系统服务 <code>fs/serv.c</code> 的状态查询功能并返回 IPC 的返回值。</p><p>（二）加密解密工作由文件系统服务 <code>fs/serv.c</code> 完成。</p><p>系统状态：记录当前文件系统服务是否已经加载密钥，同时缓存加载的密钥。缓存密钥是为了实现只要加载了密钥，多个读写进程就可以利用文件系统服务使用相同的密钥以加密方式读写文件。同时，即使打开的密钥文件在完成密钥加载操作后被关闭，也不会影响到加密解密工作。</p><p>密钥加载：判断当前状态是否已加载密钥，如果已加载密钥，IPC 返回 <code>-E_BAD_KEY</code> ；如果未加载密钥，读取通过 IPC 传入的打开的文件，并判断该文件是否为合法密钥文件。如果不合法，IPC 返回 <code>-E_INVALID_KEY_FILE</code> ；如果未加载密钥且密钥文件合法，读取文件中的密钥并缓存，同时将当前状态标记为已加载密钥。</p><p>密钥卸载：判断当前状态是否已加载密钥，如果未加载密钥，IPC 返回 <code>-E_BAD_KEY</code> 。如果已加载密钥，则将当前状态标记为未加载密钥，并且清除密钥缓存。<br>状态查询：如果当前状态已加载密钥，IPC 返回 1，如果未加载密钥，IPC 返回 0。</p><p>文件解密：从磁盘读取以加密方式打开的文件的磁盘块时，判断当前状态是否已加载密钥，如果未加载密钥，IPC 返回 <code>-E_BAD_KEY</code> 。如果已加载密钥，在完成磁盘块读取后对其进行解密。</p><p>文件加密：写入以加密方式打开的文件的磁盘块到磁盘时，判断当前状态是否已加载密钥，如果未加载密钥，IPC 返回 <code>-E_BAD_KEY</code> 。如果已加载密钥，对原文进行加密后写入对应磁盘块。</p><p>（三）说明：</p><p><code>O_ENCRYPT</code> 需在 <code>user/include/lib.h</code> 中定义。</p><p><code>-E_INVALID_KEY_FILE</code> 和 <code>-E_BAD_KEY</code> 需在 <code>include/error.h</code> 中定义。</p><p><code>fskey_set()</code> 、<code>fskey_unset()</code> 和 <code>fskey_isset()</code> 函数需在 <code>user/include/lib.h</code> 中声明并在 <code>user/lib/file.c</code> 中实现。</p><p>若没有发生错误，应当 IPC 返回 0 。</p><h3 id="参考实现"><a href="#参考实现" class="headerlink" title="参考实现"></a>参考实现</h3><p>只需要满足加密算法和系统行为的要求，各种实现方式都是可以接受的。下面给出一种参考实现方式。</p><p>在 <code>include/error.h</code> 中合适位置定义 <code>-E_INVALID_KEY_FILE</code> 和 <code>-E_BAD_KEY</code> 。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> E_INVALID_KEY_FILE 14</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> E_BAD_KEY 15</span></span><br></pre></td></tr></table></figure><p>在 <code>user/include/fsreq.h</code> 中增加新的文件系统服务 IPC 请求号，并定义新的请求结构体。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    ......</span><br><span class="line">    FSREQ_KEY_SET,</span><br><span class="line">    FSREQ_KEY_UNSET,</span><br><span class="line">    FSREQ_KEY_ISSET,</span><br><span class="line">    MAX_FSREQNO,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Fsreq_key_set</span> &#123;</span></span><br><span class="line">    <span class="type">int</span> req_fileid;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>在 <code>user/include/lib.h</code> 中合适位置声明新增的函数和 IPC 函数，并定义 <code>O_ENCRYPT</code> 。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_set</span><span class="params">(u_int fileid)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_unset</span><span class="params">()</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_isset</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_set</span><span class="params">(<span class="type">int</span> fd)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_unset</span><span class="params">()</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_isset</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> O_ENCRYPT 0x0010</span></span><br></pre></td></tr></table></figure><p>在 <code>fs/serv.c</code> 中定义相关变量用于标记当前状态和缓存密钥，实现相关的服务函数，并且添加到 <code>serve_table</code> 中。密钥文件的要求见“加密算法”一节。</p><p>提示：可以参考 <code>fs/fs.c</code> 的 <code>dir_lookup()</code> 和 <code>read_super()</code> 函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">static</span> <span class="type">int</span> encrypt_key_set = <span class="number">0</span>;</span><br><span class="line"><span class="type">static</span> <span class="type">unsigned</span> <span class="type">char</span> encrypt_key[BLOCK_SIZE];</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">serve_key_set</span><span class="params">(u_int envid, <span class="keyword">struct</span> Fsreq_key_set *rq)</span> &#123;</span><br><span class="line">    <span class="comment">// 判断当前状态是否已加载密钥，如果已加载密钥， IPC 返回 -E_BAD_KEY</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 利用 open_lookup 找到对应的 Open 结构体，判断文件大小是否至少有两个磁盘块大小</span></span><br><span class="line">    <span class="comment">// 利用 file_get_block 读取文件的第一个磁盘块，判断第一个字是否为 FS_MAGIC</span></span><br><span class="line">    <span class="comment">// 如果密钥文件不合法， IPC 返回 -E_INVALID_KEY_FILE</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 利用 file_get_block 读取文件的第二个磁盘块，将密钥复制到 encrypt_key 中</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将当前状态标记为已加载密钥</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// IPC 返回 0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">serve_key_unset</span><span class="params">(u_int envid)</span> &#123;</span><br><span class="line">    <span class="comment">// 判断当前状态是否已加载密钥，如果未加载密钥， IPC 返回 -E_BAD_KEY</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将当前状态标记为未加载密钥</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将密钥缓存 encrypt_key 清零</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// IPC 返回 0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">serve_key_isset</span><span class="params">(u_int envid)</span> &#123;</span><br><span class="line">    <span class="comment">// IPC 返回当前状态</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> *serve_table[MAX_FSREQNO] = &#123;</span><br><span class="line">    ......</span><br><span class="line">    [FSREQ_KEY_SET] = serve_key_set,</span><br><span class="line">    [FSREQ_KEY_UNSET] = serve_key_unset,</span><br><span class="line">    [FSREQ_KEY_ISSET] = serve_key_isset,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>查看 <code>user/lib/file.c</code> 中的 <code>open()</code> 函数，确保对 <code>fsipc_map()</code> 函数的调用返回错误代码时，该错误代码将作为 <code>open()</code> 函数的返回值。</p><p>提示：可以使用 <code>try()</code> 宏。</p><p>修改 <code>fs/serv.c</code> 中的 <code>serve_map()</code> 函数。在读取文件磁盘块之前，判断该文件是否是以加密方式打开的，如果是，判断当前是否已加载密钥，如果未加载密钥， IPC 返回 <code>-E_BAD_KEY</code> ，不将读取到磁盘块内存地址通过 IPC 共享。对于以加密方式打开的文件，读取文件磁盘块之后，对读取到的磁盘块进行解密。解密算法见“加密算法”一节。</p><p>修改 <code>fs/serv.c</code> 中的 <code>serve_close()</code> 函数。在调用 <code>file_close()</code> 将磁盘块写入之前，判断这个文件是否是以加密方式打开的，如果是，判断当前是否已加载密钥，如果未加载密钥， IPC 返回 <code>-E_BAD_KEY</code> ，不进行后续的磁盘写入操作。对于以加密方式打开的文件，对文件的所有磁盘块进行加密后再调用 <code>file_close()</code> 进行磁盘写入。加密算法见“加密算法”一节。<br>提示：可以参考 <code>fs/fs.c</code> 的 <code>file_flush()</code> 函数。</p><p>在 <code>user/lib/file.c</code> 中实现 <code>fskey_set()</code> 、 <code>fskey_unset()</code> 和 <code>fsipc_key_isset()</code> 。在 <code>user/lib/fsipc.c</code> 中实现 <code>fsipc_key_set()</code> 、<code>fsipc_key_unset()</code> 和 <code>fsipc_key_isset()</code> 函数，合理调用 <code>user/lib/fsipc.c</code> 与文件系统服务 <code>fs/serv.c</code> 进行 IPC 。</p><p>提示：可以参考 <code>user/lib/file.c</code> 中的 <code>ftruncate()</code> 函数。与文件系统服务进行 IPC 的编程方式可以参考 <code>user/lib/file.c</code> 和 <code>user/lib/fsipc.c</code> 中的其他函数。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* user/lib/file.c */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_set</span><span class="params">(<span class="type">int</span> fdnum)</span> &#123;</span><br><span class="line">    <span class="comment">// 使用 fd_lookup 找到对应的 Fd 结构体。判断传入的文件描述符是否合法</span></span><br><span class="line">    <span class="comment">// 如果不合法返回 fd_lookup 函数的返回值（该函数除了0之外，只会返回 -E_INVAL 错误码，符合系统行为要求）</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 判断文件是否以加密方式打开，判断打开方式是否为只写</span></span><br><span class="line">    <span class="comment">// 密钥文件要求以非加��且允许读的方式打开，不满足则返回 -E_INVAL</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过对 Fd 结构体进行处理获得 fileid ，合理调用相应文件系统 IPC 函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_unset</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 合理调用相应文件系统 IPC 函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fskey_isset</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 合理调用相应文件系统 IPC 函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* user/lib/fsipc.c */</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_set</span><span class="params">(u_int fileid)</span> &#123;</span><br><span class="line">    <span class="comment">// 合理调用 fsipc 函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_unset</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="comment">// 合理调用 fsipc 函数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">fsipc_key_isset</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    <span class="comment">// 合理调用 fsipc 函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><p>所有测试点的密钥均与下发样例的密钥不同。硬编码下发的密钥无法通过评测。</p><p>MOS 的文件系统可能存在一定的缺陷，保证各评测点均不考察下发代码中非填写部分的已有设计或者潜在缺陷。特别说明以下三种情况：</p><p>现有部分函数的实现方式可能导致文件描述符无法被回收。评测保证在文件描述符相关功能实现正确的前提下，打开的文件数量不会耗尽可用的文件描述符。</p><p>现有 <code>close()</code> 函数的行为是无论关闭过程是否发生了错误，都会将传入的文件描述符释放。评测保证如果调用 <code>close()</code> 函数关闭文件时发生了错误（如未加载密钥），不会对已被释放的文件描述符再次调用 <code>close()</code> 等文件操作函数。</p><p>现有的磁盘块映射到内存机制是在已有映射的情况下不会再次从磁盘读取该磁盘块，然而文件系统服务的全周期并不会解除映射。这会导致未写入磁盘的数据据滞留内存且不会再读取磁盘中对应磁盘块的原始数据。评测保证调用 <code>close()</code> 函数关闭文件时发生了错误（如未加载密钥），不会以加密或非加密方式重新打开该文件进行读写。</p><p>在线评测时，所有的 <code>.mk</code> 文件、所有的 <code>Makefile</code> 文件、<code>init/init.c</code> 以及 <code>tests/</code> 和 <code>tools/</code> 目录下的所有文件都可能被替换为标准版本，因此请同学们本地开发时，不要在这些文件中编写实际功能所依赖的代码。</p><p>可能今天是很多同学最后一次参与 OS 限时测试了。无论是否完成了今天所有的题目，OS 实验课程的学习是顺利还是遗憾，都恭喜你，祝福你！希望之后 OS 和其他课程的学习顺利！</p><p>如果你也希望传递 OS 实验课程的学习经验与心得，有热情参与 OS 实验课程的建设中。欢迎加入 OS 实验课程助教团队，敬请留意后续通知。 Let’s be the S.T.A.R. of OS!</p><div class="custom-summary" onclick="closeDetails('lab5-extra')">  <div class="triangle"></div>  点击收起详细题目</div></details><br><details id="lab5-extra-answer">  <summary>点击查看题目解析</summary><h3 id="题目解析-11"><a href="#题目解析-11" class="headerlink" title="题目解析"></a>题目解析</h3><div class="custom-summary" onclick="closeDetails('lab5-extra-answer')">  <div class="triangle"></div>  点击收起题目解析</div></details><style>  .custom-summary {    display: flex;    align-items: center;    cursor: pointer;    margin-bottom: 10px;  }  .triangle {    width: 0;    height: 0;    border: 4px solid transparent;    background-color: #4C4920;    clip-path: polygon(50% 0%, 0% 100%, 100% 100%);    margin-right: 5px;    margin-top: 2px;  }</style><script>  function closeDetails(detail) {    const top = document.getElementById(detail).getBoundingClientRect().top + window.scrollY;    document.getElementById(detail).open = false;    window.scrollTo({ top: top - 300, behavior: 'smooth' });  }</script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>OS2025 Shell Challenge</title>
      <link href="/Kamonto_blog/2025/07/02/OS2025%20Shell%20Challenge/"/>
      <url>/Kamonto_blog/2025/07/02/OS2025%20Shell%20Challenge/</url>
      
        <content type="html"><![CDATA[<h1 id="OS-Shell-挑战性任务实验报告"><a href="#OS-Shell-挑战性任务实验报告" class="headerlink" title="OS Shell 挑战性任务实验报告"></a>OS Shell 挑战性任务实验报告</h1><h2 id="任务要求"><a href="#任务要求" class="headerlink" title="任务要求"></a>任务要求</h2><p>在 lab6 中，我们在 MOS 中实现了简单的 Shell ，但是距离真正的 Shell 还有很大一段距离。在本次挑战性任务中，我们需要基于 lab 6 的实现，完善我们的 Shell 的下列功能，通过自动评测并撰写实验报告：</p><ol><li>支持相对路径</li><li>加入内建指令 <code>cd</code> <code>pwd</code> </li><li>支持局部变量与全局变量</li><li>加入内建指令 <code>declare</code> <code>unset</code> </li><li>支持在输入指令时使用 <code>left-arrow</code> 和 <code>right-arrow</code> 移动光标</li><li>支持在输入指令时在任意位置插入和删除字符</li><li>支持兼容不以 <code>.b</code> 结尾的外部指令</li><li>支持 <code>ctrl-E</code> <code>ctrl-A</code> <code>ctrl-K</code> <code>ctrl-U</code> <code>ctrl-W</code> 等快捷键</li><li>支持使用 <code>up-arrow</code> 和 <code>down-arrow</code> 回溯历史指令</li><li>加入内建指令 <code>history</code> </li><li>支持使用 <code>#</code> 进行注释</li><li>支持反引号 <code>`</code> </li><li>支持使用分号 <code>;</code> 实现一行多指令</li><li>支持使用 <code>&amp;&amp;</code> 和 <code>||</code> 实现指令条件执行</li><li>加入外部指令 <code>mkdir</code> <code>touch</code> <code>rm</code> 和内建指令 <code>exit</code> </li><li>支持使用 <code>&gt;&gt;</code> 实现追加重定向</li></ol><h2 id="写在前面的话"><a href="#写在前面的话" class="headerlink" title="写在前面的话"></a>写在前面的话</h2><p>本篇实验报告与源代码在部分内容上有所出入，是因为在我完成整个实验，重新复盘整个实现过程时，发现早期写的有些内容虽能通过自动评测，但可能并不严谨，或者写法较为麻烦，或者有重复造轮子的现象，在写实验报告时均加以改正。由于写本篇实验报告时，自动评测已经截止，所以不能确定改正部分完全正确，<strong>如实验报告中内容出现错误，以源代码为准</strong>。</p><p>本篇实验报告的“实现细节”部分按照课程组提供的任务清单的顺序撰写，但会出现前文任务依赖后文实现的错乱情况，在真正进行挑战性任务的时候并非完全按照文中顺序完成。我个人倾向于首先完成指令自由修改和指令格式兼容，接下来完成相对路径和三个外部指令，以及追加重定向，最后再完成剩下的内容。</p><h2 id="实现细节"><a href="#实现细节" class="headerlink" title="实现细节"></a>实现细节</h2><h3 id="支持相对路径"><a href="#支持相对路径" class="headerlink" title="支持相对路径"></a>支持相对路径</h3><p>首先我们需要考虑如何存取当前路径，即当前的工作目录。这里我选择将当前路径以字符串的形式存储在 <code>struct Env</code> 结构体中，也就是每个进程都有自己的当前路径：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/env.h</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> &#123;</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// Shell: relative path and environment variables</span></span><br><span class="line">    <span class="type">char</span> relative_path[<span class="number">256</span>];</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>注意到，此时的当前路径是存储在内核中的，在用户态下我们不能直接获取其内容，需要通过 syscall 进行存取。于是我们设计两种系统调用 <code>syscall_get_relative_path</code> 和 <code>syscall_set_relative_path</code> 。在 lab 4 我们已经熟练掌握了添加新系统调用的方法，所以我们故技重施：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_relative_path</span><span class="params">(<span class="type">char</span> *res)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_set_relative_path</span><span class="params">(<span class="type">char</span> *src)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/lib/syscall_lib.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_relative_path</span><span class="params">(<span class="type">char</span> *res)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_get_relative_path, res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_set_relative_path</span><span class="params">(<span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_set_relative_path, src);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// include/syscall.h</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    ...</span><br><span class="line">    SYS_get_relative_path,</span><br><span class="line">    SYS_set_relative_path,</span><br><span class="line">    ...</span><br><span class="line">    MAX_SYSNO,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="type">void</span> *syscall_table[MAX_SYSNO] = &#123;</span><br><span class="line">    ...</span><br><span class="line">    [SYS_get_relative_path] = sys_get_relative_path,</span><br><span class="line">    [SYS_set_relative_path] = sys_set_relative_path,</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_get_relative_path</span><span class="params">(<span class="type">char</span> *res)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (res == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> -E_INVAL;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">strcpy</span>(res, curenv-&gt;relative_path);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_set_relative_path</span><span class="params">(<span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (src == <span class="literal">NULL</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> -E_INVAL;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">strcpy</span>(curenv-&gt;relative_path, src);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，我们在用户态通过调用 <code>syscall_get_relative_path()</code> 和 <code>syscall_set_relative_path()</code> 两个函数就可以实现对当前路径的存取了。</p><p>在 Shell 中，我们经常需要 fork 子进程来执行任务，为了让子进程也能读取到当前路径，我们在 fork 的同时将当前路径传递给子进程：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_exofork</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="built_in">strcpy</span>(e-&gt;relative_path, curenv-&gt;relative_path);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在我们已经能够自由存取当前路径了，接下来就是使用它了。在所有出现使用相对路径的场景，我们都需要将相对路径与当前路径进行拼接，组合成绝对路径使用。为此，我们需要创建一个将相对路径转换成绝对路径的函数 <code>rel2abs()</code> ：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">rel2abs</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *src, <span class="type">char</span> *res)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/lib/file.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">rel2abs</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *src, <span class="type">char</span> *res)</span> &#123;</span><br><span class="line">    <span class="type">char</span> relpath[<span class="number">1024</span>];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果首位是 &#x27;/&#x27; 则为绝对路径，否则为相对路径</span></span><br><span class="line">    <span class="keyword">if</span> (src[<span class="number">0</span>] == <span class="string">&#x27;/&#x27;</span>) &#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(relpath, src);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        syscall_get_relative_path(relpath);</span><br><span class="line">        <span class="type">int</span> temprellen = <span class="built_in">strlen</span>(relpath);</span><br><span class="line">        <span class="comment">// 进行拼接时，如果当前路径不以 &#x27;/&#x27; 结尾，则添加 &#x27;/&#x27; 后再拼接</span></span><br><span class="line">        <span class="keyword">if</span> (relpath[temprellen] != <span class="string">&#x27;/&#x27;</span>) &#123;</span><br><span class="line">            relpath[temprellen++] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">strcpy</span>(relpath + temprellen, src);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> rellen = <span class="built_in">strlen</span>(relpath);</span><br><span class="line">    <span class="type">int</span> <span class="built_in">stack</span>[<span class="number">1024</span>];</span><br><span class="line">    <span class="type">int</span> stacktop = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> begin = <span class="number">1</span>;</span><br><span class="line">    <span class="type">int</span> allres = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 分割路径，处理 . 和 .. </span></span><br><span class="line">    <span class="comment">// stack 数组记录处理后每层路径的起始下标</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= rellen; i++) &#123;</span><br><span class="line">        <span class="comment">// 每次读到 &#x27;/&#x27; 或字符串结束时记录下标</span></span><br><span class="line">        <span class="keyword">if</span> (i == rellen || relpath[i] == <span class="string">&#x27;/&#x27;</span>) &#123;</span><br><span class="line">            <span class="type">int</span> len = i - begin;</span><br><span class="line">            <span class="comment">// 如果本段字符串是 . 则不记录下标</span></span><br><span class="line">            <span class="keyword">if</span> (len == <span class="number">1</span> &amp;&amp; relpath[begin] == <span class="string">&#x27;.&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// do nothing</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果本段字符串是 .. 若下标个数不为 0 则弹出上一个下标</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (len == <span class="number">2</span> &amp;&amp; relpath[begin] == <span class="string">&#x27;.&#x27;</span> &amp;&amp; relpath[begin] == <span class="string">&#x27;.&#x27;</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (stacktop &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    stacktop--;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 否则记录本段字符串的起始位置</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (len &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">stack</span>[stacktop++] = begin;</span><br><span class="line">            &#125;</span><br><span class="line">            begin = i + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 若栈为空则为根目录，单走一个 &#x27;/&#x27;</span></span><br><span class="line">    <span class="keyword">if</span> (stacktop == <span class="number">0</span>) &#123;</span><br><span class="line">        res[allres++] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 否则将每个下标对应的字符串写入 res 中</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; stacktop; i++) &#123;</span><br><span class="line">        res[allres++] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> j = <span class="built_in">stack</span>[i]; j &lt; rellen &amp;&amp; relpath[j] != <span class="string">&#x27;/&#x27;</span>; j++) &#123;</span><br><span class="line">            res[allres++] = relpath[j];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    res[allres] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这样我们就实现了相对路径到绝对路径的转换，只要在用户态下调用该函数即可。目前只有 <code>user/lib/file.c</code> 中的 <code>open()</code> 函数会使用路径识别文件（其它所有函数都是使用 <code>open()</code> 函数返回的文件标识符识别文件），所以我们在 <code>open()</code> 函数中将 <code>path</code> 送进 <code>rel2path()</code> 函数中转换一下即可。</p><h3 id="加入内建指令-cd-pwd"><a href="#加入内建指令-cd-pwd" class="headerlink" title="加入内建指令 cd pwd"></a>加入内建指令 <code>cd</code> <code>pwd</code></h3><p>这里要强调的是，在我们将当前路径存储在 <code>struct Env</code> 中的前提下，<code>cd</code> 就必须是内建指令，可以参考指导书 Thinking 6.7 给出原因。对于读当前路径的操作，只要保证在 fork 出子进程之后，父进程的当前路径不会改变，我们就可以在子进程中读出父进程的当前路径；然而我们万万不可在子进程中写当前路径，这样是不能自动传回父进程的。</p><p>内建指令的要求是不能创建任何子进程，必须在主 Shell 进程中执行指令。正常情况下，我们在主函数中进入 <code>runcmd()</code> 函数之前就会 fork 一次，在进入 <code>runcmd()</code> 函数之后还会 spawn 一次，这就创建了两个子进程。在内建指令中，我们要取消掉这两次创建子进程，首先我们需要一个判断当前指令是不是内建指令的函数 <code>is_built_in_command()</code> ：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">is_built_in_command</span><span class="params">(<span class="type">char</span> *buffer)</span> &#123;</span><br><span class="line">    <span class="type">char</span> cmd[<span class="number">10</span>];</span><br><span class="line">    <span class="type">int</span> buflen = <span class="built_in">strlen</span>(buffer);</span><br><span class="line">    <span class="type">int</span> i;</span><br><span class="line">    <span class="comment">// 取出 buffer 中的第一个单词</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; buflen &amp;&amp; buffer[i] != <span class="string">&#x27; &#x27;</span>; i++) &#123;</span><br><span class="line">        cmd[i] = buffer[i];</span><br><span class="line">    &#125;</span><br><span class="line">    cmd[i] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="comment">// 进行字符串比对</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(cmd, <span class="string">&quot;cd&quot;</span>)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (...) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在主函数中，我们判断如果是内建指令，就跳过 fork 的环节：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">if</span> (!is_built_in_command(buf)) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((r = fork()) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                user_panic(<span class="string">&quot;fork: %d&quot;</span>, r);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                runcmd(buf);</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                wait(r);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            runcmd(buf);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>runcmd()</code> 函数中，我们判断如果是内建指令，就直接执行后返回，不需要再执行 spawn 及以后的内容：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">runcmd</span><span class="params">(<span class="type">char</span> *s)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], <span class="string">&quot;cd&quot;</span>)) &#123;</span><br><span class="line">        cd(argc, argv);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (...) &#123;</span><br><span class="line">        ...(argc, argv);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> child = spawn(argv[<span class="number">0</span>], argv);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在此之后我们遇到的所有内建指令都需要经过这个流程，在上述代码中的 <code>else if</code> 处继续填写即可，以后不再赘述。</p><p>下面我们就要正式实现上述代码中 <code>cd()</code> 函数的内容了。作为内建指令，<code>cd</code> 指令不是一个存储在 Shell 根目录的 <code>.b</code> 类型的可执行文件，需要 spawn 出来的子进程才能执行，它真的只是一个函数而已：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">cd</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="comment">// 如果没有指定路径，返回至根目录</span></span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">1</span>) &#123;</span><br><span class="line">        syscall_set_relative_path(<span class="string">&quot;/&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="type">char</span> abspath[<span class="number">1024</span>];</span><br><span class="line">        <span class="comment">// 将相对路径转换为绝对路径</span></span><br><span class="line">        rel2abs(argv[<span class="number">1</span>], abspath);</span><br><span class="line">        <span class="comment">// 获取文件描述符</span></span><br><span class="line">        <span class="type">int</span> fd = open(abspath, O_RDONLY);</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">Stat</span> <span class="title">st</span>;</span></span><br><span class="line">        <span class="comment">// 如果文件不存在</span></span><br><span class="line">        <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;cd: The directory &#x27;%s&#x27; does not exist\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 有 open 一定要有 close ，否则会报错</span></span><br><span class="line">        close(fd);</span><br><span class="line">        <span class="comment">// 获取文件状态（是否是目录）</span></span><br><span class="line">        stat(abspath, &amp;st);</span><br><span class="line">        <span class="keyword">if</span> (!st.st_isdir) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;cd: &#x27;%s&#x27; is not a directory\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        syscall_set_relative_path(abspath);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Too many args for cd command\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>pwd</code> 指令就更加简单了，如果把它写成内建指令的话，仿照 <code>cd</code> 指令，只需要在 <code>pwd()</code> 函数内调用一下 <code>syscall_get_relative_path()</code> 函数，再加上一些简单的异常处理即可。但是我为什么要说如果呢？因为我处于某些原因，把它写成了外部指令，这个原因后续会提到。下面就以 <code>pwd</code> 指令为例介绍一下把一个本该是内建指令写成外部指令的流程：</p><p>我们首先要做的是评判其可行性，比如我们刚才提到 <code>cd</code> 指令在我们的实现下就是不能写成外部指令的，但是我们刚才也说过，“只要保证在 fork 出子进程之后，父进程的当前路径不会改变，我们就可以在子进程中读出父进程的当前路径”，我们的 Shell 正是如此。因为我们刚才提到的一次 fork 和一次 spawn 操作都是在我们输入完当前指令后才会执行，在读出当前路径之前确实不会有机会修改当前路径，而且两个子进程在执行完指令之后也会被销毁掉，可以说把“犯罪现场”清理得干干净净，真是一场天衣无缝的“完美犯罪”~</p><p>接下来就是动手开干了！这次我们不仿照内建指令，而是仿照其它已经实现的外部指令，在 <code>user</code> 中创建一个新文件 <code>pwd.c</code> ，将实现内容写在其主函数中：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/pwd.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;pwd: expected 0 arguments; got %d\n&quot;</span>, argc - <span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="type">char</span> relpath[<span class="number">1024</span>];</span><br><span class="line">        syscall_get_relative_path(relpath);</span><br><span class="line">        <span class="comment">// 如果当前路径为空，说明在根目录，手动添加一个 &#x27;/&#x27;</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(relpath, <span class="string">&quot;&quot;</span>)) &#123;</span><br><span class="line">            relpath[<span class="number">0</span>] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">            relpath[<span class="number">1</span>] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, relpath);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来我们将其对应的 <code>.b</code> 文件（编译时会自动生成）加入到 Makefile 中。这里一定要注意的是，我们的规则不允许我们更改除了 <code>new.mk</code> 以外的所有 Makefile 文件，否则就会出现本地测试全部正常，然而交上去之后无论如何都无法通过的抓狂现象。</p><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># user/new.mk</span></span><br><span class="line">INITAPPS +=</span><br><span class="line"></span><br><span class="line">USERLIB +=</span><br><span class="line"></span><br><span class="line">USERAPPS += pwd.b</span><br></pre></td></tr></table></figure><p>还有最后一个问题，如果细心观察就可以发现，内建指令和外部指令在 Shell 中的调用方法是不同的。外部指令是一个 <code>.b</code> 文件，需要输入完整的文件路径来调用，前有 <code>/</code> ，后有 <code>.b</code>（如果当前处于根目录下，则根据相对路径可以省略 <code>/</code> ，否则会因为位置不正确而找不到指令文件）；而内部指令是一个函数，只需要输入指令名即可调用。所以在这里我们看似输入了 <code>pwd</code> ，但我们的 Shell 实际上应该将其偷偷解析成 <code>/pwd.b</code> 才能正常使用，这里的做法将会放到“支持兼容不以 <code>.b</code> 结尾的外部指令”一节去介绍。</p><h3 id="支持局部变量与全局变量"><a href="#支持局部变量与全局变量" class="headerlink" title="支持局部变量与全局变量"></a>支持局部变量与全局变量</h3><p>这一部分的内容和当前路径的存储非常相似，依旧是把变量存储于 <code>struct Env</code> 中，并通过 syscall 进行存取，只不过工作量更加巨大。首先我们在 <code>struct Env</code> 中添加变量结构体 <code>struct Envvar</code> 的数组，并记录下它们的总数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// include/env.h</span></span><br><span class="line"><span class="comment">// Environment variables</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Envvar</span> &#123;</span></span><br><span class="line">    <span class="type">char</span> key[<span class="number">16</span>];</span><br><span class="line">    <span class="type">char</span> value[<span class="number">16</span>];</span><br><span class="line">    <span class="type">int</span> is_global;    <span class="comment">// 是否是全局变量</span></span><br><span class="line">    <span class="type">int</span> is_read_only; <span class="comment">// 是否是只读变量</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Env</span> &#123;</span></span><br><span class="line">    <span class="comment">// Shell: relative path and environment variables</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Envvar</span> <span class="title">envvars</span>[16];</span></span><br><span class="line">    <span class="type">int</span> allenvvar;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这里需要注意 <code>envvars</code> 结构体的数量不能太多，否则会导致启动 Shell 时直接崩溃。</p><p>下一步就是设计一些系统调用了。由于变量相关的操作比较多，这次我们设计 4 个系统调用：用于获取一个 key 的 value 的 <code>syscall_get_envvar</code> ，用于获取全部键值对的 <code>syscall_get_all_envvar</code> ，用于写入一个键值对的 <code>syscall_put_envvar</code> ，以及用于删除一个键值对的 <code>syscall_del_envvar</code> ：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_envvar</span><span class="params">(<span class="type">char</span> *src, <span class="type">char</span> *res)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_all_envvar</span><span class="params">(<span class="type">char</span> (*res)[<span class="number">32</span>])</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_put_envvar</span><span class="params">(<span class="type">char</span> *key, <span class="type">char</span> *value, <span class="type">int</span> is_global, <span class="type">int</span> is_read_only)</span>;</span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_del_envvar</span><span class="params">(<span class="type">char</span> *src)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/lib/syscall_lib.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_envvar</span><span class="params">(<span class="type">char</span> *src, <span class="type">char</span> *res)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_get_envvar, src, res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_get_all_envvar</span><span class="params">(<span class="type">char</span> (*res)[<span class="number">32</span>])</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_get_all_envvar, res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_put_envvar</span><span class="params">(<span class="type">char</span> *key, <span class="type">char</span> *value, <span class="type">int</span> is_global, <span class="type">int</span> is_read_only)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_put_envvar, key, value, is_global, is_read_only);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">syscall_del_envvar</span><span class="params">(<span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> msyscall(SYS_del_envvar, src);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// include/syscall.h</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> &#123;</span></span><br><span class="line">    ...</span><br><span class="line">    SYS_get_envvar,</span><br><span class="line">    SYS_get_all_envvar,</span><br><span class="line">    SYS_put_envvar,</span><br><span class="line">    SYS_del_envvar</span><br><span class="line">    MAX_SYSNO,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="type">void</span> *syscall_table[MAX_SYSNO] = &#123;</span><br><span class="line">    ...</span><br><span class="line">    [SYS_get_envvar] = sys_get_envvar,</span><br><span class="line">    [SYS_get_all_envvar] = sys_get_all_envvar,</span><br><span class="line">    [SYS_put_envvar] = sys_put_envvar,</span><br><span class="line">    [SYS_del_envvar] = sys_del_envvar,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="comment">// 输入 src 为 key ，输出 res 为 value</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_get_envvar</span><span class="params">(<span class="type">char</span> *src, <span class="type">char</span> *res)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (src == <span class="literal">NULL</span> || res == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> -E_INVAL;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; curenv-&gt;allenvvar; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(curenv-&gt;envvars[i].key, src)) &#123;</span><br><span class="line">            <span class="built_in">strcpy</span>(res, curenv-&gt;envvars[i].value);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> -E_NOT_FOUND;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 输出 res 为全部键值对，格式为 &lt;key&gt;=&lt;value&gt; ，返回值为键值对总数</span></span><br><span class="line"><span class="comment">// 注意 res 的类型不能设置为 char ** ，否则会报错</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_get_all_envvar</span><span class="params">(<span class="type">char</span> (*res)[<span class="number">32</span>])</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; curenv-&gt;allenvvar; i++) &#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(res[i], curenv-&gt;envvars[i].key);</span><br><span class="line">        <span class="type">int</span> len = <span class="built_in">strlen</span>(res[i]);</span><br><span class="line">        res[i][len++] = <span class="string">&#x27;=&#x27;</span>;</span><br><span class="line">        <span class="built_in">strcpy</span>(res[i] + len, curenv-&gt;envvars[i].value);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> curenv-&gt;allenvvar;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 4 个参数全部为输入参数</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_put_envvar</span><span class="params">(<span class="type">char</span> *key, <span class="type">char</span> *value, <span class="type">int</span> is_global, <span class="type">int</span> is_read_only)</span> &#123;</span><br><span class="line">    <span class="type">int</span> allvar = curenv-&gt;allenvvar;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; curenv-&gt;allenvvar; i++) &#123;</span><br><span class="line">        <span class="comment">// 如果该变量已经存在</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(curenv-&gt;envvars[i].key, key)) &#123;</span><br><span class="line">            <span class="comment">// 只读变量不可以被修改</span></span><br><span class="line">            <span class="keyword">if</span> (curenv-&gt;envvars[i].is_read_only == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">strcpy</span>(curenv-&gt;envvars[i].value, value);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> -E_NOT_FOUND;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果该变量不存在</span></span><br><span class="line">    <span class="built_in">strcpy</span>(curenv-&gt;envvars[allvar].key, key);</span><br><span class="line">    <span class="built_in">strcpy</span>(curenv-&gt;envvars[allvar].value, value);</span><br><span class="line">    curenv-&gt;envvars[allvar].is_global = is_global;</span><br><span class="line">    curenv-&gt;envvars[allvar].is_read_only = is_read_only;</span><br><span class="line">    curenv-&gt;allenvvar++;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 输入 src 为 key</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_del_envvar</span><span class="params">(<span class="type">char</span> *src)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; curenv-&gt;allenvvar; i++) &#123;</span><br><span class="line">        <span class="comment">// 如果该变量存在</span></span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(curenv-&gt;envvars[i].key, src)) &#123;</span><br><span class="line">            <span class="comment">// 只读变量不可以被删除</span></span><br><span class="line">            <span class="keyword">if</span> (curenv-&gt;envvars[i].is_read_only == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> j = i + <span class="number">1</span>; j &lt; curenv-&gt;allenvvar; j++) &#123;</span><br><span class="line">                    curenv-&gt;envvars[j - <span class="number">1</span>] = curenv-&gt;envvars[j];</span><br><span class="line">                &#125;</span><br><span class="line">                curenv-&gt;allenvvar--;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> -E_NOT_FOUND;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> -E_NOT_FOUND;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>加入了上述定义之后，我们就可以在用户态使用 <code>syscall_..._envvar()</code> 访问这些函数了，当然这是下一节的事情了。现在我们还是来讨论一下父子进程之间变量的传递，以及如何替换指令中的变量。</p><p>还是和当前路径相同，我们在 fork 的同时向子进程传递变量，但需要注意只有全局变量会被传递，局部变量不能被传递（这就要求了 <code>declare</code> 和 <code>unset</code> 函数必须都是内建指令）：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// kern/syscall_all.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">sys_exofork</span><span class="params">(<span class="type">void</span>)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="comment">// 这里注意 allenvvar 也会发生变化，所以不能直接传值，而是每加入一个 envvar ，allenvvar++</span></span><br><span class="line">    e-&gt;allenvvar = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; curenv-&gt;allenvvar; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (curenv-&gt;envvars[i].is_global) &#123;</span><br><span class="line">            e-&gt;envvars[e-&gt;allenvvar++] = curenv-&gt;envvars[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> e-&gt;env_id;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在替换环境变量时，我们以 <code>$</code> 作为识别开始的标志，以从 <code>$</code> 开始的第一个非字母、非数字、非下划线的字符（实际上样例中只会出现空格和 <code>/</code> 两种结束符，特判一下即可），以及字符串结束作为识别结束的标志，截取这一段字符串作为 key ，使用 syscall 读出 value 后替换即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">replace_envvar</span><span class="params">(<span class="type">char</span> *buffer)</span> &#123;</span><br><span class="line">    <span class="type">char</span> res[<span class="number">1024</span>];</span><br><span class="line">    <span class="type">int</span> allres = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> buflen = <span class="built_in">strlen</span>(buffer);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; buflen; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (buffer[i] == <span class="string">&#x27;$&#x27;</span>) &#123;</span><br><span class="line">            <span class="type">char</span> key[<span class="number">16</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">            <span class="type">char</span> value[<span class="number">16</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = i + <span class="number">1</span>; j &lt; buflen &amp;&amp; buffer[j] != <span class="string">&#x27; &#x27;</span> &amp;&amp; buffer[j] != <span class="string">&#x27;/&#x27;</span>; j++) &#123;</span><br><span class="line">                key[j - i - <span class="number">1</span>] = buffer[j];</span><br><span class="line">            &#125;</span><br><span class="line">            syscall_get_envvar(key, value);</span><br><span class="line">            <span class="built_in">strcpy</span>(res + allres, value);</span><br><span class="line">            i += <span class="built_in">strlen</span>(key);</span><br><span class="line">            allres += <span class="built_in">strlen</span>(value);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            res[allres++] = buffer[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    res[allres] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>(buffer, res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">        replace_envvar(buf);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!is_built_in_command(buf)) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            runcmd(buf);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="加入内建指令-declare-unset"><a href="#加入内建指令-declare-unset" class="headerlink" title="加入内建指令 declare unset"></a>加入内建指令 <code>declare</code> <code>unset</code></h3><p>在上一节我们已经分析了这两个指令必须是内建指令，所以我们仿照 <code>cd</code> 指令，在 <code>user/sh.c</code> 中的 <code>is_built_in_command()</code> 函数和 <code>runcmd()</code> 函数中分别加入两个指令相关的分支判断，再在 <code>user/sh.c</code> 中编写两个指令对应的函数即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">declare</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> is_global = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> is_read_only = <span class="number">0</span>;</span><br><span class="line">    <span class="type">char</span> kvset[<span class="number">32</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">    <span class="type">char</span> key[<span class="number">16</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">    <span class="type">char</span> value[<span class="number">16</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// declare 后面没有参数，此时输出全部键值对内容</span></span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">char</span> envvars[<span class="number">16</span>][<span class="number">32</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">        <span class="type">int</span> allvar = syscall_get_all_envvar(envvars);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; allvar; i++) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, envvars[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 这里只取出键值对到 kvset 中，后续再进行处理</span></span><br><span class="line">    <span class="comment">// declare 后面只有一个参数，说明没有 -x, -f, -xf</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(kvset, argv[<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// declare 后面有两个参数，说明有 -x, -f, -xf</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(kvset, argv[<span class="number">2</span>]);</span><br><span class="line">        <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;-x&quot;</span>)) &#123;</span><br><span class="line">            is_global = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;-r&quot;</span>)) &#123;</span><br><span class="line">            is_read_only = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;-xr&quot;</span>)) &#123;</span><br><span class="line">            is_global = <span class="number">1</span>;</span><br><span class="line">            is_read_only = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 针对不同的属性参数，对 kvset 进行处理</span></span><br><span class="line">    <span class="type">int</span> kvsetlen = <span class="built_in">strlen</span>(kvset);</span><br><span class="line">    <span class="type">int</span> whereequal = <span class="number">-1</span>;</span><br><span class="line">    <span class="comment">// 尝试寻找 &#x27;=&#x27;</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; kvsetlen; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (kvset[i] == <span class="string">&#x27;=&#x27;</span>) &#123;</span><br><span class="line">            whereequal = i;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果未找到，说明没有设置 value ，以空字符串作为 value</span></span><br><span class="line">    <span class="keyword">if</span> (whereequal == <span class="number">-1</span>) &#123;</span><br><span class="line">        <span class="built_in">strcpy</span>(key, kvset);</span><br><span class="line">        <span class="built_in">strcpy</span>(value, <span class="string">&quot;&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 否则分别将 &#x27;=&#x27; 两边赋给 key 和 value</span></span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; whereequal; i++) &#123;</span><br><span class="line">            key[i] = kvset[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = whereequal + <span class="number">1</span>; i &lt; kvsetlen; i++) &#123;</span><br><span class="line">            value[i - whereequal - <span class="number">1</span>] = kvset[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    syscall_put_envvar(key, value, is_global, is_read_only);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">unset</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="type">int</span> r = syscall_del_envvar(argv[<span class="number">1</span>]);</span><br><span class="line">        <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持在输入指令时使用-left-arrow-和-right-arrow-移动光标"><a href="#支持在输入指令时使用-left-arrow-和-right-arrow-移动光标" class="headerlink" title="支持在输入指令时使用 left-arrow 和 right-arrow 移动光标"></a>支持在输入指令时使用 <code>left-arrow</code> 和 <code>right-arrow</code> 移动光标</h3><p>接下来我们来到了控制显示的部分，这一部分内容基本全部都在 <code>sh.c</code> 中的 <code>readline()</code> 函数里完成。这个函数的作用就是将当前使用 <code>read()</code> 函数读出的字符写入 <code>buf</code> 中，再使用 <code>printf()</code> 函数将当前的内容输出至终端。在这一部分中，我们既要维护 <code>buf</code> 中内容的正确性，还要维护终端处当前指令和光标位置的显示。</p><p><code>readline()</code> 函数原本是通过一个 for 循环，按顺序读入指令内容，功能实在可怜。现在我们需要打破按顺序读入，做到能够移动到任意位置进行插入和删除，所以需要对循环结构进行一些改造：</p><p>首先我们抛弃每循环自增 1 的倒霉孩子 <code>i</code> ，改用记录当前光标位置的 int 型变量 <code>wherenowchar</code> 以及当前读入字符的 char 型变量 <code>nowchar</code> 。另外，为了知道当前 <code>buf</code> 的尾部在哪，我们还需要维护一个 int 型变量 <code>allchar</code> 表示 <code>buf</code> 中当前所有字符的数量：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="type">int</span> wherenowchar = <span class="number">0</span>, allchar = <span class="number">0</span>;</span><br><span class="line">    <span class="type">char</span> nowchar;</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;read error: %d\n&quot;</span>, r);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">exit</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    debugf(<span class="string">&quot;line too long\n&quot;</span>);</span><br><span class="line">    <span class="keyword">while</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) == <span class="number">1</span> &amp;&amp; buf[<span class="number">0</span>] != <span class="string">&#x27;\r&#x27;</span> &amp;&amp; buf[<span class="number">0</span>] != <span class="string">&#x27;\n&#x27;</span>) &#123;</span><br><span class="line">        ;</span><br><span class="line">    &#125;</span><br><span class="line">    buf[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来我们开始实现上面代码中省略号部分的内容。要实现使用左键和右键移动光标，我们首先需要知道如何读取左键和右键，也就是此时的 <code>nowchar</code> 应该是什么。在 c 语言中，方向键会被读取为连续 3 个字符，我们需要连续读取 3 次才能识别到当前字符，上下左右键对应的字符分别如下：</p><table><thead><tr><th align="center">操作</th><th align="center">字符</th></tr></thead><tbody><tr><td align="center">up-arrow</td><td align="center">\033 + [ + A</td></tr><tr><td align="center">down-arrow</td><td align="center">\033 + [ + B</td></tr><tr><td align="center">right-arrow</td><td align="center">\033 + [ + C</td></tr><tr><td align="center">left-arrow</td><td align="center">\033 + [ + D</td></tr></tbody></table><p>（这里的 \033 是一个单独的字符，代表的是八进制下的 33 ，即十进制下的 27 ，所以使用 \x1b 也是同样的，可以自由调换）</p><p>读入左键和右键时，<code>buf</code> 中的内容没有发生改变，只有 <code>wherenowchar</code> 会发生移动，同时终端处的光标位置会发生改变。通过 printf 出上述特殊的字符串，就可以实现终端处光标的移动。但是我们还需要注意以下几个问题：一是在边界处光标位置不应当发生移动；二是当我们按下方向键时，如果光标能够向当前方向移动，它自动就会移动，而不需要我们通过 printf 移动，我们的代码需要考虑这个因素，在上述不需要移动光标的情况下进行对冲。</p><p>事实上，光标的显示是一个非常复杂的问题，尤其在边界条件的情况下往往会变得非常诡异，所以写这一部分的时候应该多跑几次，根据实际情况出发解决问题：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="comment">// 读入 &#x27;\033&#x27;</span></span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\033&#x27;</span>) &#123;</span><br><span class="line">            <span class="comment">// 读入 &#x27;[&#x27;</span></span><br><span class="line">            read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>);</span><br><span class="line">            <span class="comment">// 读入 &#x27;A/B/C/D&#x27;</span></span><br><span class="line">            read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>);</span><br><span class="line">            <span class="keyword">if</span> (nowchar == <span class="string">&#x27;A&#x27;</span>) &#123;</span><br><span class="line">                ...</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;B&#x27;</span>) &#123;</span><br><span class="line">                ...</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;C&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 如果能右移光标，则 wherenowchar++</span></span><br><span class="line">                <span class="comment">// 此时不需要处理终端处的显示，终端的光标会自己右移一格</span></span><br><span class="line">                <span class="keyword">if</span> (wherenowchar &lt; allchar) &#123;</span><br><span class="line">                    wherenowchar++;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 否则需要将终端处的光标左移回来进行对冲</span></span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[D&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;D&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 如果能左移光标，则 wherenowchar--</span></span><br><span class="line">                <span class="comment">// 此时不需要处理终端处的显示，终端的光标会自己左移一格</span></span><br><span class="line">                <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    wherenowchar--;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 否则需要将终端处的光标右移回来进行对冲</span></span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[C&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持在输入指令时在任意位置插入和删除字符"><a href="#支持在输入指令时在任意位置插入和删除字符" class="headerlink" title="支持在输入指令时在任意位置插入和删除字符"></a>支持在输入指令时在任意位置插入和删除字符</h3><p>这一部分需要我们更新原有代码中的两个逻辑：识别退格字符和其它字符。我们依旧需要维护 <code>buf</code> 数组以及相关的变量，和终端处的字符串和光标位置的显示。</p><p>对 <code>buf</code> 数组的维护比较简单，我们只需要在 <code>wherenowchar</code> 的位置处插入 <code>nowchar</code> ，或者删除这个位置的字符即可。虽然这两个操作的时间复杂度都是 O(n) ，但并不会导致时间爆炸，可以放心使用。</p><p>相比之下，终端处的显示就比较复杂了。在终端处，我们没有办法直接插入或删除一个字符，此时其它已经打印出来的位置不会发生改变。我们能够做的只有删除所有位置会发生改变的字符，并将其重新打印出来，来假装我们插入或者删除了一个字符。这里可能会用到一种新的操作 <code>\003[K</code> ，它能够删除当前光标位置右侧的所有字符。</p><p>另外，我们还需要注意两个新出现的问题：一是不可以使用其它操作直接清除整行内容，这回导致每行开头的 <code>$</code> 丢失（当然要是重新打印回来貌似也可以，我没试过）；二是在 printf 正常字符串之后光标会随之移动到刚刚 printf 出的字符串的尾部，所以我们在这部分中只要涉及到更改显示内容的情况，流程一般是 <code>向左移动光标/不移动光标 -&gt; 删除光标后所有的内容 -&gt; 打印新的内容 -&gt; 向左移动光标</code> 。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 这里注意 &#x27;\b&#x27; 是 backspace 键，0x7f 是 delete 键</span></span><br><span class="line">        <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\b&#x27;</span> || nowchar == <span class="number">0x7f</span>) &#123;</span><br><span class="line">            <span class="comment">// 判断是否在最左侧</span></span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = wherenowchar; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i - <span class="number">1</span>] = buf[i];</span><br><span class="line">                &#125;</span><br><span class="line">                wherenowchar--;</span><br><span class="line">                allchar--;</span><br><span class="line">                buf[allchar] = <span class="number">0</span>;</span><br><span class="line">                <span class="comment">// 判断是否在最右侧</span></span><br><span class="line">                <span class="comment">// 我也不知道为什么要特判这个，但是不特判就会错</span></span><br><span class="line">                <span class="keyword">if</span> (wherenowchar == allchar) &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[D\033[K&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 光标左移一格，删除右侧全部内容，输出新内容，光标左移回去</span></span><br><span class="line">                    <span class="comment">// \033[%dD 表示左移 %d 格，如 \033[5D 表示左移 5 格</span></span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[D\033[K%s\033[%dD&quot;</span>, buf + wherenowchar, allchar - wherenowchar);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = allchar - <span class="number">1</span>; i &gt;= wherenowchar; i--) &#123;</span><br><span class="line">                buf[i + <span class="number">1</span>] = buf[i];</span><br><span class="line">            &#125;</span><br><span class="line">            buf[wherenowchar++] = nowchar;</span><br><span class="line">            allchar++;</span><br><span class="line">            buf[allchar] = <span class="number">0</span>;</span><br><span class="line">            <span class="comment">// 判断是否在最右侧</span></span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &lt; allchar) &#123;</span><br><span class="line">                <span class="comment">// 删除右侧全部内容，输出新内容，光标左移回去</span></span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;\033[K%s\033[%dD&quot;</span>, buf + wherenowchar, allchar - wherenowchar);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持兼容不以-b-结尾的外部指令"><a href="#支持兼容不以-b-结尾的外部指令" class="headerlink" title="支持兼容不以 .b 结尾的外部指令"></a>支持兼容不以 <code>.b</code> 结尾的外部指令</h3><p>这里我们先实现一下这个小功能，休息一下，马上回来。在 <code>user/sh.c</code> 的 <code>runcmd()</code> 函数中，我们使用 <code>int child = spawn(argv[0], argv);</code> 这行代码 spawn 出了一个子进程，用来执行外部指令，而这里的 <code>argv[0]</code> 就是当前外部指令对应的 <code>.b</code> 文件的路径。为了兼容不含 <code>.b</code> 的外部指令路径，我们只需要在因为找不到指令文件路径而 spawn 失败的情况下，在 <code>argv[0]</code> 的后面加上一个 <code>.b</code> 字符串，再尝试 spawn 一次就好了。</p><p>但是刚才我将 <code>pwd</code> 指令偷偷改造成了外部指令，所以需要拿着 <code>pwd</code> 字符串假装输入了 <code>/pwd.b</code> 字符串，这就需要我们在加完 <code>.b</code> 之后还是找不到文件路径的情况下，再尝试在前面加上 <code>/</code> 后再次 spawn ，如法炮制即可。</p><p>其实如果不是为了适配“伪外部指令”，这个自动添加 <code>/</code> 的功能也值得一加，毕竟我们在调试的时候经常会想不起来加前面的那个 <code>/</code> ，正经人谁家指令之前还有个杠啊（笑哭）。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">runcmd</span><span class="params">(<span class="type">char</span> *s)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="type">int</span> child = spawn(argv[<span class="number">0</span>], argv);</span><br><span class="line">    <span class="keyword">if</span> (child &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="type">char</span> adddotb[<span class="number">1024</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">        <span class="built_in">strcpy</span>(adddotb, (<span class="type">char</span> *)argv[<span class="number">0</span>]);</span><br><span class="line">        <span class="built_in">strcpy</span>(adddotb + <span class="built_in">strlen</span>((<span class="type">char</span> *)argv[<span class="number">0</span>]), <span class="string">&quot;.b&quot;</span>);</span><br><span class="line">        child = spawn(adddotb, argv);</span><br><span class="line">        <span class="keyword">if</span> (child &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="type">int</span> dotlen = <span class="built_in">strlen</span>(adddotb);</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> i = dotlen; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">                adddotb[i] = adddotb[i - <span class="number">1</span>];</span><br><span class="line">            &#125;</span><br><span class="line">            adddotb[<span class="number">0</span>] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">            child = spawn(adddotb, argv);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close_all();</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持-ctrl-E-ctrl-A-ctrl-K-ctrl-U-ctrl-W-等快捷键"><a href="#支持-ctrl-E-ctrl-A-ctrl-K-ctrl-U-ctrl-W-等快捷键" class="headerlink" title="支持 ctrl-E ctrl-A ctrl-K ctrl-U ctrl-W 等快捷键"></a>支持 <code>ctrl-E</code> <code>ctrl-A</code> <code>ctrl-K</code> <code>ctrl-U</code> <code>ctrl-W</code> 等快捷键</h3><p>吃完 <code>.b</code> 这个小零食，我们继续实现显示相关的部分。之前我们已经实现了左右键和自由插入删除，这几个快捷键只需要举一反三就可以轻松解决了。这里还是要先介绍一下如何读取这几个快捷键，在 c 语言中，<code>ctrl-A</code> 对应的字符是 <code>\001</code>（注意这是单独一个字符），<code>ctrl-B</code> 对应的字符是 <code>\002</code> ，依此类推（注意是八进制，当然十六进制也可以）。下面是上述所有快捷键对应的字符：</p><table><thead><tr><th align="center">操作</th><th align="center">字符</th></tr></thead><tbody><tr><td align="center">ctrl-E</td><td align="center">\005</td></tr><tr><td align="center">ctrl-A</td><td align="center">\001</td></tr><tr><td align="center">ctrl-K</td><td align="center">\013</td></tr><tr><td align="center">ctrl-U</td><td align="center">\025</td></tr><tr><td align="center">ctrl-W</td><td align="center">\027</td></tr></tbody></table><p>另外，我们还需要注意快捷键冲突的问题。如果在跳板机上完成挑战性任务的话，<code>ctrl-A</code> 和 <code>ctrl-W</code> 会出现快捷键冲突。可以在最开始写的时候把键位改为不会发生冲突的键位，在充分调试之后确认无误的情况下，再将快捷键改回。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 我也不知道为什么会有这么多特判，反正不特判就不对</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\005&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &lt; allchar) &#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;\033[%dC&quot;</span>, allchar - wherenowchar);</span><br><span class="line">                wherenowchar = allchar;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\001&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD&quot;</span>, wherenowchar);</span><br><span class="line">                wherenowchar = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\013&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &lt; allchar) &#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;\033[K&quot;</span>);</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = wherenowchar; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i] = <span class="number">0</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                allchar = wherenowchar;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\025&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = wherenowchar; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i - wherenowchar] = buf[i];</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = allchar - wherenowchar; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i] = <span class="number">0</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (wherenowchar == allchar) &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD\033[K&quot;</span>, wherenowchar);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD\033[K%s\033[%dD&quot;</span>, wherenowchar, buf, allchar - wherenowchar);</span><br><span class="line">                &#125;</span><br><span class="line">                allchar = allchar - wherenowchar;</span><br><span class="line">                wherenowchar = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\027&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="type">int</span> flag = <span class="number">0</span>, wheredel = wherenowchar - <span class="number">1</span>;</span><br><span class="line">                <span class="comment">// 找到要被删除部分的起始位置</span></span><br><span class="line">                <span class="keyword">while</span> (wheredel &gt;= <span class="number">0</span> &amp;&amp; (flag == <span class="number">0</span> || buf[wheredel] != <span class="string">&#x27; &#x27;</span>)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (flag == <span class="number">0</span> &amp;&amp; buf[wheredel] != <span class="string">&#x27; &#x27;</span>) &#123;</span><br><span class="line">                        flag = <span class="number">1</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    wheredel--;</span><br><span class="line">                &#125;</span><br><span class="line">                wheredel++;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = wherenowchar; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i - wherenowchar + wheredel] = buf[i];</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> i = allchar - wherenowchar + wheredel; i &lt; allchar; i++) &#123;</span><br><span class="line">                    buf[i] = <span class="number">0</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (wherenowchar == allchar) &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD\033[K%s&quot;</span>, wherenowchar, buf);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD\033[K%s\033[%dD&quot;</span>, wherenowchar, buf, allchar - wherenowchar);</span><br><span class="line">                &#125;</span><br><span class="line">                allchar = allchar - wherenowchar + wheredel;</span><br><span class="line">                wherenowchar = wheredel;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持使用-up-arrow-和-down-arrow-回溯历史指令"><a href="#支持使用-up-arrow-和-down-arrow-回溯历史指令" class="headerlink" title="支持使用 up-arrow 和 down-arrow 回溯历史指令"></a>支持使用 <code>up-arrow</code> 和 <code>down-arrow</code> 回溯历史指令</h3><p>为了回溯历史指令，我们首先要做的就是将所有历史指令存储起来。课程组的要求是将所有指令存储在位于 Shell 根目录下的 <code>/.mos_history</code> 文件中，但考虑从文件中读取某一行还是比较复杂，所以我们在 <code>user/sh.c</code> 中再以二维 char 型数组的形式保存一份备份，二者同时更新。</p><p>首先我们先来探讨 <code>/.mos_history</code> 的维护。在 Shell 启动时，我们在其根目录下直接使用 <code>touch()</code> 函数创建这个文件（我们目前还未实现这个函数，可以先去实现一下）。因为这里是直接调用了 <code>touch()</code> 函数，而不是将其写入 <code>buf</code> 后按回车执行，所以这次 touch 并不会被记录在历史中。</p><p>接下来在 <code>readline()</code> 函数中，我们修改读取到回车时的操作。首先使用 <code>open()</code> 函数以写和追加的方式打开文件（我们目前也未实现追加模式，可以先去实现一下追加重定向），获取 <code>/.mos_history</code> 的文件描述符，再使用 <code>write()</code> 函数向其中追加写入当前的 <code>buf</code> ，这样就完成了对 <code>/.mos_history</code> 的维护。</p><p>紧接着我们在 <code>user/sh.c</code> 里维护一个功能相同的数组，效仿对 <code>buf</code> 相关变量的修改，我们创建一个二维 char 型数组 <code>histories</code> ，并使用 int 型变量 <code>nowhistory</code> 和 <code>allhistory</code> 记录当前指令所处的行数。当每次我们按下回车时，当前 <code>buf</code> 中的指令都会被写到 <code>histories</code> 的最后一行，并且我们当前的 <code>nowhistory</code> 也会被重置到最后一行。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">char</span> histories[<span class="number">1024</span>][<span class="number">1024</span>];</span><br><span class="line"><span class="type">int</span> allhistory = <span class="number">0</span>, nowhistory = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\r&#x27;</span> || nowchar == <span class="string">&#x27;\n&#x27;</span>) &#123;</span><br><span class="line">            buf[allchar] = <span class="number">0</span>;</span><br><span class="line">            <span class="type">int</span> fd = open(<span class="string">&quot;/.mos_history&quot;</span>, O_APPEND | O_WRONLY);</span><br><span class="line">            <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            write(fd, buf, allchar);</span><br><span class="line">            write(fd, <span class="string">&quot;\n&quot;</span>, <span class="number">1</span>);</span><br><span class="line">            close(fd);</span><br><span class="line">            <span class="built_in">strcpy</span>(histories[allhistory++], buf);</span><br><span class="line">            nowhistory = allhistory;</span><br><span class="line">            ...</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> main(<span class="type">int</span> argc, <span class="type">char</span> **argv) &#123;</span><br><span class="line">    ...</span><br><span class="line">    touch(<span class="string">&quot;/.mos_history&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>下面我们来处理上键和下键的效果，在按下上键和下键时，我们需要同时修改 <code>nowhistory</code> 的值和终端的显示。当按下上键时，如果存在上一条指令，那么 <code>nowhistory--</code> ，并且将光标位置移到指令最前面，删除后面所有内容，重新输出上一条指令的全部内容（这里不需要再左移光标，因为切换指令后，光标默认在指令尾部），并将 <code>wherenowchar</code> 的值改为当前指令的尾部。下键也是同理，这样我们就完成了上键和下键的相应。</p><p>然而真的只是这样吗？</p><p>事实上，现实中指令的回溯涉及到非常多细节，例如：相邻两条完全相同的指令会被合并；修改历史指令后如果不执行，修改内容会被保存在历史指令中；修改历史指令后如果执行，修改内容不会保存在历史指令中，而是会单独成为当前的最后一条指令被保存；当前缓冲区内的指令被存储在历史的最后一条，可以自由访问，但一旦执行了历史指令，缓冲区内保存的指令就会丢失。等等。</p><p>如果我们仅以通过评测为目的的话，可以考虑只实现一部分细节。这其中我们必须要实现的就是上述中的后两个细节。第一个细节在刚才维护 <code>histories</code> 的时候已经实现了，但第二个细节还需要我们精心设计一下，考虑如何存储这条“看似存在，实则却不存在”的缓冲区内容。</p><p>当前 <code>histories</code> 中一共有 <code>allhistory</code> 条指令，那么理论上来说 <code>nowhistory</code> 的取值范围应该是 <code>[0, allhistory - 1]</code> ，但是实际上 <code>nowhistory</code> 也可以取到 <code>allhistory</code> 这个值，这就代表着目前处于缓冲区 <code>buf</code> 中的内容并不来自于历史指令，而是我们新输入的一条内容（当然这才是我们输入指令时的常态）。</p><p>所以，当我们离开这条指令时，我们需要将这条指令保存在下来；当我们再次回到这个位置时，我们就能够读取到这条保存下来的指令。但是这条指令不能保存在 <code>histories</code> ，更不能保存在文件 <code>/.mos_history</code> 中，因为 <code>histories</code> 里面保存的都是已经执行过的，不会再被移出历史的指令，而这条指令却面临着有其它历史指令被执行就会消失的风险，所以我们为其单开一个 char 型数组 <code>buf_save</code> ，防止与其它历史指令混淆。</p><p>加入 <code>buf_save</code> 数组后，当我们离开 <code>allhistory</code> 的位置时，我们除正常操作外，额外将当前指令写入 <code>buf_save</code> ；当我们再次回到 <code>allhistory</code> 的位置时，我们从 <code>buf_save</code> 中调入指令，否则从 <code>histories</code> 的相应位置调入指令。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">char</span> buf_save[<span class="number">1024</span>];</span><br><span class="line"><span class="type">char</span> histories[<span class="number">1024</span>][<span class="number">1024</span>];</span><br><span class="line"><span class="type">int</span> allhistory = <span class="number">0</span>, nowhistory = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">readline</span><span class="params">(<span class="type">char</span> *buf, u_int n)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">while</span>(allchar &lt; n) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((r = read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>)) != <span class="number">1</span>) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (nowchar == ...) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 读 &#x27;\033&#x27;</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;\033&#x27;</span>) &#123;</span><br><span class="line">            <span class="comment">// 读 &#x27;[&#x27;</span></span><br><span class="line">            read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>);</span><br><span class="line">            <span class="comment">// 读 &#x27;A/B/C/D&#x27;</span></span><br><span class="line">            read(<span class="number">0</span>, &amp;nowchar, <span class="number">1</span>);</span><br><span class="line">            <span class="keyword">if</span> (nowchar == <span class="string">&#x27;A&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 此时光标会自动上移一格，需要手动下移一格对冲</span></span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;\033[B&quot;</span>);</span><br><span class="line">                <span class="comment">// 判断是否有上一条指令</span></span><br><span class="line">                <span class="keyword">if</span> (nowhistory &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// 如果即将离开 allhistory 的位置，则将 buf 中内容保存在 buf_save 中</span></span><br><span class="line">                    <span class="keyword">if</span> (nowhistory == allhistory) &#123;</span><br><span class="line">                        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; allchar; i++) &#123;</span><br><span class="line">                            buf_save[i] = buf[i];</span><br><span class="line">                        &#125;</span><br><span class="line">                        buf_save[allchar] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 清空当前缓冲区 buf</span></span><br><span class="line">                    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; allchar; i++) &#123;</span><br><span class="line">                        buf[i] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 从 histories 中载入上一条指令</span></span><br><span class="line">                    <span class="built_in">strcpy</span>(buf, histories[--nowhistory]);</span><br><span class="line">                    <span class="comment">// 删除当前显示的指令，输出上一条指令</span></span><br><span class="line">                    <span class="comment">// 这里又有一个奇怪的边界值特判</span></span><br><span class="line">                    <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD&quot;</span>, wherenowchar);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[K%s&quot;</span>, buf);</span><br><span class="line">                    wherenowchar = <span class="built_in">strlen</span>(buf);</span><br><span class="line">                    allchar  = <span class="built_in">strlen</span>(buf);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (nowchar == <span class="string">&#x27;B&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 判断是否有下一条指令</span></span><br><span class="line">                <span class="keyword">if</span> (nowhistory &lt; allhistory) &#123;</span><br><span class="line">                    <span class="comment">// 清空当前缓冲区 buf</span></span><br><span class="line">                    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; allchar; i++) &#123;</span><br><span class="line">                        buf[i] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 如果即将回到 allhistory 的位置，从 buf_save 将指令载入到 buf 中</span></span><br><span class="line">                    <span class="keyword">if</span> (nowhistory == allhistory - <span class="number">1</span>) &#123;</span><br><span class="line">                        <span class="built_in">strcpy</span>(buf, buf_save);</span><br><span class="line">                        nowhistory++;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 否则从 histories 中载入下一条指令</span></span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">strcpy</span>(buf, histories[++nowhistory]);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 删除当前显示的指令，输出下一条指令</span></span><br><span class="line">                    <span class="comment">// 这里还有一个奇怪的边界值特判</span></span><br><span class="line">                    <span class="keyword">if</span> (wherenowchar &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        <span class="built_in">printf</span>(<span class="string">&quot;\033[%dD&quot;</span>, wherenowchar);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;\033[K%s&quot;</span>, buf);</span><br><span class="line">                    wherenowchar = <span class="built_in">strlen</span>(buf);</span><br><span class="line">                    allchar  = <span class="built_in">strlen</span>(buf);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="加入内建指令-history"><a href="#加入内建指令-history" class="headerlink" title="加入内建指令 history"></a>加入内建指令 <code>history</code></h3><p>这一步就非常简单了，我们之前已经实现过很多内建指令了，使用 <code>history</code> 指令读取 <code>/.mos_history</code> 文件并输出即可。</p><p>首先还是在 <code>user/sh.c</code> 中的 <code>is_built_in_command()</code> 函数和 <code>runcmd()</code> 函数中加入 <code>history</code> 指令的分支判断，再在 <code>user/sh.c</code> 中编写 <code>history()</code> 函数执行指令：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">history</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="comment">// 以只读模式打开 /.mos_history ，获取文件描述符</span></span><br><span class="line">    <span class="type">int</span> fd = open(<span class="string">&quot;/.mos_history&quot;</span>, O_RDONLY);</span><br><span class="line">    <span class="type">char</span> buffer[<span class="number">8192</span>];</span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 将文件中所有内容读取到 buffer 中</span></span><br><span class="line">    r = read(fd, buffer, <span class="keyword">sizeof</span> buffer);</span><br><span class="line">    <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 输出 buffer 中的内容</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s&quot;</span>, buffer);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持使用-进行注释"><a href="#支持使用-进行注释" class="headerlink" title="支持使用 # 进行注释"></a>支持使用 <code>#</code> 进行注释</h3><p>依旧是奖励关，在执行 <code>buf</code> 中的指令之前，把第一个 <code>#</code> 以及后面的所有内容全部删掉即可。唯一需要注意的是这个动作必须发生在将该指令保存在历史记录中之后。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="comment">// 请忽略这个难绷的命名方式()</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">replace_well</span><span class="params">(<span class="type">char</span> *buffer)</span> &#123;</span><br><span class="line">    <span class="type">int</span> haswell = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> buflen = <span class="built_in">strlen</span>(buffer);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; buflen; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (buffer[i] == <span class="string">&#x27;#&#x27;</span>) &#123;</span><br><span class="line">            haswell = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (haswell == <span class="number">1</span>) &#123;</span><br><span class="line">            buffer[i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">        replace_well(buf);</span><br><span class="line">        replace_envvar(buf);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!is_built_in_command(buf)) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            runcmd(buf);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持反引号"><a href="#支持反引号" class="headerlink" title="支持反引号 `"></a>支持反引号 <code>`</code></h3><p>从这里开始，我们即将进入本次挑战性任务的最后阶段。在这个阶段，我们遇到的任务开始更加考验我们对整个操作系统的全面理解，会遇到很多魔王关，反引号就是其中的第一个。</p><p>如果仔细思考，我们就会发现，把反引号的内容摘出来并单独执行，其实并不是一件难事，真正困难的是如何取得反引号中的内容输出的结果，并写回原指令中，这就涉及到了内容的跨进程传递。</p><p>提到内容的跨进程传递，我们就很容易能够想到管道，市面上很多写法也是这样操作的。然而这种写法还是有点太吃操作了，有没有更简单的做法呢？有的兄弟，有的！</p><p>如果内容在父子进程之间通过管道传递还是太难了，我们为什么不找一个便宜实惠好用的第三方来保管传递的信息呢？没错，就是文件进程！只要我们将反引号中指令的输出重定向到文件中，再让原指令读取这个文件，就可以完美实现信息的传递了！</p><p>我们在 <code>user/sh.c</code> 中创建一个 <code>replace_backquote()</code> 函数，由这个函数执行上述操作。首先，我们需要取出反引号中的内容，这部分比较简单，就不多赘述了。取出后，我们在 Shell 的根目录创建 <code>/.backquote_temp</code> 文件（创建文件我们还是没有实现，可以先实现一下），将反引号中的指令后面添加重定向 <code> &gt; /.backquote_temp</code> ，仿照主函数执行这条反引号中的指令，这样这条指令执行后的结果就已经输出在 <code>/.backquote_temp</code> 中了。</p><p>接下来，我们只要以只读模式打开 <code>/.backquote_temp</code> ，从其中读取内容，写回原指令中反引号的位置，就完成了对 buf 中反引号的替换：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">replace_backquote</span><span class="params">(<span class="type">char</span> *buffer)</span> &#123;</span><br><span class="line">    <span class="type">char</span> res[<span class="number">1024</span>];</span><br><span class="line">    <span class="type">int</span> allres = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> buflen = <span class="built_in">strlen</span>(buf);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; buflen; i++) &#123;</span><br><span class="line">        <span class="keyword">if</span> (buffer[i] == <span class="string">&#x27;`&#x27;</span>) &#123;</span><br><span class="line">            <span class="comment">// 创建 /.backquote_temp 文件</span></span><br><span class="line">            touch(<span class="string">&quot;/.backquote_temp&quot;</span>);</span><br><span class="line">            <span class="type">char</span> innerbuf[<span class="number">1024</span>];</span><br><span class="line">            <span class="type">int</span> innerbuflen = <span class="number">0</span>;</span><br><span class="line">            <span class="comment">// 将反引号中的指令写入 innerbuf 中</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = i + <span class="number">1</span>; j &lt; buflen &amp;&amp; buffer[j] != <span class="string">&#x27;`&#x27;</span>; j++) &#123;</span><br><span class="line">                innerbuf[innerbuflen++] = buffer[j];</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 将 innerbuf 的输出重定向至 /.backquote_temp</span></span><br><span class="line">            <span class="built_in">strcpy</span>(innerbuf + innerbuflen, <span class="string">&quot; &gt; /.backquote_temp&quot;</span>);</span><br><span class="line">            i += innerbuflen + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 仿照主函数执行 innerbuf</span></span><br><span class="line">            <span class="type">int</span> r;</span><br><span class="line">            <span class="keyword">if</span> (!is_built_in_command(innerbuf)) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((r = fork()) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    user_panic(<span class="string">&quot;fork: %d&quot;</span>, r);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                    runcmd(innerbuf);</span><br><span class="line">                    <span class="built_in">exit</span>();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    wait(r);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                runcmd(innerbuf);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="type">char</span> innerres[<span class="number">1024</span>] = &#123;<span class="string">&#x27;\0&#x27;</span>&#125;;</span><br><span class="line">            <span class="comment">// 使用只读模式打开 /.backquote_temp ，获取文件描述符</span></span><br><span class="line">            <span class="type">int</span> fd = open(<span class="string">&quot;/.backquote_temp&quot;</span>, O_RDONLY);</span><br><span class="line">            <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                user_panic(<span class="string">&quot;can&#x27;t find /.backquote_temp&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 将 /.backquote_temp 中的内容读入 innerres 中</span></span><br><span class="line">            r = read(fd, innerres, <span class="keyword">sizeof</span> innerres);</span><br><span class="line">            <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                user_panic(<span class="string">&quot;can&#x27;t read /.backquote_temp&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 有 open 必有 close</span></span><br><span class="line">            close(fd);</span><br><span class="line">            <span class="type">int</span> innerreslen = <span class="built_in">strlen</span>(innerres);</span><br><span class="line">            <span class="comment">// 将 innerres 的内容写入 res 中</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="type">int</span> j = <span class="number">0</span>; j &lt; innerreslen; j++) &#123;</span><br><span class="line">                res[allres++] = innerres[j];</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 卸磨杀驴，删除 /.backquote_temp 文件</span></span><br><span class="line">            rm(<span class="string">&quot;/.backquote_temp&quot;</span>, <span class="number">0</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            res[allres++] = buffer[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    res[allres] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>(buffer,res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">        replace_well(buf);</span><br><span class="line">        replace_envvar(buf);</span><br><span class="line">        replace_backquote(buf);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!is_built_in_command(buf)) &#123;</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            runcmd(buf);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>（其实把 <code>pwd</code> 指令写成外部指令就是为了在这里能够使用，内建指令重定向会出现莫名其妙的问题，我不会修）</p><h3 id="支持使用分号-实现一行多指令"><a href="#支持使用分号-实现一行多指令" class="headerlink" title="支持使用分号 ; 实现一行多指令"></a>支持使用分号 <code>;</code> 实现一行多指令</h3><p>虽然分号的要求看起来云里雾里，但只要发现它和管道很像，只是不需要在两侧传递数据，就能够仿照管道，轻松将其秒掉了：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">parsecmd</span><span class="params">(<span class="type">char</span> **argv, <span class="type">int</span> *rightpipe)</span> &#123;</span><br><span class="line">    <span class="type">int</span> argc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">char</span> *t;</span><br><span class="line">        <span class="type">int</span> fd, r;</span><br><span class="line">        <span class="type">int</span> c = gettoken(<span class="number">0</span>, &amp;t);</span><br><span class="line">        <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;;&#x27;</span>:</span><br><span class="line">            <span class="comment">// 使用 fork 将指令分成两部分</span></span><br><span class="line">            r = fork();</span><br><span class="line">            <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;fork: %d\n&quot;</span>, r);</span><br><span class="line">            &#125;</span><br><span class="line">            *rightpipe = r;</span><br><span class="line">            <span class="comment">// 子进程（左侧部分）执行结束返回</span></span><br><span class="line">            <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> argc;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 等待子进程结束</span></span><br><span class="line">                wait(*rightpipe);</span><br><span class="line">                <span class="comment">// 父进程（右侧部分）开始执行</span></span><br><span class="line">                <span class="keyword">return</span> parsecmd(argv, rightpipe);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> argc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持使用-和-实现指令条件执行"><a href="#支持使用-和-实现指令条件执行" class="headerlink" title="支持使用 &amp;&amp; 和 || 实现指令条件执行"></a>支持使用 <code>&amp;&amp;</code> 和 <code>||</code> 实现指令条件执行</h3><p>（在实现这一步之前，必须先实现所有的外部指令 <code>mkdir</code> <code>touch</code> <code>rm</code> ）<br>根据经验，大家普遍认为这是这是最难最难的魔王关，关底 boss 级别的水准，其思路难度和代码量都已经达到了本次挑战性任务的巅峰（害怕）。</p><p>但是！只要沿用我反引号一节的实现方法，完成这个任务也并不是一件难事！且听我娓娓道来:</p><p>首先我们要做的是让 <code>user/sh.c</code> 中的 <code>parsecmd()</code> 函数能够识别双字符符号。在 <code>_gettoken()</code> 函数中，<code>t</code> 表示当前 token 的类型，<code>p1</code> 表示指向当前 token 第一个字符的二级指针，<code>p2</code> 表示指向当前 token 结束后第一个字符的二级指针。若当前 token 为特殊符号，<code>t</code> 就等于这个特殊符号本身，即 <code>&lt;</code> <code>&gt;</code> <code>|</code> <code>;</code> 等，但实际上 <code>t</code> 也可以任意定义，只要在 <code>parsecmd()</code> 函数的 switch-case 中能够将分支对应上即可。</p><p>检查 <code>user/sh.c</code> 顶部的宏定义 <code>#define SYMBOLS &quot;&lt;|&gt;&amp;;()&quot;</code>，确保我们要识别的 <code>&amp;&amp;</code> <code>||</code> <code>&gt;&gt;</code> 的第一个字符 <code>&amp;</code> <code>|</code> <code>&gt;</code> 均在其中。于是当我们读到它们的第一个字符时，我需要再判断它们后面的一个字符是否相同，如果相同就改变 <code>t</code> 的类型和 <code>p2</code> 的位置，将当前 token 变为长度为 2 的新 token ：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> _gettoken(<span class="type">char</span> *s, <span class="type">char</span> **p1, <span class="type">char</span> **p2) &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strchr</span>(SYMBOLS, *s)) &#123;</span><br><span class="line">        <span class="type">int</span> t = *s;</span><br><span class="line">        *p1 = s;</span><br><span class="line">        *s++ = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// r: 追加重定向</span></span><br><span class="line">        <span class="keyword">if</span> (t == <span class="string">&#x27;&gt;&#x27;</span> &amp;&amp; *s == <span class="string">&#x27;&gt;&#x27;</span>) &#123;</span><br><span class="line">            t = <span class="string">&#x27;r&#x27;</span>;</span><br><span class="line">            s++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// a: 条件执行</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (t == <span class="string">&#x27;&amp;&#x27;</span> &amp;&amp; *s == <span class="string">&#x27;&amp;&#x27;</span>) &#123;</span><br><span class="line">            t = <span class="string">&#x27;a&#x27;</span>;</span><br><span class="line">            s++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// o: 条件不执行</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (t == <span class="string">&#x27;|&#x27;</span> &amp;&amp; *s == <span class="string">&#x27;|&#x27;</span>) &#123;</span><br><span class="line">            t = <span class="string">&#x27;o&#x27;</span>;</span><br><span class="line">            s++;</span><br><span class="line">        &#125;</span><br><span class="line">        *p2 = s;</span><br><span class="line">        <span class="keyword">return</span> t;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>parsecmd()</code> 函数中，我们需要对其进行处理。根据刚才的经验我们不难看出，<code>&amp;&amp;</code> 和 <code>||</code> 其实和 <code>;</code> 是相同的，都是在一行连续实现多条指令，只不过这两个操作中后面的指令不一定执不执行。</p><p>如何判断后面的指令是否执行？现有的操作一般是改造 <code>exit()</code> 函数，使子进程（左侧指令）结束时通过返回值将其层层传递至父进程（右侧指令）。NO!!!!!!!!!! 不要这样做！效仿我刚才介绍的反引号实现方法，在子进程结束时，只需要将返回值写入 Shell 中的一个文件 <code>/.return_temp</code> 中，此时父进程只要从该文件中读取内容，即可无痛获取到子进程的返回值！</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">parsecmd</span><span class="params">(<span class="type">char</span> **argv, <span class="type">int</span> *rightpipe)</span> &#123;</span><br><span class="line">    <span class="type">int</span> argc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">char</span> *t;</span><br><span class="line">        <span class="type">int</span> fd, r;</span><br><span class="line">        <span class="type">int</span> c = gettoken(<span class="number">0</span>, &amp;t);</span><br><span class="line">        <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;a&#x27;</span>:</span><br><span class="line">            <span class="comment">// 使用 fork 将指令分成两部分</span></span><br><span class="line">            r = fork();</span><br><span class="line">            <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;fork: %d\n&quot;</span>, r);</span><br><span class="line">            &#125;</span><br><span class="line">            *rightpipe = r;</span><br><span class="line">            <span class="comment">// 子进程（左侧部分）执行结束返回</span></span><br><span class="line">            <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> argc;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 父进程等待子进程结束</span></span><br><span class="line">                wait(*rightpipe);</span><br><span class="line">                <span class="comment">// 以只读模式打开 /.return_temp ，获取文件描述符</span></span><br><span class="line">                <span class="type">int</span> fd = open(<span class="string">&quot;/.return_temp&quot;</span>, O_RDONLY);</span><br><span class="line">                <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    user_panic(<span class="string">&quot;can&#x27;t find /.return_temp&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="type">char</span> c;</span><br><span class="line">                <span class="comment">// 将子进程（左侧部分）的返回值读入 c 中</span></span><br><span class="line">                r = read(fd, &amp;c, <span class="number">1</span>);</span><br><span class="line">                <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    user_panic(<span class="string">&quot;can&#x27;t read /.return_temp&quot;</span>);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 有 open 必有 close</span></span><br><span class="line">                close(fd);</span><br><span class="line">                <span class="comment">// 如果子进程（左侧部分）返回值为 0</span></span><br><span class="line">                <span class="keyword">if</span> (c == <span class="string">&#x27;0&#x27;</span>) &#123;</span><br><span class="line">                    <span class="comment">// 父进程（右侧部分）开始执行</span></span><br><span class="line">                    <span class="keyword">return</span> parsecmd(argv, rightpipe);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 否则父进程（右侧部分）直接退出</span></span><br><span class="line">                <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="built_in">exit</span>();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;o&#x27;</span>:</span><br><span class="line">            <span class="comment">// 使用 fork 将指令分成两部分</span></span><br><span class="line">            r = fork();</span><br><span class="line">            <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;fork: %d\n&quot;</span>, r);</span><br><span class="line">            &#125;</span><br><span class="line">            *rightpipe = r;</span><br><span class="line">            <span class="comment">// 子进程（左侧部分）执行结束返回</span></span><br><span class="line">            <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span> argc;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 父进程等待子进程结束</span></span><br><span class="line">                    wait(*rightpipe);</span><br><span class="line">                    <span class="comment">// 以只读模式打开 /.return_temp ，获取文件描述符</span></span><br><span class="line">                    <span class="type">int</span> fd = open(<span class="string">&quot;/.return_temp&quot;</span>, O_RDONLY);</span><br><span class="line">                    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                        user_panic(<span class="string">&quot;can&#x27;t find /.return_temp&quot;</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="type">char</span> c;</span><br><span class="line">                    <span class="comment">// 将子进程（左侧部分）的返回值读入 c 中</span></span><br><span class="line">                    r = read(fd, &amp;c, <span class="number">1</span>);</span><br><span class="line">                    <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                        user_panic(<span class="string">&quot;can&#x27;t read /.return_temp&quot;</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 有 open 必有 close</span></span><br><span class="line">                    close(fd);</span><br><span class="line">                    <span class="comment">// 如果子进程（左侧部分）返回值不为 0</span></span><br><span class="line">                    <span class="keyword">if</span> (c != <span class="string">&#x27;0&#x27;</span>) &#123;</span><br><span class="line">                        <span class="comment">// 父进程（右侧部分）开始执行</span></span><br><span class="line">                        <span class="keyword">return</span> parsecmd(argv, rightpipe);</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 否则父进程（右侧部分）直接退出</span></span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="built_in">exit</span>();</span><br><span class="line">                    &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> argc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后一步，我们只需要在执行命令时将返回值写入 Shell 根目录的 <code>/.return_temp</code> 文件中即可。这一步我将在下一节进行介绍。</p><p>轻轻松松，最难关底 boss 就这样被拿下了，不是吗？</p><h3 id="加入外部指令-mkdir-touch-rm-和内建指令-exit"><a href="#加入外部指令-mkdir-touch-rm-和内建指令-exit" class="headerlink" title="加入外部指令 mkdir touch rm 和内建指令 exit"></a>加入外部指令 <code>mkdir</code> <code>touch</code> <code>rm</code> 和内建指令 <code>exit</code></h3><p>首先，我们先来解决上一节遗留下来的问题，我们可以编写一个函数 <code>note_return()</code> ，输入一个返回值，它能够自行将这个返回值写在 <code>/.return_temp</code> 文件中。为了使所有指令都能调用它，我们将其放在 <code>/user/lib</code> 文件夹的随便一个文件中，在再 <code>user/include/lib.h</code> 中注册这个函数即可：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/lib/file.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">note_return</span><span class="params">(<span class="type">int</span> r)</span> &#123;</span><br><span class="line">    <span class="comment">// 以只写模式和覆盖模式打开 /.return_temp ，获取文件描述符</span></span><br><span class="line">    <span class="type">int</span> fd = open(<span class="string">&quot;/.return_temp&quot;</span>, O_WRONLY | O_TRUNC);</span><br><span class="line">    <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        user_panic(<span class="string">&quot;can&#x27;t find /.return_temp&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">char</span> c = r + <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">    <span class="comment">// 将返回值对应的字符写入 /.return_temp 中</span></span><br><span class="line">    r = write(fd, &amp;c, <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        user_panic(<span class="string">&quot;can&#x27;t read /.return_temp&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 有 open 必有 close</span></span><br><span class="line">    close(fd);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">note_return</span><span class="params">(<span class="type">int</span> r)</span>;</span><br></pre></td></tr></table></figure><p>在 <code>user/sh.c</code> 中，我们在 Shell 启动的开始创建一个 <code>/.return_temp</code> 文件供后续使用（不需要每次都删除这个文件，因为每次写时会自动覆盖上次的内容）。其实也可以不创建，在打开该文件时加上 <code>O_CREAT</code> 模式，就可以在文件不存在时先自动创建这个文件：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    touch(<span class="string">&quot;/.mos_history&quot;</span>);</span><br><span class="line">    touch(<span class="string">&quot;/.return_temp&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>下面我们就开始正式加入上述这些指令。对于外部指令，我们只需要在 <code>user</code> 文件夹中添加其对应的 <code>.c</code> 文件，再在 <code>user/new.mk</code> 中将其对应的 <code>.b</code> 文件加入即可。</p><p>与之前不同的一点是，我们可以将这些指令的核心部分编写成函数封装在 <code>user/lib</code> 的文件中，并在 <code>user/include/lib.h</code> 中对其进行声明，这样我们就可以在用户态的所有文件中使用这些函数了，例如上一段代码中我们就在 <code>user/sh.c</code> 中使用了 <code>touch()</code> 函数：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/lib/file.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">mkdir</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> p)</span> &#123;</span><br><span class="line">    <span class="type">char</span> abspath[<span class="number">1024</span>];</span><br><span class="line">    <span class="comment">// 将相对路径转换为绝对路径</span></span><br><span class="line">    rel2abs(path, abspath);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> r = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 如果没有 -p 参数，直接打开即可</span></span><br><span class="line">    <span class="keyword">if</span> (p == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 这里用到了两个新的模式，后面会介绍到</span></span><br><span class="line">        r = open(abspath, O_MKDIR | O_EXCL);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果有 -p 参数</span></span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="type">char</span> nowpath[<span class="number">1024</span>] = &#123;<span class="string">&#x27;/&#x27;</span>&#125;;</span><br><span class="line">        <span class="type">int</span> len = <span class="built_in">strlen</span>(abspath);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= len; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i == len || abspath[i] == <span class="string">&#x27;/&#x27;</span>) &#123;</span><br><span class="line">                <span class="comment">// 从根目录开始查找，每遇到一个 &#x27;/&#x27; 或字符串结束，都会尝试创建当前的目录</span></span><br><span class="line">                r = open(abspath, O_MKDIR);</span><br><span class="line">                nowpath[i] = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                nowpath[i] = abspath[i];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">touch</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path)</span> &#123;</span><br><span class="line">    <span class="type">char</span> abspath[<span class="number">1024</span>];</span><br><span class="line">    <span class="comment">// 将相对路径转换为绝对路径</span></span><br><span class="line">    rel2abs(path, abspath);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    r = open(abspath, O_CREAT | O_EXCL);</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">rm</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *path, <span class="type">int</span> r)</span> &#123;</span><br><span class="line">    <span class="type">char</span> abspath[<span class="number">1024</span>];</span><br><span class="line">    <span class="comment">// 将相对路径转换为绝对路径</span></span><br><span class="line">    rel2abs(path, abspath);</span><br><span class="line">    </span><br><span class="line">    <span class="type">int</span> rr;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Stat</span> <span class="title">st</span>;</span></span><br><span class="line">    rr = open(abspath, O_RDONLY);</span><br><span class="line">    <span class="keyword">if</span> (rr &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> rr;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        close(rr);</span><br><span class="line">        <span class="comment">// 读取文件状态，检查是否为目录</span></span><br><span class="line">        stat(abspath, &amp;st);</span><br><span class="line">        <span class="type">int</span> isdir = st.st_isdir;</span><br><span class="line">        <span class="comment">// 如果为目录且没有 -r 参数，则报错</span></span><br><span class="line">        <span class="keyword">if</span> (isdir &amp;&amp; r == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">-114514</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            remove(abspath);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> rr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/mkdir.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="comment">// 此时只有一个参数，说明没有 -p 参数</span></span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">2</span>) &#123;</span><br><span class="line">        r = mkdir(argv[<span class="number">1</span>], <span class="number">0</span>);</span><br><span class="line">        <span class="comment">// 如果文件已经存在</span></span><br><span class="line">        <span class="keyword">if</span> (r == -E_FILE_EXISTS) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;mkdir: cannot create directory &#x27;%s&#x27;: File exists\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            note_return(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 如果上一级目录不存在</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;mkdir: cannot create directory &#x27;%s&#x27;: No such file or directory\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            note_return(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 正常情况</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            close(r);</span><br><span class="line">            note_return(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 此时有两个参数，说明有 -p 参数</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">3</span>) &#123;</span><br><span class="line">        r = mkdir(argv[<span class="number">2</span>], <span class="number">1</span>);</span><br><span class="line">        close(r);</span><br><span class="line">        note_return(<span class="number">0</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    note_return(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/touch.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">2</span>) &#123;</span><br><span class="line">        r = touch(argv[<span class="number">1</span>]);</span><br><span class="line">        <span class="comment">// 如果文件已经存在</span></span><br><span class="line">        <span class="keyword">if</span> (r == -E_FILE_EXISTS) &#123;</span><br><span class="line">            note_return(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 如果上一级目录不存在</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;touch: cannot touch &#x27;%s&#x27;: No such file or directory\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            note_return(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 正常情况</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            close(r);</span><br><span class="line">            note_return(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    note_return(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// user/rm.c</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;lib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span> &#123;</span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="comment">// 此时只有一个参数，说明没有 -r, -rf</span></span><br><span class="line">    <span class="keyword">if</span> (argc == <span class="number">2</span>) &#123;</span><br><span class="line">        r = rm(argv[<span class="number">1</span>], <span class="number">0</span>);</span><br><span class="line">        <span class="comment">// 如果该文件是目录</span></span><br><span class="line">        <span class="keyword">if</span> (r == <span class="number">-114514</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;rm: cannot remove &#x27;%s&#x27;: Is a directory\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            note_return(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 如果该文件不存在</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;rm: cannot remove &#x27;%s&#x27;: No such file or directory\n&quot;</span>, argv[<span class="number">1</span>]);</span><br><span class="line">            note_return(<span class="number">1</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 正常情况</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            note_return(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果有两个参数，说明有 -r, -rf</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (argc == <span class="number">3</span>) &#123;</span><br><span class="line">        r = rm(argv[<span class="number">2</span>], <span class="number">1</span>);</span><br><span class="line">        <span class="comment">// 如果该文件不存在</span></span><br><span class="line">        <span class="keyword">if</span> (r &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果没有 -rf 参数</span></span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;-rf&quot;</span>)) &#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;rm: cannot remove &#x27;%s&#x27;: No such file or directory\n&quot;</span>, argv[<span class="number">2</span>]);</span><br><span class="line">                note_return(<span class="number">1</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果有 -rf 参数</span></span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                note_return(<span class="number">0</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 正常情况</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            note_return(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight makefile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># user/new.mk</span></span><br><span class="line">INITAPPS +=</span><br><span class="line"></span><br><span class="line">USERLIB +=</span><br><span class="line"></span><br><span class="line">USERAPPS += mkdir.b \</span><br><span class="line">            touch.b \</span><br><span class="line">            rm.b    \</span><br><span class="line">            pwd.b</span><br></pre></td></tr></table></figure><p>这里我们对新增的三个外部指令在返回前加入了 <code>note_return()</code> ，从而向 <code>/.return_temp</code> 文件传递了返回值，我们也可以按需求对其它的所有外部指令和内部指令添加返回值传递，毋庸置疑。</p><p>刚才我们在实现 <code>mkdir</code> 和 <code>touch</code> 的过程中使用了新的模式 <code>O_MKDIR</code> 和 <code>O_EXCL</code> ，在 <code>user/include/lib.h</code> 中能够找到它们的定义：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="comment">// Unimplemented open modes</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> O_EXCL 0x0400  <span class="comment">/* error if already exists */</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> O_MKDIR 0x0800 <span class="comment">/* create directory, not regular file */</span></span></span><br></pre></td></tr></table></figure><p>可以看出，<code>O_MKDIR</code> 和 <code>O_CREAT</code> 的作用相似，后者是在找不到文件的时候创建文件，而前者则是在找不到目录的时候创建目录；<code>O_EXCL</code> 的作用则是当文件已经存在时直接报错。</p><p>然而，我们还应该注意到二者上方的 <code>Unimplemented open modes</code> ，实际上，这两个模式根本就没有在我们的代码中实现过，只是给出了定义而已，这或许是课程组留给我们的一些小提示吧。</p><p>要实现这两个内容，我们需要进入文件系统，在 <code>fs/serv.c</code> 的 <code>serv_open()</code> 函数中给出相关的实现。对于 <code>O_MKDIR</code> 模式，我们完全可以仿照 <code>O_CREAT</code> 的部分给出实现，只不过在后续需要将文件的 <code>f_type</code> 改为 <code>FTYPE_DIR</code> 。对于 <code>O_EXCL</code> 模式，只需要在创建文件时识别到 <code>-E_FILE_EXISTS</code> 错误时返回即可。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// fs/serv.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">serve_open</span><span class="params">(u_int envid, <span class="keyword">struct</span> Fsreq_open *rq)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">f</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Filefd</span> *<span class="title">ff</span>;</span></span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Open</span> *<span class="title">o</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Find a file id.</span></span><br><span class="line">    <span class="keyword">if</span> ((r = open_alloc(&amp;o)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对 O_CREAT 和 O_EXCL 进行处理</span></span><br><span class="line">    <span class="keyword">if</span> ((rq-&gt;req_omode &amp; O_CREAT) &amp;&amp; (r = file_create(rq-&gt;req_path, &amp;f)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 文件的上一级目录不存在，创建文件失败</span></span><br><span class="line">        <span class="keyword">if</span> (r != -E_FILE_EXISTS) &#123;</span><br><span class="line">            ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 文件已经存在</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (rq-&gt;req_omode &amp; O_EXCL) &#123;</span><br><span class="line">            ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 对 O_MKDIR 和 O_EXCL 进行处理</span></span><br><span class="line">    <span class="keyword">if</span> ((rq-&gt;req_omode &amp; O_MKDIR) &amp;&amp; (r = file_create(rq-&gt;req_path, &amp;f)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 文件的上一级目录不存在，创建文件失败</span></span><br><span class="line">        <span class="keyword">if</span> (r != -E_FILE_EXISTS) &#123;</span><br><span class="line">            ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 文件已经存在</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (rq-&gt;req_omode &amp; O_EXCL) &#123;</span><br><span class="line">            ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Open the file.</span></span><br><span class="line">    <span class="keyword">if</span> ((r = file_open(rq-&gt;req_path, &amp;f)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        ipc_send(envid, r, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果模式为 O_MKDIR ，则将 f-&gt;f_type 设置为 FTYPE_DIR</span></span><br><span class="line">    <span class="keyword">if</span> (rq-&gt;req_omode &amp; O_MKDIR) &#123;</span><br><span class="line">        f-&gt;f_type = FTYPE_DIR;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>最后是内建指令 <code>exit</code> ，这个指令就非常简单了，我们甚至不需要为其单开一个函数解决，在 <code>user/sh.c</code> 中，我们照例在 <code>is_built_in_command()</code> 函数和 <code>runcmd()</code> 函数的判断处添加 <code>exit</code> 指令，在 <code>runcmd()</code> 函数中，我们可以直接在 <code>return</code> 之前加上 <code>exit()</code> 跳出当前进程，即可直接实现 <code>exit</code> 指令的功能：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">runcmd</span><span class="params">(<span class="type">char</span> *s)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (...) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(argv[<span class="number">0</span>], <span class="string">&quot;exit&quot;</span>)) &#123;</span><br><span class="line">        <span class="built_in">exit</span>();</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">is_built_in_command</span><span class="params">(<span class="type">char</span> *buffer)</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (...) &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="built_in">strcmp</span>(cmd, <span class="string">&quot;exit&quot;</span>)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="支持使用-实现追加重定向"><a href="#支持使用-实现追加重定向" class="headerlink" title="支持使用 &gt;&gt; 实现追加重定向"></a>支持使用 <code>&gt;&gt;</code> 实现追加重定向</h3><p>不难想到，追加重定向与重定向肯定是十分相似的，其实二者只是在打开文件的模式上有差别：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/sh.c</span></span><br><span class="line"><span class="type">int</span> <span class="title function_">parsecmd</span><span class="params">(<span class="type">char</span> **argv, <span class="type">int</span> *rightpipe)</span> &#123;</span><br><span class="line">    <span class="type">int</span> argc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="type">char</span> *t;</span><br><span class="line">        <span class="type">int</span> fd, r;</span><br><span class="line">        <span class="type">int</span> c = gettoken(<span class="number">0</span>, &amp;t);</span><br><span class="line">        <span class="keyword">switch</span> (c) &#123;</span><br><span class="line">        ...</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&gt;&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (gettoken(<span class="number">0</span>, &amp;t) != <span class="string">&#x27;w&#x27;</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;syntax error: &gt; not followed by word\n&quot;</span>);</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            fd = open(t, O_WRONLY | O_CREAT | O_TRUNC);</span><br><span class="line">            <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;failed to open &#x27;%s&#x27;\n&quot;</span>, t);</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            dup(fd, <span class="number">1</span>);</span><br><span class="line">            close(fd);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;r&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (gettoken(<span class="number">0</span>, &amp;t) != <span class="string">&#x27;w&#x27;</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;syntax error: &gt;&gt; not followed by word\n&quot;</span>);</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            fd = open(t, O_WRONLY | O_CREAT | O_APPEND);</span><br><span class="line">            <span class="keyword">if</span> (fd &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                debugf(<span class="string">&quot;failed to open &#x27;%s&#x27;\n&quot;</span>, t);</span><br><span class="line">                <span class="built_in">exit</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            dup(fd, <span class="number">1</span>);</span><br><span class="line">            close(fd);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> argc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>（这里的 <code>r</code> 代表追加重定向，在实现 <code>&amp;&amp;</code> 和 <code>||</code> 的 token 解析时顺手实现的）</p><p>可以看到，追加重定向的打开模式由 <code>O_TRUNC</code> 变为了 <code>O_APPEND</code> ，这是一个全新的模式，需要我们自行添加。</p><p>联想刚才实现 <code>O_MKDIR</code> 和 <code>O_EXCL</code> 的过程，首先我们在 <code>user/include/lib.h</code> 中添加 <code>O_APPEND</code> 的宏定义，紧接着在 <code>fs/serv.c</code> 中修改 <code>serv_open()</code> 函数，修改打开文件时的偏移，即可实现在原文件的末尾进行添加的功能：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// user/include/lib.h</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> O_APPEND 0x0004  <span class="comment">/* write after original text */</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// fs/serv.c</span></span><br><span class="line"><span class="type">void</span> <span class="title function_">serve_open</span><span class="params">(u_int envid, <span class="keyword">struct</span> Fsreq_open *rq)</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">File</span> *<span class="title">f</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Filefd</span> *<span class="title">ff</span>;</span></span><br><span class="line">    <span class="type">int</span> r;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Open</span> *<span class="title">o</span>;</span></span><br><span class="line"></span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">// If mode include O_APPEND, set offset to file size</span></span><br><span class="line">    <span class="keyword">if</span> (rq-&gt;req_omode &amp; O_APPEND) &#123;</span><br><span class="line">        <span class="comment">// 当前偏移设置为将文件大小</span></span><br><span class="line">        ff-&gt;f_fd.fd_offset = f-&gt;f_size;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ipc_send(envid, <span class="number">0</span>, o-&gt;o_ff, PTE_D | PTE_LIBRARY);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>到这里，我们就已经实现了全部的要求，可喜可贺，可喜可贺！</p><h2 id="写在后面的话"><a href="#写在后面的话" class="headerlink" title="写在后面的话"></a>写在后面的话</h2><p>不知道为什么，今年的 Shell 挑战性任务比往年题量要大得多、难得多，导致我在紧张的期末周和繁忙的招生任务中，凭借着每天只睡四小时的骇人睡眠时长，花费整整一周写完了整个任务。</p><p>2025 年 6 月 27 日凌晨 2:53:33 ，当评测界面出现绿色的 100 的时候，我疯了，发了两个月以来的第一条朋友圈。我是非常不经常发朋友圈的，但我是真疯了。</p><p>但是话又说回来，完成整个任务之后，我觉得整个挑战性任务设置得确实很有水平，用一系列任务把整个学期的大部分知识点都串联了起来。</p><p>从 lab 3 的修改 PCB ，到 lab 4 的添加 syscall 和修改 exofork ，再到 lab 5 的文件系统操作，最后到 lab 6 的 Shell ，我确确实实地体会到了我对 MOS 操作系统的认知从一盘散沙逐渐聚起，我现在觉得我甚至能和 lab 5-extra 碰一碰（然而提交已经截止，完全看不到了，taxi 了）。</p><p>更可惜的是，我在这门课程已经全部抬走之后，才真正参透很多其中的精髓，有一种在弥留之际突然醒悟，回看自己愚蠢的一生的无奈和哀伤。</p><p>到这里，我迷茫的大二生活基本就已经画上句号了。So, in case I don’t see you, good afternoon, good evening, and good night.</p><h2 id="致谢"><a href="#致谢" class="headerlink" title="致谢"></a>致谢</h2><p>感谢以下学长和同学，你们的博客和源代码为我提供了非常丰富的灵感和思路（排名不分先后）：</p><p>CookedBear 学长：<br><a href="https://cookedbear.top/p/28193.html">https://cookedbear.top/p/28193.html</a>; <a href="https://github.com/CookedBear/BUAA-OS-2023/">https://github.com/CookedBear/BUAA-OS-2023/</a></p><p>Y(t) 学长：<br><a href="https://github.com/zhangyitonggg/BUAA-OS-challenge/">https://github.com/zhangyitonggg/BUAA-OS-challenge/</a></p><p>Lazyfish 同学：<br><a href="https://lazyfish-lc.github.io/2025/06/16/BUAA-OS-shell-challenge/">https://lazyfish-lc.github.io/2025/06/16/BUAA-OS-shell-challenge/</a></p><p>wrongization 同学：<br><a href="https://wrongization.site/2025/06/18/os-shell.html">https://wrongization.site/2025/06/18/os-shell.html</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>如何使用 vscode 优雅地完成 oop &amp; oo 课程</title>
      <link href="/Kamonto_blog/2025/06/29/%E5%A6%82%E4%BD%95%E4%BD%BF%E7%94%A8%20vscode%20%E4%BC%98%E9%9B%85%E5%9C%B0%E5%AE%8C%E6%88%90%20oop%20&amp;%20oo%20%E8%AF%BE%E7%A8%8B/"/>
      <url>/Kamonto_blog/2025/06/29/%E5%A6%82%E4%BD%95%E4%BD%BF%E7%94%A8%20vscode%20%E4%BC%98%E9%9B%85%E5%9C%B0%E5%AE%8C%E6%88%90%20oop%20&amp;%20oo%20%E8%AF%BE%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<blockquote><p>本篇文章基于 Windows 的 vscode 提供，阅读之前请确认是否正确 ~<br>如果图片不清晰，请右键图片，选择“放大图像”或“在新标签页中打开图像”</p></blockquote><h2 id="oop-使用命令行完成-git"><a href="#oop-使用命令行完成-git" class="headerlink" title="oop : 使用命令行完成 git"></a>oop : 使用命令行完成 git</h2><h3 id="下载-git"><a href="#下载-git" class="headerlink" title="下载 git"></a>下载 git</h3><p>首先，我们在<a href="https://git-scm.com/downloads"> git 官网</a>下载 git ：</p><p><img src="/Kamonto_blog/img/20250609/%E4%B8%8B%E8%BD%BDgit1.png" alt="Picture Not Found!"></p><p><img src="/Kamonto_blog/img/20250609/%E4%B8%8B%E8%BD%BDgit2.png" alt="Picture Not Found!"></p><p>下载之后，我们可以将其放在自己喜欢的位置（比如我喜欢放在 D 盘根目录）：</p><p><img src="/Kamonto_blog/img/20250609/%E5%AE%89%E8%A3%85git1.png" alt="Picture Not Found!"></p><p><img src="/Kamonto_blog/img/20250609/%E5%AE%89%E8%A3%85git2.png" alt="Picture Not Found!"></p><p>点击图片中的 Git-2.46.0-64-bit.exe ，进行安装，安装时一路点击 Next 即可。</p><h3 id="配置-git"><a href="#配置-git" class="headerlink" title="配置 git"></a>配置 git</h3><p>接下来，我们来进行 git 的配置。首先我们在任意文件夹下打开 powershell 终端（比如我现在在写博客的文件夹）：</p><p><img src="/Kamonto_blog/img/20250609/%E6%89%93%E5%BC%80powershell.png" alt="Picture Not Found!"></p><p>接着<strong>依次</strong>输入以下两条指令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git config --global user.name <span class="string">&quot;你的昵称&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;你的邮箱&quot;</span></span><br></pre></td></tr></table></figure><p>（要把 <code>你的昵称</code> 和 <code>你的邮箱</code> 换成你的昵称和邮箱，不会有人原封不动写上去吧）</p><p>为了检查是否设置成功，可以输入以下指令进行检查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --global --list</span><br></pre></td></tr></table></figure><p>如果出现类似以下内容，证明昵称和邮箱设置成功：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">user.name=Kamonto</span><br><span class="line">user.email=Kamonto@126.com</span><br><span class="line">http.proxy=http://127.0.0.1:10809</span><br></pre></td></tr></table></figure><p>（这里的值是我的昵称和邮箱，你的值应该是你刚才填的昵称和邮箱）</p><p>这样，我们就完成了 git 的配置。在之后，使用 powershell 或 bash 终端都可以进行 git 指令的操作。</p><p><img src="/Kamonto_blog/img/20250609/powershell%E5%92%8Cbash%E7%BB%88%E7%AB%AF.png" alt="Picture Not Found!"></p><h3 id="配置-SSH-Key"><a href="#配置-SSH-Key" class="headerlink" title="配置 SSH Key"></a>配置 SSH Key</h3><p>在任意目录下的 powershell 或 bash 终端输入如下指令，生成 SSH Key 密钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t rsa -C <span class="string">&quot;你的邮箱&quot;</span></span><br></pre></td></tr></table></figure><p>在生成时，我们需要输入保存密钥的地址，这里我们选择它推荐的地址即可：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Generating public/private rsa key pair.</span><br><span class="line">Enter file in which to save the key (/c/Users/24502/.ssh/id_rsa): </span><br></pre></td></tr></table></figure><p>（这里每个人的用户名是不同的，我的用户名是 24502 ）</p><p>这个地址位于家目录下的 .ssh 文件夹，等价于 <code>~/.ssh/id_rsa</code> ，记住这个 <code>~/.ssh</code> 文件夹，过后有用。</p><p>输入地址之后，我们还可以为其设置密码，这里如果直接按回车跳过的话就是不设置密码。顺带一提，我个人是比较喜欢设置密码的。</p><p>这样我们就成功生成了 SSH Key ，下面我们要在 oop 课程的 gitlab 平台添加这个密钥。</p><p>打开 gitlab 平台的任意界面，点击右上角的图像，选择 Edit profile ，进入个人主页，然后就能看到左侧的 SSH Key 选项了。</p><p><img src="/Kamonto_blog/img/20250609/gitlabSSHKey.png" alt="Picture Not Found!"></p><p>在上述界面的 Key 一栏中，我们需要输入我们的<strong>公钥</strong>，这个公钥储存在 <code>~/.ssh/id_rsa.pub</code> 中（如果你刚才将密钥存储在默认位置 <code>~/.ssh/id_rsa</code> 的话；如果不是的话，将你刚才存储密钥的位置后面加上 <code>.pub</code> 即可）。</p><p>我们在终端输入 <code>cat ~/.ssh/id_rsa.pub</code> ，复制输出的全部内容，这个内容的格式应该如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ssh-rsa</span><br><span class="line">AAAAxxxxxxxxxxxxxxxxx...(一堆乱码)= 你的邮箱</span><br></pre></td></tr></table></figure><p>这个就是我们的公钥，我们将其复制到刚才图中界面的 Key 一栏中。对于 Title 一栏，我们可以随便给密钥起个名字，起什么都没有关系。而 Expiration date 是密钥的失效日期，如果你不想在上机实验的时候 git 突然失效，那我劝你把这个日期设远一些，或者设在寒暑假期间。&lt;- 个人惨痛经历</p><p>最后点击下方的 Add key ，我们就可以使用 git 了。</p><h3 id="使用-git-进行版本管理-git-clone"><a href="#使用-git-进行版本管理-git-clone" class="headerlink" title="使用 git 进行版本管理 - git clone"></a>使用 git 进行版本管理 - git clone</h3><p>因为这是一个入门教程，在这里只介绍最基本的 git 版本管理，确保你能成功提交第一次作业。如果想学习更多的 git 知识，可以参考这本 git 神书：<a href="https://www.progit.cn/#_pro_git">Pro Git</a></p><p>现在我们学习第一个 git 指令：<code>git clone</code> 。以下面的例子为例，我想将下图中的作业内容复制在本地，该如何操作呢？</p><p><img src="/Kamonto_blog/img/20250609/gitclone1.png" alt="Picture Not Found!"></p><p>我们首先点击页面上蓝色的 clone ，使用 Clone with SSH 复制下仓库的地址。紧接着，我们使用 vscode 打开我们想将其复制到的文件夹，我们这里以下图中的 <code>oop_test</code> 文件夹为例：</p><p><img src="/Kamonto_blog/img/20250609/gitclone2.png" alt="Picture Not Found!"></p><p>在 powershell 或 bash 终端输入 <code>git clone 刚才复制下来的地址</code> ，输入密码（如果你刚才设置了的话），就可以将远程仓库的内容复制到本地了。（注意：一定要确认终端处于当前文件夹下！如果不在的话，可以使用 <code>cd</code> 指令进入想要的文件夹）</p><p>现在我们能够看到，<code>oop_test</code> 文件夹下出现了一个文件夹，名字为我们刚才复制的远程仓库的名字，这个文件夹的内容就是远程仓库的内容。</p><p>这里要注意，即使我们的作业仓库最开始是空的，也需要在最开始的时候将其 <code>git clone</code> 下来。这是因为由 <code>git clone</code> 复制下来的文件夹中<strong>记载着</strong>远程仓库的位置，这样我们写完作业后才能使用 <code>git push</code> 将我们的作业推送到远程仓库中（其实用 <code>git init</code> 和 <code>git remote add</code> 也能够实现，详情可以参考上文中的 Git Pro 教程）。</p><p>这里插一句，上一段中的“记载着”到底是如何实现的呢？我们观察从远程仓库复制下来的文件夹，其中会有一个文件夹的名字名为 <code>.git</code>（这是一个隐藏文件，需要在文件夹中选择“显示隐藏文件”选项，或在终端中使用 <code>ls -a</code> 查看）。请千万记住，这个文件夹里存放着这个仓库所有的版本管理信息，千万不要移动、修改、删除它！</p><p><img src="/Kamonto_blog/img/20250609/git.png" alt="Picture Not Found!"> </p><p>接下来，我们需要将终端切换到 <code>.git</code> 文件所在的目录（即远程仓库的根目录），这是非常重要的一步，千万不要忘记！你可以使用 <code>cd 文件夹所在位置</code> 进入该文件夹，例如上图中我就使用了 <code>cd ooprehomework_2024_xxxxxxxx_hw_4</code> 进行了切换；或者直接在 vscode 的左上角“文件”处打开你刚才复制下来的文件夹。这里我推荐后者，因为当有多个 java 工程在同一个文件夹下的话，前者会导致无法通过编译，这里我们在下一章会细说，先按下不表。如果你使用的是 bash 终端的话，你就可以像上图中在终端所在位置的后面看到当前分支的名字（一般是 master）。下面你就可以着手完成作业了！</p><h3 id="使用-git-进行版本管理-git-add-git-commit-git-push"><a href="#使用-git-进行版本管理-git-add-git-commit-git-push" class="headerlink" title="使用 git 进行版本管理 - git add, git commit, git push"></a>使用 git 进行版本管理 - git add, git commit, git push</h3><p>在完成作业后，我们肯定要提交我们的作业，但这也是一个巨大的挑战。我本人在最开始接触 oop 的时候真的很崩溃，因为从来没有哪个科目能让我连作业都交不上去的，所以我想让屏幕前的你能够顺利地完成这个挑战！</p><p>目前，我们修改了一些文件，所以很多文件的右面出现了 M 字样（即 Modified）。此外，如果我们添加了新文件，就会出现 U 字样（即 Untracked），删除文件会出现 D 字样（即 deleted）。这时候就需要我们使用 <code>git add</code> 指令对其进行暂存了。</p><p><img src="/Kamonto_blog/img/20250609/gitadd.png" alt="Picture Not Found!"></p><p>如果想对某个文件进行暂存，就可以输入 <code>git add 文件相对位置</code> 进行暂存。这里的相对位置指的是相对远程仓库的根目录的位置，如上图中 <code>Book.java</code> 的相对位置为 <code>src/Book.java</code> 。</p><p>如果想对所有 <code>.java</code> 类型的文件暂存，可以使用通配符 <code>*</code> 来解决，例如上图中情况就可以使用 <code>git add *.java</code> 进行暂存。更粗暴一点的话，使用 <code>git add .</code> 可以对当前文件夹下所有未暂存的文件进行暂存。</p><p>当然，随着我们日后的学习，我们也会发现有些东西是不适合推送至远程仓库的，例如下图中，我在其中加入了一些用于辅助的文件夹（你们会在之后再见到它们的）。实际上，如果把它们直接晾在那里，不去 <code>git add</code> 它们也可以，但是绿色的 U 实在是有点太显眼了，这时候我们就可以创建一个名为 <code>.gitignore</code> 的文件，在其中写下我们不需要上传至远程仓库的文件名。这个文件名可以是文件，也可以是文件夹，总之可以是相对于当前目录的任何路径。另外别忘了，<code>.gitignore</code> 文件本身也要写进去！</p><p><img src="/Kamonto_blog/img/20250609/gitignore1.png" alt="Picture Not Found!"></p><p><img src="/Kamonto_blog/img/20250609/gitignore2.png" alt="Picture Not Found!"></p><p>接下来，我们需要将暂存的文件进行保存，这时候就需要用到 <code>git commit</code> 指令了。在终端中输入 <code>git commit -m &quot;更新信息&quot;</code> ，就可以将当前的所有更改保存至 <code>.git</code> 文件中了。理论上来说，我还是建议好好写一写更新信息的（虽然我本人写得也很潦草），毕竟这个东西会永远留在仓库中，可能会有些尴尬。</p><p>最后，我们在经历可能一次可能多次的 <code>git commit</code> 之后，可以使用 <code>git push</code> 指令（就是 <code>git push</code> ，原封不动输入进终端就可以）把我们之前的每一次保存上传至远程的仓库中。这个环节也需要输入密码（如果你之前有设置的话），不要以为是卡死了！完成这一步之后，我们就可以在 gitlab 上看到我们上传的代码了，同时也可以在 oo 课程平台提交作业了。</p><p>很多时候，我们都很难一次提交就通过全部评测，在提交之后往往还需要再修改，不过不要担心，只要在修改之后重复上述三个步骤，就没有任何问题了！</p><p>再说一句题外话，因为上述三个操作的撤销都比较复杂，所以我们可以尽量避免错误执行上述三条指令的情况，在执行这些指令之前，我们可以在终端输入 <code>git status</code> ，这条指令可以帮助我们看到当前所有文件的状态，避免误将某些不该暂存的文件暂存、修改了不该修改的文件等异常行为。</p><h2 id="oop-运行-java-程序"><a href="#oop-运行-java-程序" class="headerlink" title="oop : 运行 java 程序"></a>oop : 运行 java 程序</h2><h3 id="vscode-中的-java-工程"><a href="#vscode-中的-java-工程" class="headerlink" title="vscode 中的 java 工程"></a>vscode 中的 java 工程</h3><p>虽然我们刚才已经学习到如何交作业了，但是我怕你还不会在 vscode 里编译运行 java 文件，所以我们接下来学习一下如何在 vscode 中完成一个 java 工程。</p><p>和我们大一学习过的程序设计和数据结构不同，我们以后的工程不再是只靠一个文件就能完成的了。在刚才的例子中我们也看到了，一次作业的内容往往由一堆 java 文件（以后还可能会有其它类型的文件）组成，这就需要引入一个更大的概念——工程。上述的所有文件都属于这个工程的一部分，我们也是围绕整个工程进行编程的。</p><p>不同于 idea 等喂饭到嘴边的工具，vscode 中并没有提供一个界面让我们手动选择项目工程的范围。但是说来也简单，vscode 认为一个工程的范围其实就是当前所打开的文件夹，比如说我们上面的所有图片中，工程的范围都是 <code>oop_test</code> 文件夹。</p><p>为什么我们要了解工程的范围呢？这其实直接关系到我们的 java 代码能否正常通过编译，例如下图中，我的 <code>oop</code> 文件夹中有 oop 课程中所有作业的工程，但如果在这个界面下运行 java 程序，就会导致任何一个工程都无法正常编译运行。这是因为 vscode 认为它们都隶属于一个大工程，如果把它们都看成一个整体的话，自然是没有办法解释的。</p><p><img src="/Kamonto_blog/img/20250609/java%E5%B7%A5%E7%A8%8B1.png" alt="Picture Not Found!"></p><p>正确的做法是选择其中的一个文件夹打开，确保左侧的列表中没有多余的 java 文件：</p><p><img src="/Kamonto_blog/img/20250609/java%E5%B7%A5%E7%A8%8B2.png" alt="Picture Not Found!"></p><p>了解了工程的概念后，我们就可以开始编程了。</p><h3 id="java-程序的编译运行"><a href="#java-程序的编译运行" class="headerlink" title="java 程序的编译运行"></a>java 程序的编译运行</h3><p>写完作业之后，我们肯定需要在本地运行我们的代码进行测试，那么 vscode 中如何运行 java 程序呢？我们知道，<del>vscode 是一款轻量级的文本编辑器</del>，所以肯定是不会自带 java 编译器的，所以我们要去插件市场下载 java 相关的插件，后续我们也会经常下载插件来辅助我们完成任务。</p><p>来到插件市场，下载这个插件：Extension Pack for Java ：</p><p><img src="/Kamonto_blog/img/20250609/extensionPackForJava.png" alt="Picture Not Found!"></p><p>接下来，我们回到编辑界面，点击任意一个 java 文件，这时你会看到左下角在加载一个叫 JAVA PROJECTS 的菜单:</p><p><img src="/Kamonto_blog/img/20250609/javaProjects.png" alt="Picture Not Found!"></p><p>这就证明了我们已经成功为这个项目加载了 java 插件。在编辑区域右键，选择 Run Java 或 Debug Java ，再在上方选择主函数所在文件（只有打开工程后第一次运行，且不是在主函数所在文件选择的 Run Java 和 Debug Java ，才会出现这个选项），就可以进入运行或者调试阶段了。</p><h2 id="oop"><a href="#oop" class="headerlink" title="oop :"></a>oop :</h2>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/Kamonto_blog/2025/05/02/hello-world/"/>
      <url>/Kamonto_blog/2025/05/02/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Test Picture</title>
      <link href="/Kamonto_blog/2025/03/26/Test-Picture/"/>
      <url>/Kamonto_blog/2025/03/26/Test-Picture/</url>
      
        <content type="html"><![CDATA[<p><img src="/Kamonto_blog/img/fdJasK1.png"></p>]]></content>
      
      
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title></title>
      <link href="/Kamonto_blog/css/custom.css"/>
      <url>/Kamonto_blog/css/custom.css</url>
      
        <content type="html"><![CDATA[/* 页脚与头图透明 */#footer {  background: transparent !important;}#page-header {  background: transparent !important;}  /* 白天模式遮罩透明 */#footer::before {  background: transparent !important;}#page-header::before {  background: transparent !important;}  /* 夜间模式遮罩透明 */[data-theme="dark"] #footer::before {  background: transparent !important;}[data-theme="dark"] #page-header::before {  background: transparent !important;}/*卡片的透明度 */:root {  --card-bg: rgba(255, 255, 255, .75);}[data-theme="dark"] {  --card-bg: rgba(18, 18, 18, .75);}/* 侧边栏个人信息卡片动态渐变色 */#aside-content > .card-widget.card-info {  background: linear-gradient(    -45deg,    #e8d8b9,    #eccec5,    #a3e9eb,    #bdbdf0,    #eec1ea  );  box-shadow: 0 0 2px rgb(66, 68, 68);  position: relative;  background-size: 400% 400%;  -webkit-animation: Gradient 10s ease infinite;  -moz-animation: Gradient 10s ease infinite;  animation: Gradient 10s ease infinite !important;}/* 黑夜模式适配 */[data-theme="dark"] #aside-content > .card-widget.card-info {  background: linear-gradient(    -45deg,    #202020,    #282828,    #1a2b42,    #2d2d44,    #1a2b2b  );  box-shadow: 0 0 2px rgb(66, 68, 68);  position: relative;  background-size: 400% 400%;  -webkit-animation: Gradient 10s ease infinite;  -moz-animation: Gradient 10s ease infinite;  animation: Gradient 10s ease infinite !important;}/* 个人信息Follow me按钮 */#aside-content > .card-widget.card-info > #card-info-btn {  background-color: #39c5bb;  border-radius: 8px;}[data-theme="dark"] #aside-content > .card-widget.card-info > #card-info-btn {  background-color: #007971;  border-radius: 8px;}@-webkit-keyframes Gradient {  0% {    background-position: 0% 50%;  }  50% {    background-position: 100% 50%;  }  100% {    background-position: 0% 50%;  }}@-moz-keyframes Gradient {  0% {    background-position: 0% 50%;  }  50% {    background-position: 100% 50%;  }  100% {    background-position: 0% 50%;  }}@keyframes Gradient {  0% {    background-position: 0% 50%;  }  50% {    background-position: 100% 50%;  }  100% {    background-position: 0% 50%;  }}/* 返回顶部 */button#go-up #percent {  display: none;  font-weight: bold;  font-size: 15px !important;}button#go-up span {  font-size: 12px!important;  margin-right: -1px;}/* 鼠标滑动到按钮上时显示返回顶部图标 */button#go-up:hover i {  display: block !important;}button#go-up:hover #percent {  display: none !important;}]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>movies</title>
      <link href="/Kamonto_blog/movies/index.html"/>
      <url>/Kamonto_blog/movies/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>关于</title>
      <link href="/Kamonto_blog/about/index.html"/>
      <url>/Kamonto_blog/about/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>music</title>
      <link href="/Kamonto_blog/music/index.html"/>
      <url>/Kamonto_blog/music/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>标签</title>
      <link href="/Kamonto_blog/tags/index.html"/>
      <url>/Kamonto_blog/tags/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>友链</title>
      <link href="/Kamonto_blog/link/index.html"/>
      <url>/Kamonto_blog/link/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
  
</search>
